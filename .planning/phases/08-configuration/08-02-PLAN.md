---
phase: 08-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/templates/SPECIAL-FLOWS.md, src/storage/flows-manager.ts, src/commands/flows.ts]
autonomous: true
requirements: [CONF-02]

must_haves:
  truths:
    - "User can initialize special flows with /openpaul:flows init"
    - "User can list all flows with /openpaul:flows list"
    - "User can enable a flow with /openpaul:flows enable"
    - "User can disable a flow with /openpaul:flows disable"
    - "Flows stored in .openpaul/SPECIAL-FLOWS.md"
  artifacts:
    - path: "src/storage/flows-manager.ts"
      provides: "Flows load/save/list/enable/disable operations"
    - path: "src/commands/flows.ts"
      provides: "/openpaul:flows command with list/enable/disable/init actions"
    - path: ".openpaul/SPECIAL-FLOWS.md"
      provides: "Special flows configuration file (created on init)"
  key_links:
    - from: "src/commands/flows.ts"
      to: "src/storage/flows-manager.ts"
      via: "FlowsManager class"
      pattern: "import.*FlowsManager"
---

<objective>
Implement FlowsManager and /openpaul:flows command for specialized workflow management.
</objective>

<context>
@.planning/phases/08-configuration/08-RESEARCH.md
@.planning/phases/08-configuration/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Check and create SPECIAL-FLOWS.md template</name>
  <files>src/templates/SPECIAL-FLOWS.md</files>
  <action>
Check if src/templates/SPECIAL-FLOWS.md already exists. If not, create it with template structure per RESEARCH.md:
- Header with project name and description
- Workflows section with example flows (security-review, docs-update)
- Each flow: name, enabled (boolean), trigger (pattern)
Use TemplateManager patterns from milestone-manager.ts for reference.
  </action>
  <verify>SPECIAL-FLOWS.md exists in src/templates/</verify>
  <done>SPECIAL-FLOWS.md template exists with workflow structure</done>
</task>

<task type="auto">
  <name>Create FlowsManager for special flows management</name>
  <files>src/storage/flows-manager.ts</files>
  <action>
Create FlowsManager class in src/storage/flows-manager.ts:
- Flow interface: { name: string; enabled: boolean; trigger?: string }
- load() method: reads .openpaul/SPECIAL-FLOWS.md, parses markdown table to Flow[]
- save() method: writes Flow[] back to markdown table format
- list() method: returns all flows
- enable(name) / disable(name): toggles flow enabled status
- resolveFlowsPath() helper: checks .openpaul, falls back to .paul
Use fs for file operations, follow ConfigManager patterns for path resolution.
  </action>
  <verify>FlowsManager can load and list flows from SPECIAL-FLOWS.md</verify>
  <done>FlowsManager class exported with load, save, list, enable, disable methods</done>
</task>

<task type="auto">
  <name>Build /openpaul:flows command</name>
  <files>src/commands/flows.ts</files>
  <action>
Create /openpaul:flows command in src/commands/flows.ts:
- action: enum ['list', 'enable', 'disable', 'init'] - default 'list'
- name: string (optional) - flow name for enable/disable
- Implement init: copies template from src/templates/SPECIAL-FLOWS.md to .openpaul/SPECIAL-FLOWS.md
- Implement list: shows all flows with enabled status
- Implement enable: sets flow.enabled = true
- Implement disable: sets flow.enabled = false
Use FlowsManager for all operations. Follow patterns from complete-milestone.ts for file generation.
  </action>
  <verify>/openpaul:flows action=list returns formatted flow list</verify>
  <done>/openpaul:flows command registered with list, enable, disable, init actions</done>
</task>

</tasks>

<verification>
FlowsManager loads/saves flows, /openpaul:flows command responds to all actions
</verification>

<success_criteria>
CONF-02: User can configure specialized flows via /openpaul:flows with SPECIAL-FLOWS.md
</success_criteria>

<output>
Create .planning/phases/08-configuration/08-02-SUMMARY.md after completion
</output>
