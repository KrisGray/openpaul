---
phase: 08-configuration
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/storage/config-manager.ts]
autonomous: true
requirements: [CONF-01]

must_haves:
  truths:
    - "Missing config keys return default values"
    - ".paul config migrates to .openpaul on first access"
    - "Invalid YAML shows parse error with line number"
    - "Config validates on load with Zod"
  artifacts:
    - path: "src/storage/config-manager.ts"
      provides: "Enhanced ConfigManager with defaults, migration, validation"
  key_links:
    - from: "src/storage/config-manager.ts"
      to: ".openpaul/config.md"
      via: "load() method with defaults merge"
      pattern: "load.*merge.*defaults"
---

<objective>
Add configuration precedence resolution logic for .openpaul/.paul compatibility and default values.
</objective>

<context>
@.planning/phases/08-configuration/08-RESEARCH.md
@.planning/phases/08-configuration/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement config precedence resolution</name>
  <files>src/storage/config-manager.ts</files>
  <action>
Enhance ConfigManager with precedence resolution:
- Create default config constants with sensible defaults (version: "1.0", preferences, etc.)
- Add getWithDefaults(key, defaultValue) method that returns value or default
- Ensure load() merges with defaults so missing keys use defaults
- Document precedence: explicit config > defaults
- Add migration helper: if .paul/config.md exists but .openpaul doesn't, copy to .openpaul
Handle .openpaul vs .paul per RESEARCH.md Pitfall 1.
  </action>
  <verify>Config with missing values returns defaults, .paul config migrates to .openpaul</verify>
  <done>ConfigManager has proper defaults and precedence handling</done>
</task>

<task type="auto">
  <name>Add config validation and error handling</name>
  <files>src/storage/config-manager.ts</files>
  <action>
Enhance ConfigManager with validation:
- Add Zod schema for ProjectConfig validation
- Validate on load, show clear error if invalid (per Pitfall 2)
- Wrap YAML parse in try-catch with actionable error messages
- Add validate() method to check config integrity
- Add fix() method to repair common issues (missing keys, wrong types)
  </action>
  <verify>Invalid YAML shows line number in error, invalid config caught on load</verify>
  <done>ConfigManager validates config and provides clear error messages</done>
</task>

</tasks>

<verification>
Config loads with defaults, invalid config shows actionable errors
</verification>

<success_criteria>
Configuration has proper precedence resolution and validation
</success_criteria>

<output>
Create .planning/phases/08-configuration/08-04-SUMMARY.md after completion
</output>
