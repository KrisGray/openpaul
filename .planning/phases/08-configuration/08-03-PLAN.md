---
phase: 08-configuration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/directory-scanner.ts, src/utils/codebase-generator.ts, src/commands/map-codebase.ts]
autonomous: true
requirements: [CONF-03]

must_haves:
  truths:
    - "User can generate codebase documentation with /openpaul:map-codebase"
    - "CODEBASE.md contains structure, stack, conventions, concerns, integrations, architecture"
    - "User can customize output path with --output flag"
    - "User can control depth with --max-depth flag"
  artifacts:
    - path: "src/utils/directory-scanner.ts"
      provides: "Recursive directory scanning with depth limiting"
    - path: "src/utils/codebase-generator.ts"
      provides: "CODEBASE.md markdown generation"
    - path: "src/commands/map-codebase.ts"
      provides: "/openpaul:map-codebase command"
    - path: "CODEBASE.md"
      provides: "Generated codebase documentation (created on run)"
  key_links:
    - from: "src/commands/map-codebase.ts"
      to: "src/utils/directory-scanner.ts"
      via: "scanDirectory function"
      pattern: "import.*scanDirectory"
    - from: "src/commands/map-codebase.ts"
      to: "src/utils/codebase-generator.ts"
      via: "generateCodebaseDoc function"
      pattern: "import.*generateCodebaseDoc"
---

<objective>
Implement /openpaul:map-codebase command for codebase documentation generation.
</objective>

<context>
@.planning/phases/08-configuration/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create directory scanning utility</name>
  <files>src/utils/directory-scanner.ts</files>
  <action>
Create directory scanning utility in src/utils/directory-scanner.ts:
- scanDirectory(dir, maxDepth, excludeDirs) recursive function
- Returns tree structure: { name, type: 'file'|'directory', children?: TreeNode[] }
- Default maxDepth: 5, exclude: ['node_modules', '.git', 'dist', 'coverage', '.opencode', '.planning']
- Handle symlinks (skip), permission errors (log and skip)
Follow pattern from RESEARCH.md: Pattern 4 - Directory Scanning for Codebase Mapping.
  </action>
  <verify>scanDirectory returns correct tree structure for src/</verify>
  <done>Directory scanner utility exported with scanDirectory function</done>
</task>

<task type="auto">
  <name>Create codebase documentation generator</name>
  <files>src/utils/codebase-generator.ts</files>
  <action>
Create codebase documentation generator in src/utils/codebase-generator.ts:
- generateCodebaseDoc(projectRoot, structure) function
- Reads package.json for tech stack info
- Generates markdown with sections per RESEARCH.md CODEBASE.md format:
  - Structure: tree visualization of scanned directories
  - Stack: table of technologies from package.json
  - Conventions: inferred from tsconfig.json, .gitignore patterns
  - Concerns: from src/ directory organization
  - Integrations: from config files (jest.config.js, tsconfig.json, etc.)
  - Architecture: command → manager → storage → filesystem pattern
Use TemplateManager patterns for template replacement.
  </action>
  <verify>generateCodebaseDoc produces valid markdown with all sections</verify>
  <done>Codebase generator exports generateCodebaseDoc function</done>
</task>

<task type="auto">
  <name>Build /openpaul:map-codebase command</name>
  <files>src/commands/map-codebase.ts</files>
  <action>
Create /openpaul:map-codebase command in src/commands/map-codebase.ts:
- output: string (optional) - output file path, default 'CODEBASE.md'
- maxDepth: number (optional) - max directory depth, default 5
- Execute: scan directory, generate doc, write with atomicWrite
- Output formatted result showing file counts and created path
Use directory-scanner and codebase-generator utilities.
  </action>
  <verify>/openpaul:map-codebase creates CODEBASE.md in project root</verify>
  <done>/openpaul:map-codebase command registered with output and maxDepth args</done>
</task>

</tasks>

<verification>
/openpaul:map-codebase creates CODEBASE.md with structure, stack, conventions, concerns, integrations, architecture
</verification>

<success_criteria>
CONF-03: User can document codebase structure via /openpaul:map-codebase creating CODEBASE.md
</success_criteria>

<output>
Create .planning/phases/08-configuration/08-03-SUMMARY.md after completion
</output>
