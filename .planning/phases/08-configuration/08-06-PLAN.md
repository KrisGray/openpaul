---
phase: 08-configuration
plan: 06
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified: [src/tests/storage/config-manager.test.ts, src/tests/storage/flows-manager.test.ts, src/tests/commands/config.test.ts, src/tests/commands/flows.test.ts, src/tests/commands/map-codebase.test.ts]
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "ConfigManager tests cover load/save/get/set/defaults/migration"
    - "FlowsManager tests cover load/save/enable/disable/list"
    - "/openpaul:config tests cover init/list/get/set"
    - "/openpaul:flows tests cover init/list/enable/disable"
    - "/openpaul:map-codebase tests cover generation with all sections"
    - "All tests pass with >80% coverage"
  artifacts:
    - path: "src/tests/storage/config-manager.test.ts"
      provides: "ConfigManager unit tests"
    - path: "src/tests/storage/flows-manager.test.ts"
      provides: "FlowsManager unit tests"
    - path: "src/tests/commands/config.test.ts"
      provides: "/openpaul:config command tests"
    - path: "src/tests/commands/flows.test.ts"
      provides: "/openpaul:flows command tests"
    - path: "src/tests/commands/map-codebase.test.ts"
      provides: "/openpaul:map-codebase command tests"
  key_links:
    - from: "src/tests/storage/config-manager.test.ts"
      to: "src/storage/config-manager.ts"
      via: "Jest describe blocks"
      pattern: "describe.*ConfigManager"
---

<objective>
Add comprehensive tests for configuration commands.
</objective>

<context>
@.planning/phases/08-configuration/08-RESEARCH.md
@.planning/phases/08-configuration/08-01-SUMMARY.md
@.planning/phases/08-configuration/08-02-SUMMARY.md
@.planning/phases/08-configuration/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create ConfigManager tests</name>
  <files>src/tests/storage/config-manager.test.ts</files>
  <action>
Create Jest tests for ConfigManager:
- test: load/save roundtrip preserves config
- test: get/set updates values correctly
- test: defaults applied when keys missing
- test: invalid YAML shows parse error
- test: .paul to .openpaul migration works
- Use per-test fs mocks per Phase 01 decision
Follow test patterns from existing tests in src/tests/
  </action>
  <verify>npm test -- config-manager.test.ts passes</verify>
  <done>ConfigManager has comprehensive test coverage</done>
</task>

<task type="auto">
  <name>Create FlowsManager tests</name>
  <files>src/tests/storage/flows-manager.test.ts</files>
  <action>
Create Jest tests for FlowsManager:
- test: load/save roundtrip preserves flows
- test: enable/disable toggles correctly
- test: list returns all flows with status
- test: non-existent flow returns error
Use per-test fs mocks.
  </action>
  <verify>npm test -- flows-manager.test.ts passes</verify>
  <done>FlowsManager has comprehensive test coverage</done>
</task>

<task type="auto">
  <name>Create config command tests</name>
  <files>src/tests/commands/config.test.ts</files>
  <action>
Create Jest tests for /openpaul:config command:
- test: init creates config file
- test: list shows all config
- test: get returns single value
- test: set updates value
- test: set with invalid key shows error
Follow command test patterns from existing tests.
  </action>
  <verify>npm test -- commands/config.test.ts passes</verify>
  <done>/openpaul:config command has test coverage</done>
</task>

<task type="auto">
  <name>Create flows and map-codebase command tests</name>
  <files>src/tests/commands/flows.test.ts, src/tests/commands/map-codebase.test.ts</files>
  <action>
Create Jest tests for /openpaul:flows and /openpaul:map-codebase:
- flows: init creates SPECIAL-FLOWS.md, list shows flows, enable/disable works
- map-codebase: creates CODEBASE.md with all sections, respects maxDepth
Use per-test fs mocks and temp directories.
  </action>
  <verify>npm test -- commands/flows.test.ts commands/map-codebase.test.ts pass</verify>
  <done>Both commands have test coverage</done>
</task>

</tasks>

<verification>
All tests pass with >80% coverage for config, flows, map-codebase
</verification>

<success_criteria>
All three commands have comprehensive Jest test coverage
</success_criteria>

<output>
Create .planning/phases/08-configuration/08-06-SUMMARY.md after completion
</output>
