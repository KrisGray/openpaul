---
phase: 08-configuration
plan: 05
type: execute
wave: 2
depends_on: [08-03]
files_modified: [src/utils/directory-scanner.ts, src/commands/map-codebase.ts]
autonomous: true
requirements: [CONF-03]

must_haves:
  truths:
    - "Second map-codebase run is fast when no changes"
    - "--force flag bypasses cache"
    - "--verbose shows progress for large directories"
    - "Cache invalidates when files change"
  artifacts:
    - path: "src/utils/directory-scanner.ts"
      provides: "Enhanced scanner with caching"
    - path: "src/commands/map-codebase.ts"
      provides: "Enhanced command with --force and --verbose flags"
    - path: ".openpaul/.codebase-cache.json"
      provides: "Cache file for incremental mapping"
  key_links:
    - from: "src/utils/directory-scanner.ts"
      to: ".openpaul/.codebase-cache.json"
      via: "File-based cache with mtime comparison"
      pattern: "cache.*mtime"
---

<objective>
Add incremental mapping support for large codebases with caching and progress indication.
</objective>

<context>
@.planning/phases/08-configuration/08-RESEARCH.md
@.planning/phases/08-configuration/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add incremental mapping with cache</name>
  <files>src/utils/directory-scanner.ts</files>
  <action>
Enhance directory-scanner with incremental mapping:
- Add cache file: .openpaul/.codebase-cache.json
- Store: { path, mtime, hash } for each file
- On scan: compare mtime, skip unchanged files (per Pitfall 3)
- Add --force flag to /openpaul:map-codebase to bypass cache
- Add cache invalidation when CODEBASE.md is older than cache
Implement per RESEARCH.md Pitfall 3: "Command hangs for >30 seconds"
  </action>
  <verify>Second run with no changes is fast, --force bypasses cache</verify>
  <done>Directory scanner uses cache for incremental mapping</done>
</task>

<task type="auto">
  <name>Add progress indication for large scans</name>
  <files>src/commands/map-codebase.ts</files>
  <action>
Enhance /openpaul:map-codebase with progress:
- Add --verbose flag for progress output
- Show file count as scanning progresses
- Warn if estimated time > 30 seconds
- Add --timeout flag to set max runtime
- Use process.stdout.write for non-blocking progress
Handle large monorepos per Pitfall 3.
  </action>
  <verify>Large directory shows progress, --verbose shows file counts</verify>
  <done>map-codebase command shows progress for large directories</done>
</task>

</tasks>

<verification>
Incremental mapping works, progress shown for large directories
</verification>

<success_criteria>
Large codebases map efficiently with incremental caching
</success_criteria>

<output>
Create .planning/phases/08-configuration/08-05-SUMMARY.md after completion
</output>
