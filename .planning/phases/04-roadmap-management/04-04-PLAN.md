---
phase: 04-roadmap-management
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/tests/roadmap/roadmap-manager.test.ts
  - src/tests/commands/add-phase.test.ts
  - src/tests/commands/remove-phase.test.ts
autonomous: true
requirements:
  - ROAD-01
  - ROAD-02

must_haves:
  truths:
    - "All RoadmapManager methods have test coverage"
    - "Add-phase command handles success and error cases"
    - "Remove-phase command validates safety checks"
    - "Tests cover edge cases (missing files, invalid phases, concurrent operations)"
  artifacts:
    - path: "src/tests/roadmap/roadmap-manager.test.ts"
      provides: "Unit tests for RoadmapManager"
      min_lines: 200
    - path: "src/tests/commands/add-phase.test.ts"
      provides: "Unit tests for add-phase command"
      min_lines: 100
    - path: "src/tests/commands/remove-phase.test.ts"
      provides: "Unit tests for remove-phase command"
      min_lines: 150
  key_links:
    - from: "src/tests/roadmap/roadmap-manager.test.ts"
      to: "src/roadmap/roadmap-manager.ts"
      via: "Jest mocks"
      pattern: "RoadmapManager"
---

<objective>
Add comprehensive tests for roadmap management operations.

Purpose: Ensure reliability and prevent regressions in phase manipulation.
Output: Test files with coverage for RoadmapManager and commands.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/tests/commands/status.test.ts
@src/roadmap/roadmap-manager.ts
@src/commands/add-phase.ts
@src/commands/remove-phase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoadmapManager tests</name>
  <files>src/tests/roadmap/roadmap-manager.test.ts</files>
  <action>
Create comprehensive tests for RoadmapManager following patterns from status.test.ts:

1. Mock dependencies:
   - Mock fs module (readFileSync, writeFileSync, existsSync, mkdirSync, rmSync, readdirSync)
   - Mock path.join

2. Test suites:
   - `describe('RoadmapManager')` with nested describes for each method

3. Test cases for resolveRoadmapPath:
   - Returns .paul/ROADMAP.md when exists
   - Returns .openpaul/ROADMAP.md as fallback
   - Returns null when neither exists

4. Test cases for parsePhases:
   - Parses phase sections (### Phase N:)
   - Parses progress table (| N. | name |)
   - Returns empty array for empty/invalid ROADMAP.md
   - Handles malformed content gracefully

5. Test cases for generateDirectoryName:
   - Lowercases name
   - Replaces spaces with hyphens
   - Removes special characters
   - Formats as `{number}-{slug}`

6. Test cases for addPhase:
   - Adds phase after reference phase
   - Adds phase before reference phase
   - Creates phase directory
   - Renumers subsequent phases in ROADMAP.md
   - Throws on missing ROADMAP.md

7. Test cases for canRemovePhase:
   - Returns valid for removable phase
   - Returns invalid for completed phase
   - Returns invalid for current phase (from STATE.md)
   - Returns invalid for phase with in-progress plans
   - Returns invalid for non-existent phase

8. Test cases for removePhase:
   - Removes phase entry from ROADMAP.md
   - Deletes phase directory
   - Renumbers subsequent phases (decrement)
   - Returns removed phase info
   - Throws on missing ROADMAP.md

Use jest.mock patterns consistent with existing tests.
  </action>
  <verify>
    <automated>npx jest src/tests/roadmap/roadmap-manager.test.ts --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>All RoadmapManager tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create add-phase command tests</name>
  <files>src/tests/commands/add-phase.test.ts</files>
  <action>
Create tests for add-phase command:

1. Mock dependencies:
   - Mock RoadmapManager
   - Mock @opencode-ai/plugin (follow status.test.ts pattern)

2. Test suites:
   - `describe('Add Phase Command')`

3. Test cases:
   - Success: adds phase with --after flag
   - Success: adds phase with --before flag
   - Success: returns brief confirmation message
   - Error: both --after and --before provided
   - Error: neither --after nor --before provided
   - Error: empty name provided
   - Error: ROADMAP.md not found
   - Error: reference phase does not exist
  </action>
  <verify>
    <automated>npx jest src/tests/commands/add-phase.test.ts --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>All add-phase command tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create remove-phase command tests</name>
  <files>src/tests/commands/remove-phase.test.ts</files>
  <action>
Create tests for remove-phase command:

1. Mock dependencies:
   - Mock RoadmapManager
   - Mock @opencode-ai/plugin (follow status.test.ts pattern)
   - Mock fs for STATE.md checks

2. Test suites:
   - `describe('Remove Phase Command')`

3. Test cases:
   - Success: removes phase and returns confirmation
   - Success: shows renumbered phases in output
   - Error: ROADMAP.md not found
   - Error: phase does not exist
   - Error: cannot remove completed phase
   - Error: cannot remove current phase
   - Error: cannot remove phase with in-progress plans
   - Error: displays all validation errors clearly
  </action>
  <verify>
    <automated>npx jest src/tests/commands/remove-phase.test.ts --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>All remove-phase command tests pass</done>
</task>

</tasks>

<verification>
- [ ] src/tests/roadmap/roadmap-manager.test.ts exists with comprehensive coverage
- [ ] src/tests/commands/add-phase.test.ts exists with success and error cases
- [ ] src/tests/commands/remove-phase.test.ts exists with safety validation tests
- [ ] All tests pass with `npx jest src/tests/roadmap src/tests/commands/add-phase.test.ts src/tests/commands/remove-phase.test.ts`
</verification>

<success_criteria>
All roadmap management code has comprehensive test coverage with passing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-roadmap-management/04-04-SUMMARY.md`
</output>
