---
phase: 01-core-infrastructure
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [src/types/state.ts, src/types/plan.ts, src/types/command.ts, src/types/loop.ts, src/types/index.ts]
autonomous: true
requirements: [INFR-02, NFR-06]
must_haves:
  truths:
    - "All core TypeScript types are defined and type-safe"
    - "Zod schemas provide runtime validation matching TypeScript types"
    - "Types can be exported for external use"
  artifacts:
    - path: "src/types/loop.ts"
      provides: "Loop phase enumeration"
      must_contain: ["LoopPhase", "PLAN", "APPLY", "UNIFY"]
    - path: "src/types/state.ts"
      provides: "State type definition with Zod schema"
      must_contain: ["State", "StateSchema", "LoopPhase"]
    - path: "src/types/plan.ts"
      provides: "Plan type definition with Zod schema"
      must_contain: ["Plan", "PlanSchema", "Task", "TaskSchema"]
    - path: "src/types/command.ts"
      provides: "Command type definitions"
      must_contain: ["Command", "CommandSchema"]
    - path: "src/types/index.ts"
      provides: "Type exports"
      must_contain: ["export * from './loop'", "export * from './state'"]
  key_links:
    - from: "src/types/state.ts"
      to: "src/types/loop.ts"
      via: "import LoopPhase"
      pattern: "import.*LoopPhase"
    - from: "src/types/plan.ts"
      to: "src/types/state.ts"
      via: "reference State type"
      pattern: "import.*State"
---

<objective>
Define all core TypeScript types (State, Plan, Command) with matching Zod schemas for runtime validation, ensuring type safety and data integrity across the PAUL framework.
</objective>

<context>
@./.planning/ROADMAP.md
@./.planning/phases/01-core-infrastructure/01-CONTEXT.md
@./.planning/phases/01-core-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create Loop phase types</name>
  <files>src/types/loop.ts</files>
  <action>
    Create src/types/loop.ts defining the loop phase enumeration:
    
    ```typescript
    import { z } from 'zod'
    
    /**
     * Loop Phase - The three stages of the PAUL loop
     * 
     * PLAN: Create executable plan with tasks and acceptance criteria
     * APPLY: Execute the approved plan sequentially
     * UNIFY: Close the loop with reconciliation and state updates
     */
    export type LoopPhase = 'PLAN' | 'APPLY' | 'UNIFY'
    
    /**
     * Zod schema for LoopPhase validation
     */
    export const LoopPhaseSchema = z.enum(['PLAN', 'APPLY', 'UNIFY'])
    
    /**
     * Valid state transitions for the loop
     * Enforces: PLAN → APPLY → UNIFY → PLAN
     */
    export const VALID_TRANSITIONS: Record<LoopPhase, LoopPhase[]> = {
      PLAN: ['APPLY'],
      APPLY: ['UNIFY'],
      UNIFY: ['PLAN'],
    }
    
    /**
     * Check if a transition from one phase to another is valid
     */
    export function isValidTransition(from: LoopPhase, to: LoopPhase): boolean {
      const allowed = VALID_TRANSITIONS[from]
      return allowed?.includes(to) ?? false
    }
    ```
    
    This defines the core loop phases and valid transitions as specified in CONTEXT.md.
  </action>
  <verify>
    <automated>grep -E "(LoopPhase|VALID_TRANSITIONS|isValidTransition)" src/types/loop.ts</automated>
  </verify>
  <done>Loop phase types and transitions defined with Zod validation</done>
</task>

<task type="auto">
  <name>Create State types</name>
  <files>src/types/state.ts</files>
  <action>
    Create src/types/state.ts defining state management types:
    
    ```typescript
    import { z } from 'zod'
    import type { LoopPhase } from './loop'
    import { LoopPhaseSchema } from './loop'
    
    /**
     * State - Current position in the PAUL loop
     * 
     * Per-phase state files (state-phase-N.json) track:
     * - Current loop position (PLAN/APPLY/UNIFY)
     * - Current plan being executed
     * - Timestamp of last state change
     * - Phase-specific metadata
     */
    export interface State {
      phase: LoopPhase
      phaseNumber: number
      currentPlanId?: string
      lastUpdated: number
      metadata: Record<string, unknown>
    }
    
    /**
     * Zod schema for State validation
     */
    export const StateSchema = z.object({
      phase: LoopPhaseSchema,
      phaseNumber: z.number().int().positive(),
      currentPlanId: z.string().optional(),
      lastUpdated: z.number(),
      metadata: z.record(z.string(), z.unknown()),
    })
    
    /**
     * Phase State - State specific to a phase (stored in state-phase-N.json)
     */
    export interface PhaseState extends State {
      phaseNumber: number
      plans: PlanReference[]
      completedPlans: string[]
    }
    
    export const PhaseStateSchema = StateSchema.extend({
      plans: z.array(z.custom<PlanReference>((data) => {
        if (typeof data === 'object' && data !== null && 'id' in data && 'status' in data) {
          return data as PlanReference
        }
        return z.custom<PlanReference>(z.any())
      })),
      completedPlans: z.array(z.string()),
    })
    
    /**
     * Plan Reference - Lightweight reference to a plan in a phase
     */
    export interface PlanReference {
      id: string
      status: 'pending' | 'in-progress' | 'completed' | 'failed'
      created: number
    }
    
    export const PlanReferenceSchema = z.object({
      id: z.string(),
      status: z.enum(['pending', 'in-progress', 'completed', 'failed']),
      created: z.number(),
    })
    ```
    
    This implements per-phase state organization as specified in CONTEXT.md.
  </action>
  <verify>
    <automated>grep -E "(interface State|StateSchema|PhaseState)" src/types/state.ts</automated>
  </verify>
  <done>State types defined with per-phase organization</done>
</task>

<task type="auto">
  <name>Create Plan and Task types</name>
  <files>src/types/plan.ts</files>
  <action>
    Create src/types/plan.ts defining plan and task structures:
    
    ```typescript
    import { z } from 'zod'
    import type { LoopPhase } from './loop'
    
    /**
     * Task - Individual unit of work within a plan
     * 
     * Each task has:
     * - Type (auto, checkpoint:human-verify, checkpoint:decision, checkpoint:human-action)
     * - Name (descriptive identifier)
     * - Files (paths to create/modify)
     * - Action (implementation instructions)
     * - Verify (how to test completion)
     * - Done (acceptance criteria)
     */
    export type TaskType = 'auto' | 'checkpoint:human-verify' | 'checkpoint:decision' | 'checkpoint:human-action'
    
    export interface Task {
      type: TaskType
      name: string
      files?: string[]
      action: string
      verify: string
      done: string
    }
    
    export const TaskSchema = z.object({
      type: z.enum(['auto', 'checkpoint:human-verify', 'checkpoint:decision', 'checkpoint:human-action']),
      name: z.string().min(1),
      files: z.array(z.string()).optional(),
      action: z.string().min(1),
      verify: z.string().min(1),
      done: z.string().min(1),
    })
    
    /**
     * Plan - Executable plan with tasks
     * 
     * Plans contain:
     * - Phase and plan identifiers
     * - Type (execute or tdd)
     * - Wave number for parallel execution
     * - Dependencies on other plans
     * - Files to be modified
     * - Autonomous flag (true if no checkpoints)
     * - Tasks to execute
     * - Must-haves for goal-backward verification
     */
    export interface Plan {
      phase: string
      plan: string
      type: 'execute' | 'tdd'
      wave: number
      depends_on: string[]
      files_modified: string[]
      autonomous: boolean
      requirements: string[]
      tasks: Task[]
      must_haves: MustHaves
    }
    
    export const PlanSchema = z.object({
      phase: z.string(),
      plan: z.string(),
      type: z.enum(['execute', 'tdd']),
      wave: z.number().int().nonnegative(),
      depends_on: z.array(z.string()),
      files_modified: z.array(z.string()),
      autonomous: z.boolean(),
      requirements: z.array(z.string()).min(1),
      tasks: z.array(TaskSchema).min(1).max(5),
      must_haves: MustHavesSchema,
    })
    
    /**
     * Must-Haves - Goal-backward verification criteria
     */
    export interface MustHaves {
      truths: string[]
      artifacts: Artifact[]
      key_links: KeyLink[]
    }
    
    export const MustHavesSchema = z.object({
      truths: z.array(z.string()).min(1),
      artifacts: z.array(ArtifactSchema).min(1),
      key_links: z.array(KeyLinkSchema),
    })
    
    export interface Artifact {
      path: string
      provides: string
      must_contain?: string[]
      min_lines?: number
    }
    
    export const ArtifactSchema = z.object({
      path: z.string(),
      provides: z.string(),
      must_contain: z.array(z.string()).optional(),
      min_lines: z.number().int().positive().optional(),
    })
    
    export interface KeyLink {
      from: string
      to: string
      via: string
      pattern: string
    }
    
    export const KeyLinkSchema = z.object({
      from: z.string(),
      to: z.string(),
      via: z.string(),
      pattern: z.string(),
    })
    ```
    
    This defines the complete plan and task structures with all required fields.
  </action>
  <verify>
    <automated>grep -E "(interface Plan|PlanSchema|interface Task)" src/types/plan.ts</automated>
  </verify>
  <done>Plan and Task types defined with Zod schemas</done>
</task>

<task type="auto">
  <name>Create Command types</name>
  <files>src/types/command.ts</files>
  <action>
    Create src/types/command.ts defining command types:
    
    ```typescript
    import { z } from 'zod'
    import type { LoopPhase } from './loop'
    
    /**
     * Command - User command to interact with PAUL
     * 
     * Commands include:
     * - /paul:init - Initialize PAUL in project
     * - /paul:plan - Create executable plan
     * - /paul:apply - Execute approved plan
     * - /paul:unify - Close the loop
     * - /paul:progress - View current status
     * - /paul:help - Command reference
     * - And 20 more commands from roadmap
     */
    export type CommandType =
      | 'init'
      | 'plan'
      | 'apply'
      | 'unify'
      | 'progress'
      | 'help'
      | 'pause'
      | 'resume'
      | 'handoff'
      | 'status'
      | 'milestone'
      | 'complete-milestone'
      | 'discuss-milestone'
      | 'map-codebase'
      | 'discuss'
      | 'assumptions'
      | 'discover'
      | 'consider-issues'
      | 'research'
      | 'research-phase'
      | 'verify'
      | 'plan-fix'
      | 'add-phase'
      | 'remove-phase'
      | 'flows'
      | 'config'
    
    /**
     * Command Input - User input for a command
     */
    export interface CommandInput {
        type: CommandType
        args?: string[]
        flags?: Record<string, unknown>
      }
    
    export const CommandInputSchema = z.object({
        type: z.enum([
          'init', 'plan', 'apply', 'unify', 'progress', 'help',
          'pause', 'resume', 'handoff', 'status',
          'milestone', 'complete-milestone', 'discuss-milestone', 'map-codebase',
          'discuss', 'assumptions', 'discover', 'consider-issues',
          'research', 'research-phase', 'verify', 'plan-fix',
          'add-phase', 'remove-phase', 'flows', 'config',
        ]),
        args: z.array(z.string()).optional(),
        flags: z.record(z.string(), z.unknown()).optional(),
      })
    
    /**
     * Command Result - Result of executing a command
     */
    export interface CommandResult {
        success: boolean
        message: string
        data?: unknown
        nextAction?: string
      }
    
    export const CommandResultSchema = z.object({
        success: z.boolean(),
        message: z.string(),
        data: z.unknown().optional(),
        nextAction: z.string().optional(),
      })
    
    /**
     * Command Handler - Function that handles a command
     */
    export type CommandHandler = (
        input: CommandInput,
        context: CommandContext
    ) => Promise<CommandResult>
    
    /**
     * Command Context - Context passed to command handlers
     */
    export interface CommandContext {
        projectRoot: string
        currentState: LoopPhase
        phaseNumber: number
    }
    ```
    
    This defines all 26 PAUL commands and input/result structures.
  </action>
  <verify>
    <automated>grep -E "(CommandType|interface Command)" src/types/command.ts</automated>
  </verify>
  <done>Command types defined for all 26 PAUL commands</done>
</task>

<task type="auto">
  <name>Create type exports</name>
  <files>src/types/index.ts</files>
  <action>
    Create src/types/index.ts to export all types:
    
    ```typescript
    /**
     * PAUL Framework Type Exports
     * 
     * This file exports all types for external use.
     * Users can import from '@krisgray/openpaul' and get:
     * - LoopPhase and state machine utilities
     * - State and PhaseState types
     * - Plan and Task types
     * - Command types and handlers
     * - Zod schemas for runtime validation
     */
    
    // Loop types
    export * from './loop'
    
    // State types
    export * from './state'
    
    // Plan types
    export * from './plan'
    
    // Command types
    export * from './command'
    ```
    
    This enables maximum extensibility as required by CONTEXT.md.
  </action>
  <verify>
    <automated>grep -E "export \* from" src/types/index.ts | wc -l</automated>
    <sampling_rate>Run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All types exported from central index file</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
- TypeScript compiles without errors: `npm run build`
- All types have matching Zod schemas
- Types can be imported from src/types/index.ts
- Zod schemas validate correctly (test with sample data)
</verification>

<success_criteria>
- All core TypeScript types (State, Plan, Command) are defined and type-safe
- State can be saved to and loaded from JSON files reliably
- Zod schemas provide runtime validation for all types
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-02-SUMMARY.md` documenting:
- Types defined (LoopPhase, State, Plan, Task, Command)
- Zod schemas created for runtime validation
- Type exports configured
- Any decisions made during implementation
</output>
