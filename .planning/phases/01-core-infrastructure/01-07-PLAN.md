---
phase: 01-core-infrastructure
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [src/tests/storage/atomic-writes.test.ts]
autonomous: true
gap_closure: true
requirements: [INFR-03, NFR-02, NFR-04]

must_haves:
  truths:
    - "Atomic writes handle errors gracefully with temp file cleanup"
    - "Validation errors are thrown before file writes occur"
    - "Error cleanup paths are tested and verified"
  artifacts:
    - path: "src/tests/storage/atomic-writes.test.ts"
      provides: "Error handling test coverage for atomic writes"
      min_lines: 100
  key_links:
    - from: "src/tests/storage/atomic-writes.test.ts"
      to: "src/storage/atomic-writes.ts"
      via: "import and test"
      pattern: "atomicWriteValidated|toThrow"
---

<objective>
Add error handling test coverage for atomic-writes.ts to reach 80% branch coverage

Purpose: Ensure error cleanup paths and validation errors are properly tested
Output: Comprehensive error handling tests in atomic-writes.test.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-infrastructure/01-03-SUMMARY.md

# Gap context from VERIFICATION.md
- Branch coverage 75% in atomic-writes.ts (below 80%)
- Lines 44-51: Error cleanup path when write/rename fails
- Lines 70-76: Validation error path in atomicWriteValidated

# Source file reference
@src/storage/atomic-writes.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Add error handling tests for atomic writes</name>
  <files>src/tests/storage/atomic-writes.test.ts</files>
  <action>
Add the following test cases to the existing test file:

1. **Test atomicWriteValidated with valid data** - Verify atomicWriteValidated works with valid schema and data
2. **Test atomicWriteValidated with invalid data** - Verify atomicWriteValidated throws Zod error when schema validation fails (covers line 70)
3. **Test atomicWriteValidated creates file with validated content** - Verify the validated data is written to file
4. **Test error cleanup when rename fails** - Mock or trigger a scenario where renameSync fails after successful temp file write, verify temp file is cleaned up (covers lines 44-51)
5. **Test error includes original error** - Verify that when an error occurs, the original error is thrown (not swallowed)

Use existing test patterns from the file. Import `atomicWriteValidated` and `z` from zod for validation tests.
  </action>
  <verify>
<verify>
  <automated>npm test -- --coverage --testPathPattern=atomic-writes</automated>
  <manual>Verify branch coverage for atomic-writes.ts is >= 80%</manual>
</verify>
  </verify>
  <done>
- atomicWriteValidated tests pass with valid and invalid data
- Error cleanup paths are tested
- Branch coverage for atomic-writes.ts reaches 80%+
  </done>
</task>

</tasks>

<verification>
- All new tests pass
- Branch coverage for atomic-writes.ts >= 80%
- No TypeScript errors
</verification>

<success_criteria>
- Error handling paths in atomic-writes.ts are fully tested
- Branch coverage meets 80% threshold
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-07-SUMMARY.md`
</output>
