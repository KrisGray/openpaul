---
phase: 01-core-infrastructure
plan: 09
type: execute
wave: 1
depends_on: []
files_modified: [src/tests/types/loop.test.ts, src/tests/loop-enforcer.test.ts]
autonomous: true
gap_closure: true
requirements: [INFR-05, NFR-04]

must_haves:
  truths:
    - "Loop type conditional branches are fully tested"
    - "Loop enforcer boundary conditions are covered"
    - "All conditional logic paths have test coverage"
  artifacts:
    - path: "src/tests/types/loop.test.ts"
      provides: "Test coverage for isValidTransition conditional"
      min_lines: 40
    - path: "src/tests/loop-enforcer.test.ts"
      provides: "Boundary condition tests for loop enforcer"
  key_links:
    - from: "src/tests/types/loop.test.ts"
      to: "src/types/loop.ts"
      via: "import and test isValidTransition"
    - from: "src/tests/loop-enforcer.test.ts"
      to: "src/state/loop-enforcer.ts"
      via: "existing tests + boundary additions"
---

<objective>
Add boundary condition tests for loop.ts isValidTransition and loop-enforcer.ts to reach 80% branch coverage

Purpose: Ensure all conditional branches and boundary conditions are tested
Output: New loop.test.ts and enhanced loop-enforcer.test.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-infrastructure/01-05-SUMMARY.md

# Gap context from VERIFICATION.md
- loop.ts branch coverage 50% (line 32 uncovered)
- Line 32: `allowed?.includes(to) ?? false` - the nullish coalescing branch
- loop-enforcer.ts branch coverage 66.66% (lines 29, 67-70 uncovered)
- Line 29: `allowed?.join` optional chaining path
- Lines 67-70: Error message building with undefined currentPhase

# Source file references
@src/types/loop.ts
@src/state/loop-enforcer.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create tests for loop.ts types and isValidTransition</name>
  <files>src/tests/types/loop.test.ts</files>
  <action>
Create a new test file at src/tests/types/loop.test.ts with the following tests:

1. **Test isValidTransition returns true for valid transitions** - Test all 3 valid transitions (PLAN→APPLY, APPLY→UNIFY, UNIFY→PLAN)
2. **Test isValidTransition returns false for invalid transitions** - Test all 6 invalid transitions (PLAN→UNIFY, PLAN→PLAN, APPLY→PLAN, APPLY→APPLY, UNIFY→APPLY, UNIFY→UNIFY)
3. **Test VALID_TRANSITIONS constant structure** - Verify VALID_TRANSITIONS has all 3 phases as keys
4. **Test VALID_TRANSITIONS PLAN allows only APPLY** - Verify VALID_TRANSITIONS.PLAN equals ['APPLY']
5. **Test VALID_TRANSITIONS APPLY allows only UNIFY** - Verify VALID_TRANSITIONS.APPLY equals ['UNIFY']
6. **Test VALID_TRANSITIONS UNIFY allows only PLAN** - Verify VALID_TRANSITIONS.UNIFY equals ['PLAN']
7. **Test LoopPhaseSchema validates valid phases** - Verify LoopPhaseSchema.parse works for 'PLAN', 'APPLY', 'UNIFY'
8. **Test LoopPhaseSchema rejects invalid phases** - Verify LoopPhaseSchema.parse throws for 'INVALID', 'plan' (lowercase), etc.

Import from '../types/loop' (adjust path as needed from tests/types/ directory).

Note: Line 32 (`allowed?.includes(to) ?? false`) has optional chaining and nullish coalescing. In the current implementation, VALID_TRANSITIONS is a complete Record<LoopPhase, LoopPhase[]>, so `allowed` will never be undefined for valid LoopPhase inputs. The ?? false branch is defensive code. Testing with type-safe inputs won't trigger this branch. This is acceptable - the branch exists for runtime safety and cannot be triggered through the public API with valid types.
  </action>
  <verify>
<verify>
  <automated>npm test -- --coverage --testPathPattern=loop.test</automated>
  <manual>Verify tests pass and cover main branches</manual>
</verify>
  </verify>
  <done>
- All isValidTransition tests pass
- VALID_TRANSITIONS tests pass
- LoopPhaseSchema tests pass
  </done>
</task>

<task type="auto">
  <name>task 2: Add boundary condition tests for loop enforcer</name>
  <files>src/tests/loop-enforcer.test.ts</files>
  <action>
Add the following test cases to the existing test file:

1. **Test enforceCanStartNewLoop includes currentPhase in error message** - When called with 'PLAN', verify error message contains 'PLAN' and 'Current state: PLAN' (covers lines 67-70)

2. **Test enforceCanStartNewLoop with undefined includes 'no state'** - When called with undefined, verify error message contains 'no state' and 'initial state' (covers lines 67-70)

3. **Test enforceCanStartNewLoop with undefined suggests initialization** - When called with undefined, verify error message suggests running /paul:plan to initialize (covers line 70)

Note: Line 29 (`allowed?.join`) uses optional chaining. In the current implementation, VALID_TRANSITIONS[from] will always return an array for valid LoopPhase values, so the optional chaining is defensive. The existing tests already cover the main error path. Adding the above tests will exercise the error message building code paths.

The current tests at lines 95-105 already test enforceCanStartNewLoop. Review them and ensure they verify the specific error message content to cover lines 67-70.
  </action>
  <verify>
<verify>
  <automated>npm test -- --coverage --testPathPattern=loop-enforcer</automated>
  <manual>Verify branch coverage for loop-enforcer.ts is >= 80%</manual>
</verify>
  </verify>
  <done>
- Boundary condition tests pass
- Branch coverage for loop-enforcer.ts reaches 80%+
  </done>
</task>

</tasks>

<verification>
- All new tests pass
- Branch coverage for loop.ts >= 80%
- Branch coverage for loop-enforcer.ts >= 80%
- No TypeScript errors
</verification>

<success_criteria>
- Loop type conditional branches are tested
- Loop enforcer boundary conditions are tested
- Coverage thresholds met for both files
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-09-SUMMARY.md`
</output>
