---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, jest.config.js, src/index.ts]
autonomous: true
requirements: [INFR-01, INFR-06, NFR-02, NFR-04, NFR-05]
must_haves:
  truths:
    - "Plugin loads in under 500ms with zero data loss"
    - "TypeScript compiles without errors"
    - "Jest test framework is configured and can run tests"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      must_contain: ["typescript", "zod", "@opencode-ai/plugin", "jest", "ts-jest"]
    - path: "tsconfig.json"
      provides: "TypeScript compiler configuration"
      must_contain: ["strict: true", "\"target\": \"ES2020\""]
    - path: "jest.config.js"
      provides: "Jest testing configuration"
      must_contain: ["preset: 'ts-jest'", "testEnvironment: 'node'"]
    - path: "src/index.ts"
      provides: "Plugin entry point"
      min_lines: 10
  key_links:
    - from: "package.json"
      to: "tsconfig.json"
      via: "tsc reads tsconfig.json"
      pattern: "\"typescript\".*\".*tsconfig"
---

<objective>
Initialize OpenCode plugin project structure with TypeScript configuration, dependencies, and Jest testing framework to support the PAUL framework.
</objective>

<context>
@./.planning/ROADMAP.md
@./.planning/phases/01-core-infrastructure/01-CONTEXT.md
@./.planning/phases/01-core-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize package.json with dependencies</name>
  <files>package.json</files>
  <action>
    Update package.json with the following structure:
    
    - Name: "@krisgray/openpaul"
    - Version: "1.0.0"
    - Description: "PAUL - Plan-Apply-Unify Loop plugin for OpenCode"
    - Main: "dist/index.js"
    - Types: "dist/index.d.ts"
    - Files: ["dist", "README.md"]
    - Keywords: ["opencode", "plugin", "paul", "plan-apply-unify", "ai", "development"]
    - Repository: Update to openpaul repo
    - Engines: node >=16.7.0
    - Dependencies:
      - @opencode-ai/plugin: ^1.2.0
      - zod: ^3.22.0
    - DevDependencies:
      - typescript: ^5.0.0
      - jest: ^29.0.0
      - @types/node: ^20.0.0
      - @types/jest: ^29.0.0
      - ts-jest: ^29.0.0
    - Scripts:
      - "build": "tsc"
      - "test": "jest"
      - "test:coverage": "jest --coverage"
      - "prepublishOnly": "npm run build"
    
    DO NOT modify existing bin, author, license, bugs, or homepage fields.
  </action>
  <verify>
    <automated>cat package.json | jq '.dependencies | keys' | grep -E "(opencode-ai|zod)"</automated>
    <automated>cat package.json | jq '.devDependencies | keys' | grep -E "(typescript|jest|@types)"</automated>
  </verify>
  <done>package.json contains all required dependencies and scripts</done>
</task>

<task type="auto">
  <name>Create TypeScript configuration</name>
  <files>tsconfig.json</files>
  <action>
    Create tsconfig.json with strict TypeScript configuration:
    
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "module": "commonjs",
        "lib": ["ES2020"],
        "outDir": "./dist",
        "rootDir": "./src",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true,
        "moduleResolution": "node",
        "resolveJsonModule": true,
        "types": ["node", "jest"]
      },
      "include": ["src/**/*"],
      "exclude": ["node_modules", "dist", "**/*.test.ts"]
    }
    ```
    
    This ensures type safety and compatibility with Node.js and OpenCode plugin API.
  </action>
  <verify>
    <automated>cat tsconfig.json | jq '.compilerOptions.strict'</automated>
    <automated>cat tsconfig.json | jq '.compilerOptions.target'</automated>
  </verify>
  <done>tsconfig.json created with strict mode enabled</done>
</task>

<task type="auto">
  <name>Create Jest configuration</name>
  <files>jest.config.js</files>
  <action>
    Create jest.config.js for TypeScript testing:
    
    ```javascript
    /** @type {import('ts-jest').JestConfigWithTsJest} */
    module.exports = {
      preset: 'ts-jest',
      testEnvironment: 'node',
      roots: ['<rootDir>/src'],
      testMatch: ['**/__tests__/**/*.ts', '**/?(*.)+(spec|test).ts'],
      collectCoverageFrom: [
        'src/**/*.ts',
        '!src/**/*.d.ts',
        '!src/**/*.test.ts',
        '!src/**/__tests__/**',
      ],
      coverageThreshold: {
        global: {
          branches: 80,
          functions: 80,
          lines: 80,
          statements: 80,
        },
      },
      coverageReporters: ['text', 'lcov', 'html'],
      verbose: true,
    };
    ```
    
    This configures Jest with ts-jest for TypeScript support and sets 80% coverage threshold as required.
  </action>
  <verify>
    <automated>cat jest.config.js | grep -E "(preset|coverageThreshold)"</automated>
  </verify>
  <done>Jest configuration created with 80% coverage target</done>
</task>

<task type="auto">
  <name>Create plugin entry point</name>
  <files>src/index.ts</files>
  <action>
    Create src/index.ts as the main plugin entry point:
    
    ```typescript
    import type { Plugin } from '@opencode-ai/plugin'
    
    /**
     * PAUL - Plan-Apply-Unify Loop Plugin for OpenCode
     * 
     * This plugin enforces the PLAN → APPLY → UNIFY loop with mandatory reconciliation,
     * ensuring every plan closes properly with full traceability and context preservation.
     */
    export const PaulPlugin: Plugin = async ({ project, client, directory, worktree }) => {
      // Plugin initialization
      await client.app.log({
        body: {
          service: 'paul-plugin',
          level: 'info',
          message: 'PAUL plugin initialized',
          extra: {
            project: project.name,
            directory,
          },
        },
      })
      
      return {
        // Event hooks will be added in subsequent plans
      }
    }
    
    // Export types for external use
    export * from './types'
    ```
    
    This creates the basic plugin structure that will be extended in later plans.
  </action>
  <verify>
    <automated>test -f src/index.ts && grep -q "PaulPlugin" src/index.ts</automated>
  </verify>
  <done>Plugin entry point created with initialization logging</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
- package.json has all required dependencies
- TypeScript compiles without errors: `npm run build`
- Jest can discover tests: `npm test` (should find no tests initially)
- Plugin loads successfully in OpenCode
</verification>

<success_criteria>
- Plugin loads in OpenCode without errors in under 500ms
- All core TypeScript types (State, Plan, Command) are defined and type-safe
- Jest test framework is configured with 80%+ coverage target
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md` documenting:
- Dependencies installed
- Configuration files created
- Plugin structure initialized
- Any decisions made during implementation
</output>
