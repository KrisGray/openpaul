---
phase: 01-core-infrastructure
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-13]
files_modified: [src/tests/state/loop-enforcer.test.ts]
autonomous: true
requirements: [INFR-05]
must_haves:
  truths:
    - "Loop enforcer prevents invalid PLAN → APPLY → UNIFY transitions"
    - "Test framework validates loop enforcer behavior"
  artifacts:
    - path: "src/tests/state/loop-enforcer.test.ts"
      provides: "Tests for loop enforcer"
      must_contain: ["describe", "test.*", "expect.*", "toBe"]
      min_lines: 40
  key_links:
    - from: "src/state/loop-enforcer.ts"
      to: "src/tests/state/loop-enforcer.test.ts"
      via: "import LoopEnforcer"
      pattern: "import.*LoopEnforcer"
---

<objective>
Implement a comprehensive test suite for the loop enforcer to ensure the PLAN → APPLY → UNIFY transition is enforced correctly with clear, user-friendly error messages.
</objective>

<context>
@./.planning/ROADMAP.md
@./.planning/phases/01-core-infrastructure/01-CONTEXT.md
@./.planning/phases/01-core-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create loop enforcer tests</name>
  <files>src/tests/state/loop-enforcer.test.ts</files>
  <action>
    Create src/tests/state/loop-enforcer.test.ts:
    ```typescript
    import { LoopEnforcer } from '../../state/loop-enforcer'
    import { isValidTransition } from '../../types/loop'
    
    describe('LoopEnforcer', () => {
      let enforcer: LoopEnforcer
      
      beforeEach(() => {
        enforcer = new LoopEnforcer()
      })
      
      describe('canTransition', () => {
        it('allows valid transitions', () => {
          expect(enforcer.canTransition('PLAN', 'APPLY')).toBe(true)
          expect(enforcer.canTransition('APPLY', 'UNIFY')).toBe(true)
          expect(enforcer.canTransition('UNIFY', 'PLAN')).toBe(true)
        })
        
        it('rejects invalid transitions', () => {
          expect(enforcer.canTransition('PLAN', 'UNIFY')).toBe(false)
          expect(enforcer.canTransition('APPLY', 'PLAN')).toBe(false)
          expect(enforcer.canTransition('UNIFY', 'APPLY')).toBe(false)
        })
      })
      
      describe('isValidTransition', () => {
        it('matches loop utility behavior', () => {
          expect(isValidTransition('PLAN', 'APPLY')).toBe(true)
          expect(isValidTransition('PLAN', 'UNIFY')).toBe(false)
        })
      })
      
      describe('enforceTransition', () => {
        it('does not throw for valid transitions', () => {
          expect(() => enforcer.enforceTransition('PLAN', 'APPLY')).not.toThrow()
          expect(() => enforcer.enforceTransition('APPLY', 'UNIFY')).not.toThrow()
          expect(() => enforcer.enforceTransition('UNIFY', 'PLAN')).not.toThrow()
        })
        
        it('throws with guidance for invalid transitions', () => {
          expect(() => enforcer.enforceTransition('PLAN', 'UNIFY')).toThrow(/Invalid transition/)
          try {
            enforcer.enforceTransition('PLAN', 'UNIFY')
          } catch (error) {
            const message = (error as Error).message
            expect(message).toContain('Invalid transition')
            expect(message).toContain('Current state')
            expect(message).toContain('Valid next states')
            expect(message).toContain('Run /paul:apply')
          }
        })
      })
    })
    ```
    
    This tests the loop enforcer behavior with strict validation and user-friendly error messages.
  </action>
  <verify>
    <automated>npm test -- src/tests/state/loop-enforcer.test.ts --pass</automated>
  </verify>
  <done>Loop enforcer tests created with strict validation and user-friendly errors</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
- TypeScript compiles: `npm run build`
- All tests pass: `npm test`
- Loop enforcer validates transitions correctly
- Error messages include guidance
</verification>

<success_criteria>
- Loop enforcer prevents invalid PLAN → APPLY → UNIFY transitions
- Error messages are user-friendly with clear guidance on next actions
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-05-SUMMARY.md` documenting:
- Loop enforcer implemented with strict validation
- Tests created and passing
- Error messages are user-friendly with guidance
- Any decisions made during implementation
</output>
