---
phase: 01-core-infrastructure
plan: 10
type: execute
wave: 6
depends_on: [01-02, 01-03, 01-07, 01-09, 01-13]
files_modified:
  - src/tests/types/loop.test.ts
  - src/tests/state/loop-enforcer.test.ts
  - src/tests/storage/atomic-writes-mock.test.ts
autonomous: true
requirements:
  - INFR-06
  - NFR-04

must_haves:
  truths:
    - "Branch coverage for loop.ts, loop-enforcer.ts, and atomic-writes.ts meets the 80% threshold"
    - "Defensive branches are exercised with targeted tests without changing production code"
  artifacts:
    - path: "src/tests/types/loop.test.ts"
      provides: "Defensive branch coverage for isValidTransition"
      min_lines: 110
    - path: "src/tests/state/loop-enforcer.test.ts"
      provides: "Defensive branch coverage for error-message construction"
      min_lines: 140
    - path: "src/tests/storage/atomic-writes-mock.test.ts"
      provides: "Mocked fs tests for atomic-writes cleanup branches"
      min_lines: 60
  key_links:
    - from: "src/tests/types/loop.test.ts"
      to: "src/types/loop.ts"
      via: "isValidTransition invalid-key path"
      pattern: "isValidTransition\(.*as LoopPhase"
    - from: "src/tests/state/loop-enforcer.test.ts"
      to: "src/state/loop-enforcer.ts"
      via: "enforceTransition invalid-key path"
      pattern: "enforceTransition\(.*as LoopPhase"
    - from: "src/tests/storage/atomic-writes-mock.test.ts"
      to: "src/storage/atomic-writes.ts"
      via: "jest mocked fs functions"
      pattern: "jest\.mock\(['\"]fs['\"]"
---

<objective>
Close the remaining branch-coverage gaps by adding targeted tests for defensive and cleanup branches.

Purpose: Satisfy the 80% coverage threshold without altering production behavior.
Output: New and updated tests that exercise currently uncovered defensive branches.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-VERIFICATION.md
@.planning/phases/01-core-infrastructure/01-07-SUMMARY.md
@.planning/phases/01-core-infrastructure/01-09-SUMMARY.md
@src/types/loop.ts
@src/state/loop-enforcer.ts
@src/storage/atomic-writes.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add defensive-branch tests for loop and enforcer</name>
  <files>src/tests/types/loop.test.ts, src/tests/state/loop-enforcer.test.ts</files>
  <action>
Add test cases that explicitly exercise the defensive branches without changing production code:
- In `src/tests/types/loop.test.ts`, add a test that calls `isValidTransition('INVALID' as LoopPhase, 'PLAN')` and asserts `false` to cover the `allowed?.includes(to) ?? false` fallback when `VALID_TRANSITIONS[from]` is undefined.
- In `src/tests/state/loop-enforcer.test.ts`, add a test that calls `enforcer.enforceTransition('INVALID' as LoopPhase, 'PLAN')`, asserts the thrown error includes `Valid next states: none`, and keep assertions focused on the defensive branch output.
- Add a test that calls `enforcer.enforceCanStartNewLoop(null as unknown as LoopPhase)` to force the error path and assert the message includes `initial state`, `no state`, and `Run /paul:plan to initialize PAUL` to cover the falsy-branch formatting.
- Keep tests isolated: no mutations to `VALID_TRANSITIONS` and no production code changes.
  </action>
  <verify>
    <automated>npm test -- --coverage --testPathPattern=loop --collectCoverageFrom='src/types/loop.ts' --collectCoverageFrom='src/state/loop-enforcer.ts'</automated>
  </verify>
  <done>
`loop.ts` and `loop-enforcer.ts` defensive branches are covered by tests and branch coverage meets the 80% threshold for these files.
  </done>
</task>

<task type="auto">
  <name>task 2: add mocked fs tests for atomic-writes cleanup branches</name>
  <files>src/tests/storage/atomic-writes-mock.test.ts</files>
  <action>
Create `src/tests/storage/atomic-writes-mock.test.ts` to exercise cleanup branches that are unreachable via real filesystem failures:
- Use `jest.resetModules()` and `jest.mock('fs', () => ...)` per test so the module under test imports mocked `existsSync`, `writeFileSync`, `renameSync`, `unlinkSync`, and `mkdirSync`.
- Test A: set `existsSync` to return true for the initial directory check, then false for the temp file check; make `writeFileSync` throw; assert `atomicWrite` throws and `unlinkSync` is not called (covers `existsSync(tempPath) === false`).
- Test B: set `existsSync` to return true for both checks; make `writeFileSync` throw; make `unlinkSync` throw; assert `atomicWrite` still throws the original error and that the inner cleanup catch branch executes (no uncaught error from `unlinkSync`).
- Use dynamic `await import('../../storage/atomic-writes')` after the mock setup so the module binds to mocked functions.
- Keep this file isolated so the existing `atomic-writes.test.ts` continues to use real filesystem behavior.
  </action>
  <verify>
    <automated>npm test -- --coverage --testPathPattern=atomic-writes --collectCoverageFrom='src/storage/atomic-writes.ts'</automated>
  </verify>
  <done>
`atomic-writes.ts` cleanup branches are covered with mocked fs tests and branch coverage meets the 80% threshold for this file.
  </done>
</task>

</tasks>

<verification>
- `npm test -- --coverage` reports global branch coverage >= 80%
</verification>

<success_criteria>
- All targeted defensive branches in loop and loop-enforcer code paths are exercised by tests
- Atomic write cleanup branches are covered through mocked fs scenarios
- Coverage threshold (branches >= 80%) is met without production code changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-10-SUMMARY.md`
</output>
