---
phase: 01-core-infrastructure
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: [src/tests/state/state-manager.test.ts, src/tests/types/sub-stage.test.ts]
autonomous: true
gap_closure: true
requirements: [INFR-04, NFR-04]

must_haves:
  truths:
    - "State manager handles edge cases in getCurrentPosition"
    - "Sub-stage helper function getParentPhase is tested"
    - "All conditional branches are covered"
  artifacts:
    - path: "src/tests/state/state-manager.test.ts"
      provides: "Edge case test coverage for state manager"
    - path: "src/tests/types/sub-stage.test.ts"
      provides: "Test coverage for getParentPhase helper"
      min_lines: 30
  key_links:
    - from: "src/tests/state/state-manager.test.ts"
      to: "src/state/state-manager.ts"
      via: "import and test getCurrentPosition edge cases"
    - from: "src/tests/types/sub-stage.test.ts"
      to: "src/types/sub-stage.ts"
      via: "import and test getParentPhase"
---

<objective>
Add edge case tests for state-manager.ts getCurrentPosition and create tests for sub-stage.ts getParentPhase helper

Purpose: Ensure all edge cases and helper functions are properly tested
Output: Edge case tests in state-manager.test.ts and new sub-stage.test.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-infrastructure/01-04-SUMMARY.md

# Gap context from VERIFICATION.md
- state-manager.ts branch coverage 25% (lines 66, 73, 80 uncovered)
- Line 66: Empty files array check
- Line 73: Invalid filename match
- Line 80: Failed to load state
- sub-stage.ts function coverage 0% (getParentPhase untested)

# Source file references
@src/state/state-manager.ts
@src/types/sub-stage.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Add edge case tests for state manager getCurrentPosition</name>
  <files>src/tests/state/state-manager.test.ts</files>
  <action>
Add the following test cases to the existing getCurrentPosition describe block:

1. **Test returns undefined when .paul directory exists but has no state files** - Create .paul dir with non-matching files (e.g., other.json), verify getCurrentPosition returns undefined (covers line 66)

2. **Test handles corrupted state file gracefully** - Create .paul dir with state-phase-1.json containing invalid JSON, verify getCurrentPosition returns undefined (covers line 80)

3. **Test handles file with invalid naming pattern** - Create .paul dir with files that partially match pattern but fail regex (this is harder to trigger, but can be tested by creating a file like state-phase-abc.json and ensuring it's ignored - note: current regex should reject this, so this tests the match failure path at line 73)

Note: The existing tests already cover some of these scenarios. Focus on adding tests that specifically trigger the uncovered branches:
- Empty files array after filtering (line 66)
- Regex match failure (line 73)  
- loadPhaseState returning null (line 80)
  </action>
  <verify>
<verify>
  <automated>npm test -- --coverage --testPathPattern=state-manager</automated>
  <manual>Verify branch coverage for state-manager.ts is >= 80%</manual>
</verify>
  </verify>
  <done>
- Edge case tests for getCurrentPosition pass
- Branch coverage for state-manager.ts reaches 80%+
  </done>
</task>

<task type="auto">
  <name>task 2: Create tests for sub-stage helper functions</name>
  <files>src/tests/types/sub-stage.test.ts</files>
  <action>
Create a new test file at src/tests/types/sub-stage.test.ts with the following tests:

1. **Test getParentPhase returns PLAN for plan-research** - Verify getParentPhase('plan-research') returns 'PLAN'
2. **Test getParentPhase returns PLAN for plan-blueprint** - Verify getParentPhase('plan-blueprint') returns 'PLAN'
3. **Test getParentPhase returns PLAN for plan-critique** - Verify getParentPhase('plan-critique') returns 'PLAN'
4. **Test getParentPhase returns APPLY for apply-slicing** - Verify getParentPhase('apply-slicing') returns 'APPLY'
5. **Test getParentPhase returns APPLY for apply-codegen** - Verify getParentPhase('apply-codegen') returns 'APPLY'
6. **Test getParentPhase returns APPLY for apply-debug** - Verify getParentPhase('apply-debug') returns 'APPLY'
7. **Test getParentPhase returns UNIFY for unify-diff-analysis** - Verify getParentPhase('unify-diff-analysis') returns 'UNIFY'
8. **Test getParentPhase returns UNIFY for unify-verification** - Verify getParentPhase('unify-verification') returns 'UNIFY'
9. **Test getParentPhase returns UNIFY for unify-documentation** - Verify getParentPhase('unify-documentation') returns 'UNIFY'

Also add tests for SUB_STAGES_BY_PHASE constant:
10. **Test SUB_STAGES_BY_PHASE contains all PLAN sub-stages** - Verify SUB_STAGES_BY_PHASE.PLAN contains all 3 PLAN sub-stages
11. **Test SUB_STAGES_BY_PHASE contains all APPLY sub-stages** - Verify SUB_STAGES_BY_PHASE.APPLY contains all 3 APPLY sub-stages
12. **Test SUB_STAGES_BY_PHASE contains all UNIFY sub-stages** - Verify SUB_STAGES_BY_PHASE.UNIFY contains all 3 UNIFY sub-stages

Import from '../types/sub-stage' (adjust path as needed from tests/types/ directory).
  </action>
  <verify>
<verify>
  <automated>npm test -- --coverage --testPathPattern=sub-stage</automated>
  <manual>Verify function coverage for sub-stage.ts is 100%</manual>
</verify>
  </verify>
  <done>
- All getParentPhase tests pass
- SUB_STAGES_BY_PHASE tests pass
- Function coverage for sub-stage.ts reaches 100%
  </done>
</task>

</tasks>

<verification>
- All new tests pass
- Branch coverage for state-manager.ts >= 80%
- Function coverage for sub-stage.ts = 100%
- No TypeScript errors
</verification>

<success_criteria>
- State manager edge cases are fully tested
- Sub-stage helper functions are fully tested
- Coverage thresholds met for both files
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-08-SUMMARY.md`
</output>
