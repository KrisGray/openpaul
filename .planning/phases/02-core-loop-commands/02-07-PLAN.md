---
phase: 02-core-loop-commands
plan: "07"
type: execute
wave: 6
depends_on:
  - "02"
files_modified:
  - src/types/plan.ts
  - src/commands/plan.ts
  - src/output/formatter.ts
  - src/output/error-formatter.ts
  - src/tests/commands/plan.test.ts
autonomous: true
requirements:
  - CORE-02
gap_closure: true
must_haves:
  truths:
    - "User can create executable plans that include criteria and boundaries"
    - "Plan output shows an execution graph derived from task dependencies"
    - "Plan creation errors include context, suggested fix, and next steps"
  artifacts:
    - path: "src/types/plan.ts"
      provides: "Plan schema includes criteria, boundaries, taskDependencies, executionGraph"
    - path: "src/commands/plan.ts"
      provides: "Plan creation uses criteria/boundaries, dependency detection, and guided errors"
    - path: "src/output/formatter.ts"
      provides: "Execution graph rendering for plan output"
  key_links:
    - from: "src/commands/plan.ts"
      to: "src/output/error-formatter.ts"
      via: "formatGuidedError"
      pattern: "formatGuidedError"
    - from: "src/commands/plan.ts"
      to: "src/output/formatter.ts"
      via: "plan output rendering"
      pattern: "formatPlan|formatExecutionGraph"
    - from: "src/commands/plan.ts"
      to: "src/types/plan.ts"
      via: "Plan schema validation"
      pattern: "PlanSchema"
---

<objective>
Close Phase 02 plan-creation gaps by supporting criteria/boundaries, dependency graphs, and guided errors.

Purpose: Ensure /paul:plan produces executable, fully-described plans with clear output and recoverable error handling.
Output: Updated plan schema, plan command logic, and output formatting with graph display.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-commands/02-CONTEXT.md
@.planning/phases/02-core-loop-commands/02-RESEARCH.md
@.planning/phases/02-core-loop-commands/02-VERIFICATION.md
@.planning/phases/02-core-loop-commands/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: extend plan schema for criteria, boundaries, and execution graph</name>
  <files>src/types/plan.ts</files>
  <action>
Add the missing plan fields required by verification and CONTEXT.md:
- Add `criteria` and `boundaries` fields to the Plan type and Zod schema.
- Add `taskDependencies` and `executionGraph` fields for dependency tracking and graph display.
- Ensure the schema allows plan creation without requiring non-empty `requirements`/`must_haves` if not supplied (either make them optional or default to empty arrays in schema).

Keep the Plan schema consistent with how /paul:plan constructs plans, so validation succeeds when fields are omitted by user input.
  </action>
  <verify>npm test -- src/tests/commands/plan.test.ts</verify>
  <done>PlanSchema validates plan objects that include criteria/boundaries/graph fields and does not reject empty requirements/must_haves when not provided.</done>
</task>

<task type="auto">
  <name>task 2: add criteria/boundaries args and dependency graph generation in /paul:plan</name>
  <files>src/commands/plan.ts</files>
  <action>
Update /paul:plan to meet Phase 02 requirements and verification gaps:
- Accept `criteria` and `boundaries` inputs (strings or string arrays) and persist them into the plan object.
- Compute taskDependencies by file overlap (task B depends on task A if A and B modify the same file).
- Generate an executionGraph (waves) from dependencies and include it in the stored plan and output.
- Populate `requirements` and `must_haves` with provided inputs or safe defaults consistent with the updated Plan schema.

Wire `formatGuidedError` for validation/state/fs errors. Implement rollback for write failures (remove partial plan files) and retry with backoff for transient filesystem errors, per CONTEXT.md.
  </action>
  <verify>npm test -- src/tests/commands/plan.test.ts</verify>
  <done>/paul:plan writes a plan including criteria/boundaries/dependencies/graph and returns output with a dependency graph section.</done>
</task>

<task type="auto">
  <name>task 3: update plan output formatting and tests for execution graph</name>
  <files>src/output/formatter.ts, src/tests/commands/plan.test.ts</files>
  <action>
Add or extend formatter utilities to render the execution graph in plan output (e.g., "[1,3] → [2]") and apply adaptive structure based on task count (simple 1-3, medium 4-10, complex 10+). Update plan tests to cover:
- criteria/boundaries persistence
- dependency detection and execution graph output
- guided error formatting behavior for invalid input

Ensure output still aligns with CONTEXT.md: default view is scannable, verbose view shows full task metadata.
  </action>
  <verify>npm test -- src/tests/commands/plan.test.ts</verify>
  <done>Plan tests pass and output includes an execution graph with adaptive structure based on plan size.</done>
</task>

</tasks>

<verification>
Run the plan command suite: `npm test -- src/tests/commands/plan.test.ts`.
</verification>

<success_criteria>
- /paul:plan accepts and stores criteria/boundaries, and includes dependency-based execution graph output.
- Plan schema validation aligns with actual plan creation (no false failures on empty optional fields).
- Guided error formatting is used for plan creation failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-commands/02-07-SUMMARY.md`.
</output>
