---
phase: 02-core-loop-commands
plan: 10
type: execute
wave: 1
depends_on: []
files_modified: [src/tests/commands/apply.test.ts, src/tests/commands/unify.test.ts]
autonomous: true
requirements: [CORE-03, CORE-04]
user_setup: []

must_haves:
  truths:
    - "Running /paul:apply shows task list with a progress bar, transitions state from PLAN → APPLY, and supports a dryRun mode that shows tasks without changing state."
    - "Running /paul:unify generates a reconciliation summary, saves it, transitions state from APPLY → UNIFY → PLAN for the next phase, and outputs next steps."
  artifacts:
    - path: "src/tests/commands/apply.test.ts"
      provides: "Valid TypeScript syntax for apply command tests"
      min_lines: 20
    - path: "src/tests/commands/unify.test.ts"
      provides: "Valid TypeScript syntax for unify command tests"
      min_lines: 20
  key_links:
    - from: "src/tests/commands/apply.test.ts"
      to: "src/commands/apply.ts"
      via: "test suite for apply command"
      pattern: "apply.*test"
    - from: "src/tests/commands/unify.test.ts"
      to: "src/commands/unify.ts"
      via: "test suite for unify command"
      pattern: "unify.*test"
---

<objective>
Fix TypeScript compilation errors in apply and unify test files to enable proper test execution and validation of core loop commands.

Purpose: Resolve syntax issues in test files that prevent automated testing from running, which blocks verification of core loop command functionality.
Output: Valid TypeScript files that compile correctly under ts-jest
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-commands/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Fix apply.test.ts TypeScript compilation errors</name>
  <files>src/tests/commands/apply.test.ts</files>
  <action>
    Fix syntax errors in apply.test.ts:
    1. Add missing commas in object literals
    2. Close braces properly
    3. Correct object literal syntax
    4. Ensure all test definitions are valid TypeScript
    5. Remove any stray text or markdown that may have been appended
    
    Reference existing working test patterns from the codebase for correct syntax.
  </action>
  <verify>npm test -- --testPathPattern="apply.test.ts" --passWithNoTests</verify>
  <done>Valid TypeScript syntax in apply.test.ts that compiles under ts-jest</done>
</task>

<task type="auto">
  <name>Fix unify.test.ts TypeScript compilation errors</name>
  <files>src/tests/commands/unify.test.ts</files>
  <action>
    Fix syntax errors in unify.test.ts:
    1. Replace 'letEach' with 'beforeEach' (typo fix)
    2. Correct jest.Mock usage
    3. Repair malformed expect blocks
    4. Remove appended markdown/text after tests
    5. Ensure all test definitions are valid TypeScript
    
    Reference existing working test patterns from the codebase for correct syntax.
  </action>
  <verify>npm test -- --testPathPattern="unify.test.ts" --passWithNoTests</verify>
  <done>Valid TypeScript syntax in unify.test.ts that compiles under ts-jest</done>
</task>

<task type="auto">
  <name>Verify both test files compile successfully</name>
  <files>src/tests/commands/apply.test.ts, src/tests/commands/unify.test.ts</files>
  <action>
    Run the full test suite for both files to ensure they now compile and execute properly.
    This should resolve the compilation errors that were preventing the automated tests from running.
  </action>
  <verify>npm test -- --testPathPattern="apply.test.ts|unify.test.ts" --passWithNoTests</verify>
  <done>Both test files compile without errors and can be executed by ts-jest</done>
</task>

</tasks>

<verification>
- Both test files compile successfully under ts-jest
- No syntax errors in either file
- Tests can execute without compilation failures
- Core loop commands functionality can be properly verified
</verification>

<success_criteria>
- TypeScript compilation errors in apply.test.ts are resolved
- TypeScript compilation errors in unify.test.ts are resolved
- All tests pass without compilation issues
- Core loop commands can be verified through automated testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-commands/02-10-SUMMARY.md`
</output>