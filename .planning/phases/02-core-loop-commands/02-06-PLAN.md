---
phase: 02-core-loop-commands
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/commands/apply.test.ts
  - src/tests/commands/unify.test.ts
autonomous: true
requirements:
  - CORE-03
  - CORE-04
gap_closure: true
must_haves:
  truths:
    - "Running /paul:apply tests compiles and executes under ts-jest"
    - "Running /paul:unify tests compiles and executes under ts-jest"
  artifacts:
    - path: "src/tests/commands/apply.test.ts"
      provides: "Valid Jest/TypeScript tests for apply command"
    - path: "src/tests/commands/unify.test.ts"
      provides: "Valid Jest/TypeScript tests for unify command"
  key_links:
    - from: "src/tests/commands/apply.test.ts"
      to: "jest.config.cjs"
      via: "ts-jest compilation"
      pattern: "describe\(.*apply"
    - from: "src/tests/commands/unify.test.ts"
      to: "jest.config.cjs"
      via: "ts-jest compilation"
      pattern: "describe\(.*unify"
---

<objective>
Repair apply/unify command tests so ts-jest compiles and the UAT suite can run.

Purpose: Close Phase 02 UAT failures caused by invalid test syntax.
Output: Fixed Jest/TypeScript test files for /paul:apply and /paul:unify.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-commands/02-UAT.md
@.planning/phases/02-core-loop-commands/02-03-SUMMARY.md
@.planning/phases/02-core-loop-commands/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: repair apply.test.ts TypeScript syntax</name>
  <files>src/tests/commands/apply.test.ts</files>
  <action>
Fix the syntactically invalid TypeScript in the apply command tests. Specifically:
- Add missing commas in object literals and function arguments.
- Close any unbalanced braces/parentheses/arrays.
- Ensure mocked objects are valid TS (no malformed literals) and keep test intent unchanged.
- Keep existing test coverage and assertions; only correct syntax so ts-jest can compile.

This directly addresses the UAT gap root cause for CORE-03.
  </action>
  <verify>npm test -- src/tests/commands/apply.test.ts</verify>
  <done>apply.test.ts compiles under ts-jest and its tests run without TypeScript parse errors.</done>
</task>

<task type="auto">
  <name>task 2: repair unify.test.ts invalid Jest/TypeScript code</name>
  <files>src/tests/commands/unify.test.ts</files>
  <action>
Fix the invalid Jest/TypeScript code in unify command tests. Specifically:
- Replace `letEach` with `beforeEach` and ensure proper setup ordering.
- Correct invalid jest.Mock usage (use jest.Mocked or jest.fn() with proper typing).
- Repair malformed expect blocks and close all braces/parens.
- Remove any appended non-test markdown/text after the test suite.
- Preserve test intent; only correct syntax/structure so ts-jest can compile.

This directly addresses the UAT gap root cause for CORE-04.
  </action>
  <verify>npm test -- src/tests/commands/unify.test.ts</verify>
  <done>unify.test.ts compiles under ts-jest and its tests run without TypeScript parse errors.</done>
</task>

</tasks>

<verification>
Run unit tests for both command suites: `npm test -- src/tests/commands/apply.test.ts` and `npm test -- src/tests/commands/unify.test.ts`.
</verification>

<success_criteria>
- TypeScript compilation errors in apply.test.ts and unify.test.ts are resolved.
- UAT tests 3 and 4 can execute (no ts-jest compilation failure).
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-commands/02-06-SUMMARY.md`.
</output>
