---
phase: 02-core-loop-commands
plan: "09"
type: execute
wave: 6
depends_on:
  - "05"
files_modified:
  - src/commands/progress.ts
  - src/commands/help.ts
  - src/tests/commands/progress.test.ts
  - src/tests/commands/help.test.ts
autonomous: true
requirements:
  - CORE-05
  - CORE-06
gap_closure: true
must_haves:
  truths:
    - "Progress output shows active task name, time-in-progress, and a progress bar during APPLY"
    - "Help output lists all Phase 2 commands including plan/apply/unify"
    - "Help supports detailed output for a specific command"
  artifacts:
    - path: "src/commands/progress.ts"
      provides: "Enhanced APPLY-stage status with task details and progress bar"
    - path: "src/commands/help.ts"
      provides: "Complete command reference including Phase 2"
    - path: "src/tests/commands/progress.test.ts"
      provides: "Tests for APPLY task status output"
  key_links:
    - from: "src/commands/progress.ts"
      to: "src/storage/file-manager.ts"
      via: "readPlan"
      pattern: "readPlan"
    - from: "src/commands/help.ts"
      to: "src/types/command.ts"
      via: "command metadata"
      pattern: "CommandType"
---

<objective>
Close remaining progress/help gaps so users can see APPLY task context and complete command references.

Purpose: Fulfill CORE-05 and CORE-06 with richer progress output and accurate help listings.
Output: Enhanced progress/help commands with updated tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-commands/02-CONTEXT.md
@.planning/phases/02-core-loop-commands/02-RESEARCH.md
@.planning/phases/02-core-loop-commands/02-VERIFICATION.md
@.planning/phases/02-core-loop-commands/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: show active task and progress bar in APPLY status</name>
  <files>src/commands/progress.ts</files>
  <action>
Enhance /paul:progress output when the loop is in APPLY:
- Load the current plan via FileManager using the current phase/plan ID.
- Compute active task name and time-in-progress (from state metadata or task start timestamps if available; otherwise display “unknown” with guidance).
- Render a visual progress bar using progressBar(currentIndex, total).
- Keep default output concise and gated by stage (only show task details for APPLY).

Follow CONTEXT.md guidance: compact loop summary + actionable next steps.
  </action>
  <verify>npm test -- src/tests/commands/progress.test.ts</verify>
  <done>Progress output includes active task name, time-in-progress, and progress bar when in APPLY.</done>
</task>

<task type="auto">
  <name>task 2: include Phase 2 commands in help output</name>
  <files>src/commands/help.ts</files>
  <action>
Ensure /paul:help lists all Phase 2 commands (init, plan, apply, unify, progress, help) in its grouped output. If Phase 2 has a dedicated “Core Loop Commands” section, add it; otherwise include these commands in the existing core grouping. Preserve specific-command help behavior.
  </action>
  <verify>npm test -- src/tests/commands/help.test.ts</verify>
  <done>Help output shows Phase 2 commands in the grouped list and detailed help for specific commands.</done>
</task>

<task type="auto">
  <name>task 3: update progress/help tests for new output</name>
  <files>src/tests/commands/progress.test.ts, src/tests/commands/help.test.ts</files>
  <action>
Update tests to validate:
- APPLY-stage progress output includes active task name, time-in-progress, and progress bar.
- Help output includes Phase 2 commands in the grouped list.

Keep existing tests for not-initialized and verbose output behavior intact.
  </action>
  <verify>npm test -- src/tests/commands/progress.test.ts && npm test -- src/tests/commands/help.test.ts</verify>
  <done>Progress/help tests pass with new output assertions.</done>
</task>

</tasks>

<verification>
Run command tests: `npm test -- src/tests/commands/progress.test.ts` and `npm test -- src/tests/commands/help.test.ts`.
</verification>

<success_criteria>
- /paul:progress shows active task details and progress bar during APPLY.
- /paul:help lists all Phase 2 commands and preserves per-command help.
- Tests pass for both commands.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-commands/02-09-SUMMARY.md`.
</output>
