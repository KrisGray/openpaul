---
phase: 03-session-management
plan: 07
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: [src/commands/pause.ts, src/utils/change-detector.ts, src/commands/__tests__/pause-changes.test.ts]
autonomous: true
gap_closure: true
requirements: [SESS-01]

must_haves:
  truths:
    - "Pause command detects uncommitted git changes before pausing"
    - "Pause command detects modified tracked files before pausing"
    - "Pause command prompts user with options when unsaved changes exist"
    - "User can choose to commit, save, discard, or abort pause"
  artifacts:
    - path: "src/utils/change-detector.ts"
      provides: "Git status and filesystem change detection"
      min_lines: 40
      exports: ["detectUncommittedChanges", "detectModifiedFiles"]
    - path: "src/commands/pause.ts"
      provides: "Enhanced pause command with change detection"
      contains: "detectUncommittedChanges"
  key_links:
    - from: "src/commands/pause.ts"
      to: "src/utils/change-detector.ts"
      via: "import and call before session save"
      pattern: "detectUncommittedChanges|detectModifiedFiles"
---

<objective>
Add pre-pause change detection to warn users about uncommitted git changes and modified tracked files before pausing a session.

Purpose: Prevent users from accidentally pausing sessions with unsaved work, which could lead to confusion during resume or team handoffs. Fulfills the locked CONTEXT.md decision: "On pause: Prompt user if unsaved changes exist (save, discard, or abort)".

Output: Enhanced pause command with git status checking, filesystem change detection, and user prompt with actionable options.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-management/03-02-SUMMARY.md
@src/commands/pause.ts
</context>

<gap_addressed>
**From VERIFICATION.md:**
- Truth: "Pause command prompts for unsaved changes before pausing"
- Status: partial
- Reason: "Pause command warns about existing sessions but does not check for uncommitted git changes or unsaved files"
- Missing:
  - Add git status check to detect uncommitted changes
  - Add filesystem change detection for tracked files
  - Prompt user to commit or save changes before pause
</gap_addressed>

<tasks>

<task type="auto">
  <name>Create change detection utility</name>
  <files>src/utils/change-detector.ts, src/utils/__tests__/change-detector.test.ts</files>
  <action>
Create a new utility module for detecting uncommitted changes and modified files.

Also create a test scaffold at src/utils/__tests__/change-detector.test.ts with a basic Jest describe block and a placeholder test (mark it as TODO) so the test file exists for the verify command.

Export two async functions:
1. `detectUncommittedChanges(directory: string): Promise<GitChangeStatus>`
   - Run `git status --porcelain` using child_process.exec
   - Parse output to extract: modified files, added files, deleted files, untracked files
   - Return structured object: `{ hasChanges: boolean, files: GitFileStatus[] }`
   - Handle non-git projects by catching error and returning `{ hasChanges: false, files: [] }`
   - Use try/catch to gracefully handle git not being installed or not a git repo

2. `detectModifiedFiles(directory: string, trackedFiles: string[]): Promise<ModifiedFileStatus>`
   - For each file in trackedFiles array (populated from session fileChecksums)
   - Check if file exists using fs.existsSync
   - If exists, compare current SHA256 checksum with stored checksum from session
   - Return structured object: `{ hasModifications: boolean, files: FileModification[] }`

Implementation notes:
- Use Node's child_process.exec for git commands
- Reuse existing SHA256 checksum pattern from pause.ts (createHash('sha256'))
- Export types: GitChangeStatus, GitFileStatus, ModifiedFileStatus
- No external dependencies (use built-in Node modules)
  </action>
  <verify>
<automated>npm test -- src/utils/__tests__/change-detector.test.ts</automated>
<manual>Run utility functions directly and verify they detect changes in a test git repo</manual>
<sampling_rate>After this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
- src/utils/change-detector.ts exists with both functions exported
- src/utils/__tests__/change-detector.test.ts scaffold exists
- Unit tests pass with >80% coverage
- Functions correctly detect git changes and file modifications
  </done>
</task>

<task type="auto">
  <name>Enhance pause command with change detection</name>
  <files>src/commands/pause.ts, src/commands/__tests__/pause-changes.test.ts</files>
  <action>
Modify pause.ts to check for unsaved changes before creating session.

Also create a test scaffold at src/commands/__tests__/pause-changes.test.ts with a basic Jest describe block and a placeholder test (mark it as TODO) so the test file exists for the verify command.

Changes to implement:
1. Import change detection utilities at top of file:
   - `import { detectUncommittedChanges, detectModifiedFiles } from '../utils/change-detector'`

2. Add change detection check after line 60 (after existing session warning):
   ```typescript
   // Check for uncommitted changes
   const gitStatus = await detectUncommittedChanges(context.directory)
   const trackedFiles = Object.keys(fileChecksums)
   const fileStatus = await detectModifiedFiles(context.directory, trackedFiles)
   
   if (gitStatus.hasChanges || fileStatus.hasModifications) {
     const changeSummary = formatChangeSummary(gitStatus, fileStatus)
     return formatHeader(2, '⚠️ Unsaved Changes Detected') + '\n\n' +
       changeSummary + '\n\n' +
       formatBold('Options:') + '\n' +
       formatList([
         'Commit your changes: `git add . && git commit -m "message"`',
         'Save specific files manually before pausing',
         'Run `/paul:pause` again to proceed anyway (changes will be noted in HANDOFF)',
         'Run `/paul:status` to see current session info',
       ])
   }
   ```

3. Create helper function `formatChangeSummary()`:
   - Format git changes: count modified/added/deleted/untracked
   - Format file modifications: list files with changed checksums
   - Keep output concise (max 10 files shown, with "... and N more")

4. If user proceeds despite warnings (runs pause again):
   - Add `warnings: string[]` to SessionState.metadata
   - Store change detection results in metadata.warnings array
   - This preserves the information for team handoffs

Implementation notes:
- Don't block pause entirely, just warn and inform
- Preserve existing session warning logic (< 24 hours)
- Maintain backward compatibility with existing pause behavior
- Follow existing error/warning format patterns from pause.ts
  </action>
  <verify>
<automated>npm test -- src/commands/__tests__/pause-changes.test.ts</automated>
<manual>Test pause command in repo with uncommitted changes and verify warning appears</manual>
<sampling_rate>After this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
- pause.ts imports and uses change detection utilities
- src/commands/__tests__/pause-changes.test.ts scaffold exists
- Pause command warns when uncommitted changes exist
- Pause command warns when tracked files have been modified
- User receives actionable guidance on how to proceed
- Existing pause behavior unchanged when no changes detected
  </done>
</task>

<task type="auto">
  <name>Add comprehensive tests for change detection</name>
  <files>src/commands/__tests__/pause-changes.test.ts, src/utils/__tests__/change-detector.test.ts</files>
  <action>
Create test suites for the new change detection functionality.

For src/utils/__tests__/change-detector.test.ts:
- Test detectUncommittedChanges():
  - Returns no changes in clean git repo
  - Detects modified files
  - Detects added files  
  - Detects deleted files
  - Detects untracked files
  - Handles non-git directory gracefully (returns hasChanges: false)
  - Handles git not installed gracefully

- Test detectModifiedFiles():
  - Returns no modifications when checksums match
  - Detects modified files with changed checksums
  - Handles missing files gracefully
  - Works with empty trackedFiles array

For src/commands/__tests__/pause-changes.test.ts:
- Test pause command with uncommitted changes:
  - Shows warning when git has uncommitted changes
  - Shows warning when tracked files modified
  - Allows pause on second invocation (user acknowledged warning)
  - Preserves change info in session metadata
  - No warning when everything is clean

- Use Jest mocking for child_process.exec to avoid actual git operations
- Use temp directories with real files for checksum testing
- Aim for 85%+ coverage on both files
  </action>
  <verify>
<automated>npm test -- --coverage --testPathPattern="change-detector|pause-changes"</automated>
<manual>Review coverage report to ensure >85% coverage on new files</manual>
<sampling_rate>After this task commits, before finishing plan</sampling_rate>
  </verify>
  <done>
- Test files created with comprehensive coverage
- All tests pass
- Coverage >85% on change-detector.ts
- Coverage >85% on enhanced pause command logic
- Edge cases tested (no git, git not installed, etc.)
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. Pause command detects uncommitted git changes
2. Pause command detects modified tracked files
3. User receives clear warning with actionable options
4. Pause still works when no changes detected
5. Pause can proceed after warning (second invocation)
6. Change information preserved in session metadata
7. All tests pass with >85% coverage
8. No regression in existing pause functionality
</verification>

<success_criteria>
- [ ] src/utils/change-detector.ts created with git and file change detection
- [ ] pause.ts enhanced to use change detection before session save
- [ ] User receives warning when unsaved changes exist
- [ ] Warning includes actionable options (commit, save, proceed, status)
- [ ] Change info stored in session metadata for handoffs
- [ ] Comprehensive test suite with >85% coverage
- [ ] Gap from VERIFICATION.md closed: "Pause command prompts for unsaved changes before pausing"
- [ ] CONTEXT.md locked decision fulfilled: "On pause: Prompt user if unsaved changes exist"
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-07-SUMMARY.md`
</output>
