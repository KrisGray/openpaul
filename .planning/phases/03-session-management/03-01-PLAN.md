---
phase: 03-session-management
plan: 01
type: execute
wave: 1
depends_on:
  - 03-00
depends_on: []
files_modified:
  - src/types/session.ts
  - src/storage/session-manager.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - User can save session state with current PAUL loop position and work context
    - User can load saved session state from .openpaul/SESSIONS/ directory
    - User can validate session integrity before resuming
    - Session files use atomic writes for zero data loss
    - Current session reference persists in .openpaul/CURRENT-SESSION
  artifacts:
    - path: src/types/session.ts
      provides: SessionState TypeScript type with loop position and work tracking
      min_lines: 30
      exports: ["SessionState", "SessionStateSchema"]
    - path: src/storage/session-manager.ts
      provides: SessionManager class for session file operations
      min_lines: 60
      contains: "class SessionManager"
  key_links:
    - from: src/types/session.ts
      to: src/storage/session-manager.ts
      via: "SessionStateSchema for runtime validation"
      pattern: "SessionStateSchema\\.parse"
    - from: src/storage/session-manager.ts
      to: .openpaul/SESSIONS/
      via: "session file storage with atomic writes"
      pattern: "atomicWrite.*sessionId"
    - from: src/storage/session-manager.ts
      to: .openpaul/CURRENT-SESSION
      via: "current session reference file"
      pattern: "writeFile.*CURRENT-SESSION"
---

<objective>
Implement SessionManager for session state tracking with file-based persistence, atomic writes, and runtime validation via Zod schemas.

Purpose: Enable session management commands (pause, resume, handoff) to reliably store and retrieve session state without data loss.

Output: SessionManager class with save/load operations, SessionState type with Zod validation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-management/03-CONTEXT.md
@.planning/phases/03-session-management/03-RESEARCH.md
@src/types/state.ts
@src/storage/file-manager.ts
@src/storage/atomic-writes.ts
</context>

<tasks>

<task type="auto">
  <name>Create SessionState type and Zod schema</name>
  <files>src/types/session.ts</files>
  <action>
Create src/types/session.ts with SessionState interface and SessionStateSchema:

Interface fields:
- sessionId: string (unique identifier)
- createdAt: number (timestamp)
- pausedAt: number (timestamp when paused)
- phase: LoopPhase enum ('PLAN' | 'APPLY' | 'UNIFY')
- phaseNumber: number (current phase)
- currentPlanId: string | undefined (optional active plan)
- workInProgress: string[] (list of work items in progress)
- nextSteps: string[] (next actions to take)
- metadata: Record<string, unknown> (additional session metadata)

Schema validation:
- All required fields validated
- sessionId must be non-empty string
- createdAt/pausedAt must be positive numbers
- phase must be valid LoopPhase value
- phaseNumber must be positive integer
- workInProgress/nextSteps must be arrays

Export SessionState and SessionStateSchema.
  </action>
  <verify>npm test -- --testPathPattern=types/session.test.ts</verify>
  <done>SessionState type and Zod schema defined and exported from src/types/session.ts</done>
</task>

<task type="auto">
  <name>Implement SessionManager class</name>
  <files>src/storage/session-manager.ts</files>
  <action>
Create src/storage/session-manager.ts with SessionManager class:

Class constructor:
- Accept projectRoot: string
- Initialize sessionsDir: join(projectRoot, '.openpaul', 'SESSIONS')
- Initialize currentSessionPath: join(projectRoot, '.openpaul', 'CURRENT-SESSION')

Methods:
- saveSession(state: SessionState): Promise<void>
  - Validate state with SessionStateSchema.parse()
  - Ensure .openpaul/SESSIONS/ directory exists (mkdirSync with recursive)
  - Write session file: sessionsDir/{sessionId}.json using atomicWrite()
  - Write current session reference: currentSessionPath containing sessionId
  - Use existing atomicWrite from ./atomic-writes.ts

- loadCurrentSession(): SessionState | null
  - Read sessionId from currentSessionPath (readFileSync)
  - Return null if file doesn't exist
  - Read session file: sessionsDir/{sessionId}.json (readFileSync)
  - Parse and validate with SessionStateSchema.parse()
  - Return null if validation fails

- sessionExists(sessionId: string): boolean
  - Check if session file exists: sessionsDir/{sessionId}.json

- deleteSession(sessionId: string): void
  - Delete session file: sessionsDir/{sessionId}.json

- getCurrentSessionId(): string | null
  - Read currentSessionPath, return sessionId or null

- generateSessionId(): string
  - Return timestamp-based ID: session-${Date.now()}

- validateSessionState(sessionId: string): { valid: boolean; errors: string[] }
  - Load session with loadCurrentSession()
  - Check if session file exists
  - Check if pausedAt is within 24 hours
  - Return validation result with errors list

Follow existing FileManager pattern from src/storage/file-manager.ts for consistency.
  </action>
  <verify>npm test -- --testPathPattern=storage/session-manager.test.ts</verify>
  <done>SessionManager class with save/load/delete operations and validation methods</done>
</task>

</tasks>

<verification>
Overall phase checks:
- SessionManager can save and load session files with Zod validation
- Session files stored in .openpaul/SESSIONS/ with atomic writes
- Current session reference stored in .openpaul/CURRENT-SESSION
- Session state includes all required fields: sessionId, timestamps, loop position, work context
- TypeScript compilation succeeds: npm run build
</verification>

<success_criteria>
SessionManager fully functional with:
- SessionState type and Zod schema defined
- saveSession() creates session file with atomic write and updates CURRENT-SESSION
- loadCurrentSession() loads and validates session from CURRENT-SESSION reference
- sessionExists() checks session file existence
- deleteSession() removes session file
- generateSessionId() creates unique session identifiers
- validateSessionState() checks session integrity and staleness
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-01-SUMMARY.md`
</output>
