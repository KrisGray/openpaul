---
phase: 03-session-management
plan: 08
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
  - 03-04
  - 03-05
  - 03-07
files_modified:
  - src/index.ts
  - src/utils/session-context.ts
  - src/utils/handoff-template.ts
  - src/commands/handoff.ts
  - src/commands/pause.ts
  - src/tests/commands/handoff.test.ts
  - src/tests/commands/pause.test.ts
  - src/commands/__tests__/pause-changes.test.ts
autonomous: true
gap_closure: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "User can run /openpaul:pause, /openpaul:status, and /openpaul:handoff commands"
    - "Pause and handoff include work done, work in progress, and next steps context"
    - "Pause generates HANDOFF.md using the shared template"
    - "Pause requires an explicit choice when unsaved changes are detected"
  artifacts:
    - path: "src/index.ts"
      provides: "OpenPAUL-prefixed command registrations"
      contains: "openpaul:pause"
    - path: "src/utils/session-context.ts"
      provides: "Session work context extraction from phase state"
      exports: ["buildSessionContext"]
    - path: "src/utils/handoff-template.ts"
      provides: "Shared HANDOFF.md template rendering"
      exports: ["renderHandoffTemplate"]
    - path: "src/commands/pause.ts"
      provides: "Pause command with template-based handoff and work context"
      contains: "renderHandoffTemplate"
    - path: "src/commands/handoff.ts"
      provides: "Handoff command with populated accomplished/in-progress lists"
      contains: "accomplished_list"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/pause.ts"
      via: "openpaul:pause tool registration"
      pattern: "openpaul:pause"
    - from: "src/commands/pause.ts"
      to: "src/templates/HANDOFF.md"
      via: "renderHandoffTemplate"
      pattern: "HANDOFF.md"
    - from: "src/commands/handoff.ts"
      to: "src/utils/session-context.ts"
      via: "buildSessionContext"
      pattern: "buildSessionContext"
---

<objective>
Close session-management gaps around OpenPAUL command registration, work context capture, and template-based handoffs for pause and handoff commands.

Purpose: Restore compliance with Phase 3 requirements by ensuring the OpenPAUL-prefixed commands exist and handoff content reflects real work state with a shared template.

Output: OpenPAUL aliases registered, shared handoff rendering helper, and pause/handoff context captured with explicit unsaved-change choice handling.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-management/03-VERIFICATION.md
@src/commands/pause.ts
@src/commands/handoff.ts
@src/index.ts
@src/types/state.ts
@src/templates/HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Register OpenPAUL command aliases</name>
  <files>src/index.ts</files>
  <action>
Add /openpaul:* aliases for pause, resume, status, and handoff by registering the same command handlers under openpaul-prefixed tool keys. Keep existing /paul:* registrations intact to avoid breaking current usage. This closes the verification gap about missing OpenPAUL commands without changing handler behavior.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/tests/commands/status.test.ts</automated>
  </verify>
  <done>/openpaul:pause, /openpaul:resume, /openpaul:status, and /openpaul:handoff are registered alongside existing /paul:* commands.</done>
</task>

<task type="auto">
  <name>task 2: Add shared session context + handoff template helpers</name>
  <files>src/utils/session-context.ts, src/utils/handoff-template.ts, src/commands/handoff.ts, src/tests/commands/handoff.test.ts</files>
  <action>
Create a session context helper that loads current phase state via StateManager.loadPhaseState(position.phaseNumber) and derives: currentPlanId (state.currentPlanId), accomplished items (state.completedPlans mapped to readable strings), workInProgress items (plans with status in-progress or currentPlanId fallback), and nextSteps (stateManager.getRequiredNextAction). Add a shared handoff template renderer that reads src/templates/HANDOFF.md, replaces {{variable}} placeholders, and accepts accomplished/in-progress lists. Update handoff.ts to use these helpers so accomplished_list and in_progress_list are populated from real phase state instead of empty arrays. Preserve existing behavior when no session exists by generating temporary session state and using context helper fallbacks.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/tests/commands/handoff.test.ts</automated>
  </verify>
  <done>Handoff output renders from the shared template and includes non-empty accomplished/in-progress lists when phase state provides data.</done>
</task>

<task type="auto">
  <name>task 3: Update pause to use shared template and explicit unsaved-change choice</name>
  <files>src/commands/pause.ts, src/tests/commands/pause.test.ts, src/commands/__tests__/pause-changes.test.ts</files>
  <action>
Replace inline HANDOFF.md generation with the shared template renderer and session context helper so pause captures currentPlanId, workInProgress, nextSteps, and accomplished list from phase state. Switch pause to the toolFactory + zod parameters pattern (same as apply/unify) and add an optional onUnsavedChanges enum: commit | save | discard | abort. When unsaved changes exist and onUnsavedChanges is undefined, return a prompt with explicit rerun instructions using the parameter. When set to abort, return an abort message without saving; when set to commit/save/discard, proceed with saving and include the selected action in session metadata for traceability. Update tests to cover the new prompt and override behavior.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/tests/commands/pause.test.ts src/commands/__tests__/pause-changes.test.ts</automated>
  </verify>
  <done>Pause uses the shared HANDOFF template, records work context from phase state, and enforces an explicit user choice when unsaved changes are detected.</done>
</task>

</tasks>

<verification>
npm test -- --runTestsByPath src/tests/commands/pause.test.ts src/commands/__tests__/pause-changes.test.ts src/tests/commands/handoff.test.ts
</verification>

<success_criteria>
- OpenPAUL command aliases are registered for session commands
- Pause and handoff include real work-done/in-progress context sourced from phase state
- Pause writes HANDOFF.md using the shared template renderer
- Unsaved-change handling requires explicit user selection before pausing
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-08-SUMMARY.md`
</output>
