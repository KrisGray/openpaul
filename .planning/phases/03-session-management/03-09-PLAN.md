---
phase: 03-session-management
plan: 09
type: execute
wave: 5
depends_on:
  - 03-02
  - 03-03
  - 03-08
files_modified:
  - src/commands/pause.ts
  - src/commands/resume.ts
  - src/utils/session-snapshots.ts
  - src/tests/commands/pause.test.ts
  - src/tests/commands/resume.test.ts
autonomous: true
gap_closure: true
requirements:
  - SESS-02

must_haves:
  truths:
    - "Resume reads HANDOFF.md and STATE.md and asks for confirmation before restoring"
    - "Resume shows real diffs for added, modified, and deleted files"
    - "Resume restores phase/plan/loop state into runtime after validation"
    - "Pause stores file snapshots needed for resume diffs"
  artifacts:
    - path: "src/utils/session-snapshots.ts"
      provides: "Snapshot read/write helpers for session diffing"
      exports: ["captureSessionSnapshots", "loadSnapshotContent"]
    - path: "src/commands/pause.ts"
      provides: "Snapshot capture during pause"
      contains: "captureSessionSnapshots"
    - path: "src/commands/resume.ts"
      provides: "Resume confirmation + real diff rendering"
      contains: "formatFileDiff"
  key_links:
    - from: "src/commands/pause.ts"
      to: "src/utils/session-snapshots.ts"
      via: "captureSessionSnapshots"
      pattern: "captureSessionSnapshots"
    - from: "src/commands/resume.ts"
      to: "src/utils/session-snapshots.ts"
      via: "loadSnapshotContent"
      pattern: "loadSnapshotContent"
    - from: "src/commands/resume.ts"
      to: "src/output/diff-formatter.ts"
      via: "formatFileDiff"
      pattern: "formatFileDiff"
---

<objective>
Restore session resume fidelity by reading handoff/state context, requiring explicit confirmation, and rendering real diffs based on snapshots captured at pause time.

Purpose: Close the verification gaps around resume context restoration and placeholder diffs, ensuring users see accurate changes and confirm before resuming.

Output: Snapshot-backed diffing in resume, confirmation gate, and pause snapshot capture utilities.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-management/03-VERIFICATION.md
@src/commands/pause.ts
@src/commands/resume.ts
@src/output/diff-formatter.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Capture file snapshots on pause</name>
  <files>src/utils/session-snapshots.ts, src/commands/pause.ts, src/tests/commands/pause.test.ts</files>
  <action>
Add snapshot helpers that write a copy of each tracked file (same set used for checksums) into .openpaul/SESSIONS/{sessionId}/snapshots, preserving relative paths. Ensure snapshot capture runs after unsaved-change confirmation and before session save, and store the snapshot root in session metadata for resume. Update checksum collection to skip snapshot directories so resume does not report snapshots as new changes. Update pause tests to assert snapshot metadata presence and to tolerate snapshot writes in mocked fs calls.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/tests/commands/pause.test.ts</automated>
  </verify>
  <done>Pause writes file snapshots to the session snapshot directory and excludes snapshot artifacts from change detection.</done>
</task>

<task type="auto">
  <name>task 2: Resume reads handoff/state, confirms restore, and renders real diffs</name>
  <files>src/commands/resume.ts, src/tests/commands/resume.test.ts, src/utils/session-snapshots.ts</files>
  <action>
 Switch resume to the toolFactory + zod parameters pattern (same as plan/apply) and add an optional confirm boolean parameter. When confirm is false/undefined, read .openpaul/HANDOFF.md and .paul/STATE.md (or .openpaul/STATE.md fallback), include a brief "Context Sources" section, and prompt the user to rerun with --confirm to proceed. When confirm is true, first validate resume preconditions: session exists, snapshot/metadata files are intact, and the STATE/phase markers reference a real phase + plan in ROADMAP/phase directory (fail with a clear error if inconsistent). After validation, restore the session context into runtime state (StateManager + phase markers) using the session's phase, currentPlanId, and loop position so subsequent commands pick up the correct execution state. Replace placeholder diffs by loading snapshot content for each saved file and using formatFileDiff(oldContent, newContent) to populate FileChange.diff for added/modified/deleted cases (use empty string for missing old/new content). Update tests to cover the confirmation gate, validation failures, restore state updates, and to expect real diff output when snapshot content exists.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/tests/commands/resume.test.ts</automated>
  </verify>
  <done>Resume requires confirmation, reads HANDOFF/STATE for context, and displays real diffs for added/modified/deleted files.</done>
</task>

</tasks>

<verification>
npm test -- --runTestsByPath src/tests/commands/pause.test.ts src/tests/commands/resume.test.ts
</verification>

<success_criteria>
- Snapshot files are created on pause and referenced for resume diffing
- Resume reads HANDOFF.md and STATE.md, prompting for confirmation before restore
- Resume validates session/phase/plan consistency and restores phase/plan/loop state on confirm
- Diff output includes real content changes for added, modified, and deleted files
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-09-SUMMARY.md`
</output>
