---
phase: 07-quality
plan: 05
type: gap-closure
trigger: UAT test failures from Phase 9 branding update
autonomous: true
---

<objective>
Fix 5 categories of test failures identified in UAT.md to restore all tests to passing state.

**Root Cause:** Phase 9 branding update (BRND-01: paul → openpaul) was incomplete - tests were not fully updated to match the new command prefix, some imports have typos from find-replace errors, and missing mocks/dependencies.

**Output:** All npm tests passing (93 tests), UAT gaps closed.
</objective>

<context>
@.planning/phases/07-quality/07-UAT.md
@.planning/debug/uat-test-failures.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
<name>Fix branding prefix mismatches in tests</name>
<files>
src/tests/commands/handoff.test.ts
src/tests/commands/pause.test.ts
</files>
<action>
Update test expectations from `/paul:` to `/openpaul:` prefix:
1. handoff.test.ts line 73: `/paul:init` → `/openpaul:init`
2. pause.test.ts line 124: `/paul:resume` → `/openpaul:resume`
3. pause.test.ts line 125: `/paul:status` → `/openpaul:status`
4. pause.test.ts line 171: `/paul:resume` → `/openpaul:resume`
5. pause.test.ts line 197: `/paul:init` → `/openpaul:init`
</action>
<verify>npm test -- --testPathPattern="handoff.test.ts|pause.test.ts" passes with no failures</verify>
<done>All branding prefix tests updated and passing</done>
</task>

<task type="auto" tdd="false">
<name>Add missing @opencode-ai/plugin mocks</name>
<files>
src/tests/commands/discuss.test.ts
src/tests/commands/assumptions.test.ts
src/tests/commands/research.test.ts
src/tests/commands/discover.test.ts
src/tests/commands/consider-issues.test.ts
</files>
<action>
Add `jest.mock('@opencode-ai/plugin', () => ({ ... }))` to each test file that imports from @opencode-ai/plugin. Follow the pattern used in other test files like plan-fix.test.ts or pause.test.ts.
</action>
<verify>npm test -- --testPathPattern="discuss.test.ts|assumptions.test.ts|research.test.ts|discover.test.ts|consider-issues.test.ts" passes with no failures</verify>
<done>All test files with plugin imports have proper mocks</done>
</task>

<task type="auto" tdd="false">
<name>Add missing 'diff' dependency</name>
<files>
package.json
</files>
<action>
Add the 'diff' package to dependencies. Run `npm install diff` or add to package.json. The diff-formatter.ts file imports from 'diff' package.
</action>
<verify>npm test -- --testPathPattern="resume.test.ts|diff-formatter.test.ts" passes with no failures</verify>
<done>'diff' dependency installed, related tests pass</done>
</task>

<task type="auto" tdd="false">
<name>Fix import name typos in test files</name>
<files>
src/tests/commands/plan-fix.test.ts
src/tests/commands/discuss-milestone.test.ts
src/tests/commands/research-phase.test.ts
</files>
<action>
Fix find-replace typos in imports:
1. plan-fix.test.ts line 9: `openpaulpaulPlanFix` → `openpaulPlanFix`
2. discuss-milestone.test.ts line 7: `openpaulpaulDiscussMilestone` → `openpaulDiscussMilestone`
3. research-phase.test.ts line 5: `openpaulpaulResearchPhase` → `openpaulResearchPhase`
</action>
<verify>npm test -- --testPathPattern="plan-fix.test.ts|discuss-milestone.test.ts|research-phase.test.ts" passes with no failures</verify>
<done>All import typos fixed, tests pass</done>
</task>

<task type="auto" tdd="false">
<name>Fix directory-scanner isCacheValid test</name>
<files>
src/tests/utils/directory-scanner.test.ts
src/utils/directory-scanner.ts
</files>
<action>
Investigate and fix the isCacheValid test failure at line 128. The test expects cache validity to work correctly but it's returning false unexpectedly. Review the cache validity logic and fix either the test expectation or the implementation.
</action>
<verify>npm test -- --testPathPattern="directory-scanner.test.ts" passes with no failures</verify>
<done>isCacheValid test passes</done>
</task>

<task type="auto" tdd="false">
<name>Run full test suite verification</name>
<files>N/A</files>
<action>
Run `npm test` to verify all 93 tests pass with no failures.
</action>
<verify>npm test exits with code 0, all tests pass</verify>
<done>All npm tests passing, UAT gaps closed</done>
</task>

</tasks>

<verification>
```bash
npm test
```
Should show all tests passing (93 tests, 0 failures).
</verification>

<success_criteria>
- [ ] All branding prefix mismatches fixed (5 tests)
- [ ] All missing plugin mocks added (5 test suites)
- [ ] 'diff' dependency installed (2 test suites)
- [ ] All import typos fixed (3 test suites)
- [ ] isCacheValid test fixed (1 test)
- [ ] Full npm test suite passes (93 tests, 0 failures)
- [ ] UAT.md gaps section cleared or marked resolved
</success_criteria>

<output>
After completion:
1. Create SUMMARY.md documenting all fixes
2. Update STATE.md with resolution
3. Update UAT.md to mark gaps as resolved
</output>
