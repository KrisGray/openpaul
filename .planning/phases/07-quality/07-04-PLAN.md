---
phase: 07-quality
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - src/tests/types/quality.test.ts
  - src/tests/storage/quality-manager.test.ts
  - src/tests/commands/verify.test.ts
  - src/tests/commands/plan-fix.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - Quality types have comprehensive test coverage
    - QualityManager has tests for all public methods
    - Verify command tests cover all workflow paths
    - Plan-fix command tests cover fix plan creation
    - All tests pass with Jest
  artifacts:
    - path: src/tests/types/quality.test.ts
      provides: Quality type and schema tests
      min_lines: 40
    - path: src/tests/storage/quality-manager.test.ts
      provides: QualityManager class tests
      min_lines: 100
    - path: src/tests/commands/verify.test.ts
      provides: Verify command tests
      min_lines: 80
    - path: src/tests/commands/plan-fix.test.ts
      provides: Plan-fix command tests
      min_lines: 60
  key_links:
    - from: src/tests/storage/quality-manager.test.ts
      to: src/storage/quality-manager.ts
      via: "QualityManager method testing"
      pattern: "qualityManager\\."
    - from: src/tests/commands/verify.test.ts
      to: src/commands/verify.ts
      via: "Verify command testing"
      pattern: "paulVerify"
---

<objective>
Add comprehensive Jest tests for quality types, manager, and commands.

Purpose: Ensure quality verification workflow is reliable with proper test coverage.

Output: Jest test suites for all quality-related code.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quality/07-CONTEXT.md
@.planning/phases/07-quality/07-RESEARCH.md
@src/types/quality.ts
@src/storage/quality-manager.ts
@src/commands/verify.ts
@src/commands/plan-fix.ts
@src/tests/commands/complete-milestone.test.ts
@src/tests/commands/consider-issues.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create quality types tests</name>
  <files>src/tests/types/quality.test.ts</files>
  <action>
Create src/tests/types/quality.test.ts:

Test cases:
1. UATResult schema validates pass/fail/skip
2. UATSeverity schema validates critical/major/minor
3. UATCategory schema validates functional/visual/performance/configuration
4. UATItem schema validates required fields
5. UATIssue schema validates all required fields
6. UAT schema validates structure with items array
7. UATIssues schema validates issues array
8. Schema rejects invalid enum values
9. Schema rejects missing required fields

Follow existing test patterns from src/tests/types/ tests.
  </action>
  <verify><automated>npm test -- --testPathPattern=types/quality.test.ts</automated></verify>
  <done>Quality type tests cover all schema validation rules</done>
</task>

<task type="auto">
  <name>Create QualityManager tests</name>
  <files>src/tests/storage/quality-manager.test.ts</files>
  <action>
Create src/tests/storage/quality-manager.test.ts:

Test setup:
- Create temp directory for tests
- Create QualityManager instance with temp directory
- Cleanup after each test

Test cases:
1. resolvePhaseDir returns path for existing phase
2. resolvePhaseDir returns null for non-existent phase
3. summaryExists returns true when SUMMARY.md exists
4. summaryExists returns false when no SUMMARY.md
5. parseSummaryMustHaves extracts truths from frontmatter
6. parseSummaryMustHaves returns null for invalid file
7. readUAT returns parsed UAT data
8. readUAT returns null for missing file
9. writeUAT creates file with correct format
10. readUATIssues returns parsed issues
11. writeUATIssues creates file with table format
12. getNextAlphaPlanId returns 'a' suffix for new plan
13. getNextAlphaPlanId returns next letter for existing suffixes
14. getNextAlphaPlanId handles multiple existing plans

Mock fs operations where needed. Follow existing test patterns.
  </action>
  <verify><automated>npm test -- --testPathPattern=storage/quality-manager.test.ts</automated></verify>
  <done>QualityManager tests cover all public methods</done>
</task>

<task type="auto">
  <name>Create verify command tests</name>
  <files>src/tests/commands/verify.test.ts</files>
  <action>
Create src/tests/commands/verify.test.ts:

Test setup:
- Mock QualityManager
- Mock file system operations

Test cases:
1. Shows error when SUMMARY.md not found
2. Shows checklist when no --item flag
3. Shows progress with partial completion
4. Marks item as passed with --result pass
5. Marks item as failed with --result fail
6. Marks item as skipped with --result skip
7. Prompts for notes on failure
8. Creates UAT.md on completion
9. Creates UAT-ISSUES.md for failures
10. Shows summary on completion
11. Handles missing phase directory

Follow patterns from src/tests/commands/complete-milestone.test.ts.
  </action>
  <verify><automated>npm test -- --testPathPattern=commands/verify.test.ts</automated></verify>
  <done>Verify command tests cover all workflow paths</done>
</task>

<task type="auto">
  <name>Create plan-fix command tests</name>
  <files>src/tests/commands/plan-fix.test.ts</files>
  <action>
Create src/tests/commands/plan-fix.test.ts:

Test setup:
- Mock QualityManager
- Mock FileManager

Test cases:
1. Shows error when UAT-ISSUES.md not found
2. Shows message when no open issues
3. Shows issue list when no --issue flag
4. Creates fix plan with alpha suffix
5. Fix plan inherits parent wave
6. Fix plan depends on parent plan
7. Prompts for execution with --execute flag
8. Shows location of created plan
9. Updates issue with fix plan ID

Follow patterns from src/tests/commands/complete-milestone.test.ts.
  </action>
  <verify><automated>npm test -- --testPathPattern=commands/plan-fix.test.ts</automated></verify>
  <done>Plan-fix command tests cover fix plan creation</done>
</task>

</tasks>

<verification>
- All test files created with descriptive test cases
- All tests pass: npm test
- Tests follow existing patterns from the codebase
- Mocks used for external dependencies
- Edge cases covered
</verification>

<success_criteria>
Quality verification tests passing with:
- Quality type tests covering all schemas (9+ tests)
- QualityManager tests covering all methods (14+ tests)
- Verify command tests covering all paths (11+ tests)
- Plan-fix command tests covering all paths (9+ tests)
- All tests pass: npm test
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality/07-04-SUMMARY.md`
</output>
