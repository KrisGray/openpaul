---
phase: 07-quality
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/directory-scanner.ts
  - src/tests/utils/directory-scanner.test.ts
autonomous: true
requirements: [QUAL-01]
gap_closure: true
must_haves:
  truths:
    - "Directory scanner cache validity logic matches expected staleness rules"
    - "Directory scanner cache validity tests pass"
  artifacts:
    - path: "src/utils/directory-scanner.ts"
      provides: "isCacheValid logic for cache staleness detection"
      min_lines: 20
    - path: "src/tests/utils/directory-scanner.test.ts"
      provides: "Cache validity test coverage"
      min_lines: 20
  key_links:
    - from: "src/tests/utils/directory-scanner.test.ts"
      to: "src/utils/directory-scanner.ts"
      via: "isCacheValid assertions"
      pattern: "isCacheValid"
---

<objective>
Fix directory scanner cache validity logic so its Jest test passes reliably.

Purpose: Resolve the remaining npm test failure tied to isCacheValid behavior.
Output: Updated cache validity implementation and aligned test expectations.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quality/07-UAT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Correct isCacheValid staleness checks</name>
  <files>src/utils/directory-scanner.ts</files>
  <action>
Update isCacheValid to return false when the cache file is missing or older than the latest source directory modification time. Ensure it handles invalid cache metadata defensively, returning false when required fields are missing.
  </action>
  <verify>
    <automated>npm test -- src/tests/utils/directory-scanner.test.ts</automated>
  </verify>
  <done>isCacheValid correctly reports stale or invalid cache entries as not valid.</done>
</task>

<task type="auto">
  <name>task 2: Align cache validity tests with corrected logic</name>
  <files>src/tests/utils/directory-scanner.test.ts</files>
  <action>
Update or extend the isCacheValid tests (including the failure around line 128) to assert the corrected staleness rules, ensuring the test reflects cache freshness and missing metadata cases.
  </action>
  <verify>
    <automated>npm test -- src/tests/utils/directory-scanner.test.ts</automated>
  </verify>
  <done>Directory scanner cache validity tests pass with the updated implementation.</done>
</task>

</tasks>

<verification>
Run npm test to confirm the directory-scanner suite passes and no cache validity failures remain.</verification>

<success_criteria>
- isCacheValid returns false for stale/missing cache data and true for fresh cache data.
- src/tests/utils/directory-scanner.test.ts passes consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality/07-07-SUMMARY.md`
</output>
