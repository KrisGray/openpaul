---
phase: 13-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish.yml
  - package.json
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
user_setup:
  - service: npmjs.org
    why: "OIDC trust configuration required for passwordless publishing"
    dashboard_config:
      - task: "Configure OIDC trust with GitHub"
        location: "https://www.npmjs.com/settings/{org}/settings/trusted-publishers"
        instructions: "Add trusted publisher: GitHub repo owner/repo name, workflow name: publish.yml"

must_haves:
  truths:
    - "GitHub release published event triggers publish workflow"
    - "Workflow waits for unit tests + E2E tests + codecov all passing before publishing"
    - "Version in package.json is auto-synced to match release tag"
    - "Build (npm run build) runs successfully before publish"
    - "Package publishes to npmjs.org with provenance via OIDC trusted publishing"
    - "Pre-release tags (beta, next) publish to correct npm dist-tag"
    - "Failure uploads dist/ artifact for debugging"
  artifacts:
    - path: ".github/workflows/publish.yml"
      provides: "npm publishing workflow with quality gates"
      min_lines: 80
      contains: "release:\n    types: [published]"
    - path: "package.json"
      provides: "Package metadata with correct scope"
      contains: '"name": "@krisgray/openpaul"'
  key_links:
    - from: ".github/workflows/publish.yml"
      to: "test.yml, e2e-tests.yml, codecov.yml"
      via: "gh run list workflow status checks"
      pattern: "gh run list.*workflow"
    - from: ".github/workflows/publish.yml"
      to: "package.json"
      via: "version sync from release tag"
      pattern: "jq '.version'"
    - from: ".github/workflows/publish.yml"
      to: "npmjs.org"
      via: "npm publish with OIDC trusted publishing"
      pattern: "npm publish.*--provenance"
---

<objective>
Create GitHub Actions workflow for automated npm publishing triggered by GitHub releases, with quality gates requiring all tests and codecov to pass.

Purpose: Enable secure, automated npm package publishing with supply chain transparency via provenance, gated behind comprehensive quality checks.
Output: publish.yml workflow file and updated package.json scope
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-publish/13-CONTEXT.md
@.github/workflows/test.yml
@.github/workflows/e2e-tests.yml
@.github/workflows/codecov.yml
</context>

<tasks>

<task type="auto">
  <name>task 1: Update package.json scope to @opencode-ai/openpaul</name>
  <files>package.json</files>
  <action>
Update the package name in package.json to `@krisgray/openpaul`.

Change line 2 from:
```json
"name": "@opencode-ai/openpaul",
```
to:
```json
"name": "@krisgray/openpaul",
```

This aligns with the repository owner's npm scope for npmjs.org registry publishing.
  </action>
  <verify>
    <automated>grep -q '"name": "@krisgray/openpaul"' package.json</automated>
  </verify>
  <done>package.json shows scoped package name @krisgray/openpaul</done>
</task>

<task type="auto">
  <name>task 2: Create publish.yml workflow with quality gates and provenance</name>
  <files>.github/workflows/publish.yml</files>
  <action>
Create GitHub Actions workflow file at `.github/workflows/publish.yml` with the following structure:

**Triggers (PUB-01):**
- `release.types: [published]` - triggers on GitHub release published event only

**Workflow-Level Permissions (for OIDC):**
```yaml
permissions:
  id-token: write
  contents: read
```

**Quality Gate Job (PUB-02):**
- Job name: `quality-gates`
- Wait for all three workflows to complete successfully for the release commit SHA:
  - `CI - Unit Tests` workflow
  - `E2E Tests` workflow  
  - `Codecov Coverage Upload` workflow
- Use same polling pattern as codecov.yml: gh run list with 30-second intervals, 30-minute max wait
- Fail-fast: exit 1 if any workflow shows `failure` or `cancelled` status
- Only proceed when ALL three show `success`

**Publish Job:**
- Depends on `quality-gates` job
- Runs on `ubuntu-latest`
- Permissions: `id-token: write`, `contents: read` (for OIDC)
- Node.js 20.x with:
  ```yaml
  uses: actions/setup-node@v4
  with:
    node-version: '20'
    cache: 'npm'
    registry-url: 'https://registry.npmjs.org'
  ```

**Version Sync Step:**
- Extract version from release tag (strip 'v' prefix)
- Validate semver format: must be X.Y.Z with optional prerelease (-beta.1, -next.1)
- If tag doesn't match semver pattern, fail with clear error
- Update package.json version field using jq: `jq --arg v "$VERSION" '.version = $v' package.json > package.json.tmp && mv package.json.tmp package.json`

**Dist-tag Detection Step:**
- Parse release tag for prerelease identifiers
- If tag contains `-beta.` → set DIST_TAG=beta
- If tag contains `-next.` or `-rc.` → set DIST_TAG=next  
- Otherwise → DIST_TAG=latest

**Build Step (PUB-04):**
- Run `npm ci`
- Run `npm run build`

**Publish Step (PUB-03):**
- Run: `npm publish --access public --provenance --tag $DIST_TAG`
- NO NPM_TOKEN needed - OIDC trusted publishing via registry-url in setup-node

**Failure Artifact Upload:**
- On failure, upload `dist/` directory as artifact named `publish-failure-dist`
- Retain for 7 days

**Concurrency:**
- Group: `publish-${{ github.ref }}`
- Cancel in-progress: true

Reference opencarl/.github/workflows/publish.yml for OIDC pattern and existing workflows (test.yml, e2e-tests.yml, codecov.yml) for consistency in:
- Checkout action with fetch-depth: 1
- Node.js setup with cache: 'npm'
- Draft PR filtering (though releases don't come from PRs, pattern consistency)
- Artifact upload patterns
  </action>
  <verify>
    <automated>node -e "const yaml = require('js-yaml'); const fs = require('fs'); const w = yaml.load(fs.readFileSync('.github/workflows/publish.yml', 'utf8')); console.assert(w.on.release.types.includes('published'), 'missing release trigger'); console.assert(w.permissions['id-token'] === 'write', 'missing OIDC id-token permission'); console.assert(w.jobs['quality-gates'], 'missing quality-gates job'); console.assert(w.jobs.publish.needs.includes('quality-gates'), 'publish should need quality-gates'); console.assert(w.jobs.publish.steps.some(s => s.run && s.run.includes('npm publish') && s.run.includes('--provenance')), 'missing provenance flag'); console.log('Workflow structure valid')</automated>
    <manual>Verify workflow triggers on release.published, has OIDC id-token: write permission, quality-gates job checking all 3 workflows, publish job with provenance flag and no NPM_TOKEN</manual>
  </verify>
  <done>publish.yml exists with release trigger, OIDC permissions (id-token: write), quality gates checking test.yml+e2e-tests.yml+codecov.yml, version sync, build step, and npm publish with provenance (no NPM_TOKEN)</done>
</task>

</tasks>

<verification>
1. Workflow file exists at `.github/workflows/publish.yml`
2. Workflow triggers on `release.types: [published]`
3. OIDC permissions: `id-token: write` at workflow and publish job level
4. Quality gates job waits for all three test workflows
5. Version extraction validates semver and syncs to package.json
6. Dist-tag detection for pre-release versions
7. Build step runs before publish
8. npm publish includes `--provenance` flag (no NPM_TOKEN)
9. Failure artifact upload for dist/ directory
10. package.json updated to `@opencode-ai/openpaul` scope
</verification>

<success_criteria>
- PUB-01: Workflow triggers on GitHub release published event ✓
- PUB-02: Quality gates verify unit tests + E2E tests + codecov all pass ✓
- PUB-03: npm publish includes --provenance flag with OIDC trusted publishing ✓
- PUB-04: Build step runs via `npm run build` before publish ✓
- Package scope updated to @opencode-ai/openpaul ✓
- Pre-release tags publish to correct dist-tag (beta/next) ✓
- Failure artifacts uploaded for debugging ✓
- OIDC configured (no NPM_TOKEN secret needed) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/13-publish/13-01-SUMMARY.md`
</output>
