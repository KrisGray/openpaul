---
phase: 06-pre-planning-research
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/research.ts
  - src/storage/research-manager.ts
autonomous: true
requirements:
  - RSCH-01
  - RSCH-02
user_setup: []

must_haves:
  truths:
    - "ResearchManager can coordinate research operations"
    - "Types exist for research results, agent status, and findings"
    - "Manager supports parallel agent result aggregation"
    - "Research results include confidence levels"
  artifacts:
    - path: "src/types/research.ts"
      provides: "Type definitions and Zod schemas for research operations"
      exports: ["ResearchResult", "AgentStatus", "ResearchFinding"]
    - path: "src/storage/research-manager.ts"
      provides: "Manager class for research coordination and result aggregation"
      exports: ["ResearchManager"]
  key_links:
    - from: "src/storage/research-manager.ts"
      to: "src/types/research.ts"
      via: "Type imports"
      pattern: "import.*from.*research"
    - from: "src/storage/research-manager.ts"
      to: "src/storage/atomic-writes.ts"
      via: "atomicWrite for RESEARCH.md persistence"
      pattern: "atomicWrite"
---

<objective>
Create the ResearchManager class and associated types for research coordination and parallel agent management.

Purpose: Establish the foundation for research commands (/openpaul:research, /openpaul:research-phase) with parallel agent spawning support and result aggregation.
Output: Types file and manager class following established patterns.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pre-planning-research/06-CONTEXT.md
@.planning/phases/06-pre-planning-research/06-RESEARCH.md

# Existing patterns to follow
@src/types/milestone.ts
@src/storage/milestone-manager.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create research types with Zod schemas</name>
  <files>src/types/research.ts</files>
  <action>
Create src/types/research.ts with type definitions and Zod schemas for research operations.

Follow the pattern from src/types/milestone.ts (type + schema pairs).

Define these types with matching Zod schemas:

1. **ConfidenceLevel** - Research confidence:
   - 'high' | 'medium' | 'low'

2. **AgentStatus** - Parallel agent tracking (per CONTEXT.md):
   - topic: string
   - status: 'spawning' | 'running' | 'complete' | 'failed'
   - summary: string | null
   - error: string | null
   - startedAt: string
   - completedAt: string | null

3. **ResearchFinding** - Individual research finding:
   - topic: string
   - summary: string
   - details: string[]
   - confidence: ConfidenceLevel
   - sources: string[]

4. **ResearchResult** - Complete research result (per RSCH-01):
   - phase: number
   - query: string
   - findings: ResearchFinding[]
   - confidence: ConfidenceLevel
   - verified: boolean
   - createdAt: string

5. **ResearchPhaseResult** - Aggregated research-phase result (per RSCH-02):
   - phase: number
   - agentsSpawned: number
   - agentsCompleted: number
   - agentsFailed: number
   - findings: ResearchFinding[]
   - themes: string[] (findings organized by theme)
   - createdAt: string

6. **AgentDashboard** - For status display:
   - agents: AgentStatus[]
   - completed: number
   - total: number
   - startTime: string

Export all types and schemas.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/types/research.ts 2>&1 | head -20</automated>
    <manual>Verify types compile without errors</manual>
  </verify>
  <done>TypeScript types and Zod schemas for all research types compile without errors</done>
</task>

<task type="auto">
  <name>task 2: Create ResearchManager class</name>
  <files>src/storage/research-manager.ts</files>
  <action>
Create src/storage/research-manager.ts following the pattern from src/storage/milestone-manager.ts.

The ResearchManager class should:

1. **Constructor**: Accept projectRoot: string

2. **Path resolution methods** (dual path support):
   - resolvePhaseDir(phaseNumber: number): string | null
   - resolveResearchPath(phaseNumber: number): string | null - Find existing RESEARCH.md
   - getPlanningDir(): string - Get .paul/ or .openpaul/ directory

3. **Research result methods**:
   - createResearchResult(phaseNumber: number, query: string, findings: ResearchFinding[]): ResearchResult
   - aggregateAgentResults(agentResults: AgentStatus[]): ResearchPhaseResult

4. **Template generators** (private):
   - generateResearchContent(result: ResearchResult): string - Single RESEARCH.md for aggregated results
   - formatAgentDashboard(dashboard: AgentDashboard): string - Status dashboard for parallel agents

5. **Parallel agent coordination helpers**:
   - formatAgentStatus(agent: AgentStatus): string - Format single agent status line
   - organizeByTheme(findings: ResearchFinding[]): Map&lt;string, ResearchFinding[]&gt;

Per CONTEXT.md decisions:
- Max 4 parallel agents
- Failed agents noted in report, don't fail entire operation
- Single synthesized RESEARCH.md organized by theme
- Agent status dashboard showing topic, status, summary

Use atomicWrite from '../storage/atomic-writes' for file writes.
Import types from '../types/research'.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/storage/research-manager.ts 2>&1 | head -20</automated>
    <manual>Verify manager compiles with all coordination methods</manual>
  </verify>
  <done>ResearchManager class compiles with all coordination and aggregation methods</done>
</task>

</tasks>

<verification>
- [ ] Types file compiles without errors
- [ ] Manager file compiles without errors
- [ ] All Zod schemas match TypeScript interfaces
- [ ] Agent dashboard formatting matches CONTEXT.md requirements
- [ ] Result aggregation produces single synthesized output
</verification>

<success_criteria>
- TypeScript compiles both files without errors
- Types export all research interfaces and schemas
- ResearchManager exports class with coordination methods
- Agent status tracking supports 4 max parallel agents
</success_criteria>

<output>
After completion, create `.planning/phases/06-pre-planning-research/06-02-SUMMARY.md`
</output>
