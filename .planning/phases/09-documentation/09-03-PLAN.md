---
phase: 09-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/ROADMAP.md
  - src/templates/STATE.md
  - src/templates/PLAN.md
  - src/templates/SUMMARY.md
  - src/templates/HANDOFF.md
  - src/templates/CONTEXT.md
  - src/templates/DISCOVERY.md
  - src/templates/RESEARCH.md
  - src/templates/ISSUES.md
  - src/templates/PROJECT.md
  - src/templates/DEBUG.md
  - src/templates/UAT-ISSUES.md
  - src/templates/MILESTONES.md
  - src/templates/config.md
  - src/templates/SPECIAL-FLOWS.md
  - src/templates/milestone-context.md
  - src/templates/milestone-archive.md
  - src/templates/codebase/structure.md
  - src/templates/codebase/stack.md
  - src/templates/codebase/conventions.md
  - src/templates/codebase/concerns.md
  - src/templates/codebase/integrations.md
  - src/templates/codebase/architecture.md
  - src/templates/codebase/testing.md
  - README.md
  - package.json
autonomous: true
requirements: [BRND-01]
must_haves:
  truths:
    - "All template files use .openpaul/ directory references"
    - "All template files use /openpaul: command references"
    - "README.md uses OpenPAUL branding throughout"
    - "package.json description uses OpenPAUL branding"
  artifacts:
    - path: "README.md"
      provides: "Primary project documentation"
      contains: "OpenPAUL"
    - path: "src/templates/STATE.md"
      provides: "State template"
      contains: ".openpaul"
  key_links:
    - from: "templates"
      to: "generated files"
      via: "content copying"
      pattern: "OpenPAUL|openpaul"
---

<objective>
Update all template markdown files and project documentation to use OpenPAUL branding.

Purpose: Ensure generated files and documentation have consistent branding
Output: Rebranded templates and documentation
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@.planning/phases/09-documentation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update template markdown files</name>
  <files>src/templates/*.md, src/templates/codebase/*.md (24 files)</files>
  <action>
Update all template files to use OpenPAUL branding:

1. Replace all `/paul:` command references with `/openpaul:`:
   - `/paul:init` → `/openpaul:init`
   - `/paul:plan` → `/openpaul:plan`
   - `/paul:apply` → `/openpaul:apply`
   - `/paul:unify` → `/openpaul:unify`
   - `/paul:progress` → `/openpaul:progress`
   - `/paul:status` → `/openpaul:status`
   - `/paul:pause` → `/openpaul:pause`
   - `/paul:resume` → `/openpaul:resume`
   - etc.

2. Replace all `.paul/` directory references with `.openpaul/`:
   - `.paul/STATE.md` → `.openpaul/STATE.md`
   - `.paul/ROADMAP.md` → `.openpaul/ROADMAP.md`
   - `.paul/phases/` → `.openpaul/phases/`
   - etc.

3. Replace "PAUL" with "OpenPAUL" in titles and prose:
   - "# PAUL" → "# OpenPAUL"
   - "PAUL loop" → "OpenPAUL loop"
   - etc.

4. Update any file paths or command examples.

Files to update:
- src/templates/ROADMAP.md
- src/templates/STATE.md
- src/templates/PLAN.md
- src/templates/SUMMARY.md
- src/templates/HANDOFF.md
- src/templates/CONTEXT.md
- src/templates/DISCOVERY.md
- src/templates/RESEARCH.md
- src/templates/ISSUES.md
- src/templates/PROJECT.md
- src/templates/DEBUG.md
- src/templates/UAT-ISSUES.md
- src/templates/MILESTONES.md
- src/templates/config.md
- src/templates/SPECIAL-FLOWS.md
- src/templates/milestone-context.md
- src/templates/milestone-archive.md
- src/templates/codebase/structure.md
- src/templates/codebase/stack.md
- src/templates/codebase/conventions.md
- src/templates/codebase/concerns.md
- src/templates/codebase/integrations.md
- src/templates/codebase/architecture.md
- src/templates/codebase/testing.md
  </action>
  <verify>
    <automated>grep -r "/paul:" src/templates/ --include="*.md" | wc -l | xargs -I {} sh -c 'test {} -eq 0 && echo PASS || echo FAIL: {} /paul: references remain'</automated>
  </verify>
  <done>All template files use /openpaul: commands and .openpaul/ directories</done>
</task>

<task type="auto">
  <name>task 2: Rewrite README.md with OpenPAUL branding</name>
  <files>README.md</files>
  <action>
Completely update README.md with OpenPAUL branding:

1. Update title and description:
   - Change "PAUL" to "OpenPAUL" throughout
   - Update any taglines or descriptions

2. Update all command examples:
   - Change `/paul:` to `/openpaul:` in all code blocks
   - Update command documentation

3. Update all file path references:
   - Change `.paul/` to `.openpaul/`
   - Update directory structure examples

4. Add migration section for existing users:
   ```markdown
   ## Migration from PAUL
   
   If you have an existing project using `.paul/` directory:
   
   1. Rename `.paul/` to `.openpaul/`
   2. Update any hardcoded paths in your workflow
   
   OpenPAUL will automatically detect and use `.openpaul/` for new files
   while still reading from `.paul/` for backward compatibility.
   ```

5. Update installation and usage instructions.

6. Update any badges, links, or references to the npm package.
  </action>
  <verify>
    <automated>grep -c "OpenPAUL" README.md | xargs -I {} sh -c 'test {} -gt 5 && echo PASS || echo FAIL: insufficient OpenPAUL references'</automated>
  </verify>
  <done>README.md uses OpenPAUL branding throughout with migration instructions</done>
</task>

<task type="auto">
  <name>task 3: Update package.json description</name>
  <files>package.json</files>
  <action>
Update package.json with OpenPAUL branding:

1. Update the "description" field to use "OpenPAUL" instead of "PAUL"

2. Update any other metadata fields that reference "PAUL"

3. Verify the package name is correct (@krisgray/openpaul or similar)

Do NOT change version number or dependencies.
  </action>
  <verify>
    <automated>grep '"description"' package.json | grep -c "OpenPAUL" | xargs -I {} sh -c 'test {} -eq 1 && echo PASS || echo FAIL'</automated>
  </verify>
  <done>package.json description uses OpenPAUL branding</done>
</task>

</tasks>

<verification>
- Run `npm run build` to verify no issues
- Verify no /paul: references in templates
- Verify no .paul/ references in templates (except migration docs)
- Verify README.md reads correctly with new branding
</verification>

<success_criteria>
- All 24 template files use OpenPAUL branding
- README.md completely rebranded with migration section
- package.json description updated
- No stray PAUL or paul references in documentation
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-03-SUMMARY.md`
</output>
