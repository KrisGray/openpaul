---
phase: 09-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/init.ts
  - src/commands/plan.ts
  - src/commands/apply.ts
  - src/commands/unify.ts
  - src/commands/progress.ts
  - src/commands/status.ts
  - src/commands/help.ts
  - src/commands/pause.ts
  - src/commands/resume.ts
  - src/commands/handoff.ts
  - src/commands/add-phase.ts
  - src/commands/remove-phase.ts
  - src/commands/milestone.ts
  - src/commands/complete-milestone.ts
  - src/commands/discuss-milestone.ts
  - src/commands/discuss.ts
  - src/commands/assumptions.ts
  - src/commands/discover.ts
  - src/commands/consider-issues.ts
  - src/commands/research.ts
  - src/commands/research-phase.ts
  - src/commands/verify.ts
  - src/commands/plan-fix.ts
  - src/commands/config.ts
  - src/commands/flows.ts
  - src/commands/map-codebase.ts
  - src/commands/index.ts
  - src/index.ts
autonomous: true
requirements: [BRND-01]
must_haves:
  truths:
    - "All command exports use openpaulX naming convention"
    - "src/index.ts imports openpaulX functions"
    - "Plugin is named OpenPaulPlugin"
    - "Only openpaul: command prefix registered (no paul: aliases)"
  artifacts:
    - path: "src/commands/index.ts"
      provides: "Central export of all command functions"
      contains: "export { openpaulInit"
    - path: "src/index.ts"
      provides: "Plugin definition and command registration"
      contains: "OpenPaulPlugin"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/index.ts"
      via: "import statements"
      pattern: "from './commands"
---

<objective>
Rename all command function exports from paulX to openpaulX, update the main plugin to OpenPaulPlugin, and remove all paul: command aliases.

Purpose: Establish consistent OpenPAUL branding in all code identifiers
Output: Renamed functions, updated exports, clean plugin registration
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@.planning/phases/09-documentation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename command function exports</name>
  <files>src/commands/*.ts (all 26 command files)</files>
  <action>
For each command file in src/commands/ (except index.ts):

1. Rename the exported function constant from `paulX` to `openpaulX`:
   - `paulInit` → `openpaulInit`
   - `paulPlan` → `openpaulPlan`
   - `paulApply` → `openpaulApply`
   - `paulUnify` → `openpaulUnify`
   - `paulProgress` → `openpaulProgress`
   - `paulStatus` → `openpaulStatus`
   - `paulHelp` → `openpaulHelp`
   - `paulPause` → `openpaulPause`
   - `paulResume` → `openpaulResume`
   - `paulHandoff` → `openpaulHandoff`
   - `paulAddPhase` → `openpaulAddPhase`
   - `paulRemovePhase` → `openpaulRemovePhase`
   - `paulMilestone` → `openpaulMilestone`
   - `paulCompleteMilestone` → `openpaulCompleteMilestone`
   - `paulDiscussMilestone` → `openpaulDiscussMilestone`
   - `paulDiscuss` → `openpaulDiscuss`
   - `paulAssumptions` → `openpaulAssumptions`
   - `paulDiscover` → `openpaulDiscover`
   - `paulConsiderIssues` → `openpaulConsiderIssues`
   - `paulResearch` → `openpaulResearch`
   - `paulResearchPhase` → `openpaulResearchPhase`
   - `paulVerify` → `openpaulVerify`
   - `paulPlanFix` → `openpaulPlanFix`
   - `paulConfig` → `openpaulConfig`
   - `paulFlows` → `openpaulFlows`
   - `paulMapCodebase` → `openpaulMapCodebase`

2. Update the tool `name` property in each command definition from `paul:x` to `openpaul:x`

3. Update any user-facing strings that reference `/paul:` to `/openpaul:`

4. Update any references to `.paul/` directory to `.openpaul/` in user-facing messages (keep fallback logic if code checks both paths)

Do NOT change source file names. Do NOT change test file references yet.
  </action>
  <verify>
    <automated>grep -r "export const paul" src/commands/ --include="*.ts" | grep -v index.ts | wc -l | xargs -I {} sh -c 'test {} -eq 0 && echo PASS || echo FAIL: {} paul exports remain'</automated>
  </verify>
  <done>All 26 command files export openpaulX functions, no paulX exports remain</done>
</task>

<task type="auto">
  <name>task 2: Update commands index exports</name>
  <files>src/commands/index.ts</files>
  <action>
Update src/commands/index.ts to export all functions with openpaulX naming:

Replace all export statements:
- `export { paulInit }` → `export { openpaulInit }`
- `export { paulPlan }` → `export { openpaulPlan }`
- (and all other commands following the same pattern)

Add exports for any commands not currently exported (handoff, milestone, config, flows, map-codebase).
  </action>
  <verify>
    <automated>grep "export { openpaulInit" src/commands/index.ts && grep "export { openpaulPlan" src/commands/index.ts && echo PASS || echo FAIL</automated>
  </verify>
  <done>All commands exported with openpaulX names, no paulX exports remain</done>
</task>

<task type="auto">
  <name>task 3: Update main plugin and remove aliases</name>
  <files>src/index.ts</files>
  <action>
Update src/index.ts:

1. Rename the plugin export:
   - `export const PaulPlugin` → `export const OpenPaulPlugin`

2. Update all imports to use openpaulX names:
   - `import { paulInit }` → `import { openpaulInit }`
   - (all 26 imports)

3. Update the service name in log message:
   - `service: 'paul-plugin'` → `service: 'openpaul-plugin'`
   - `message: 'PAUL plugin initialized'` → `message: 'OpenPAUL plugin initialized'`

4. Update the file header comment:
   - `PAUL - Plan-Apply-Unify Loop Plugin` → `OpenPAUL - Plan-Apply-Unify Loop Plugin`

5. Remove ALL `paul:` command aliases from the tool registration object. Keep ONLY `openpaul:` prefixed commands:
   - Remove lines like `'paul:init': paulInit`
   - Keep only `'openpaul:init': openpaulInit`
   - This is a CLEAN BREAK - no paul: aliases allowed per user decision

The final tool object should have ~27 entries, all with `openpaul:` prefix only.
  </action>
  <verify>
    <automated>grep -c "'paul:" src/index.ts | xargs -I {} sh -c 'test {} -eq 0 && echo PASS || echo FAIL: {} paul: aliases remain'</automated>
  </verify>
  <done>Plugin renamed to OpenPaulPlugin, all imports use openpaulX, no paul: command aliases remain</done>
</task>

</tasks>

<verification>
- Run `npm run build` to verify TypeScript compiles without errors
- Verify no paulX function references remain in src/commands/*.ts
- Verify no paul: command registrations remain in src/index.ts
</verification>

<success_criteria>
- All 26 command files export openpaulX functions
- src/commands/index.ts exports all openpaulX functions
- src/index.ts uses OpenPaulPlugin name and openpaul: commands only
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-01-SUMMARY.md`
</output>
