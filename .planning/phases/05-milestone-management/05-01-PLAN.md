---
phase: 05-milestone-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/milestone-manager.ts
  - src/types/milestone.ts
  - src/tests/storage/milestone-manager.test.ts
autonomous: true
requirements:
  - MILE-01
  - MILE-02
  - MILE-03

must_haves:
  truths:
    - "MilestoneManager can create milestones with name, scope, and phases"
    - "MilestoneManager can mark milestones complete and archive them"
    - "MilestoneManager can calculate milestone progress by phases completed"
    - "MilestoneManager validates milestone scope modifications"
  artifacts:
    - path: "src/types/milestone.ts"
      provides: "Milestone type definitions and Zod schemas"
      exports: ["Milestone", "MilestoneSchema", "MilestoneProgress", "MilestoneArchiveEntry"]
    - path: "src/storage/milestone-manager.ts"
      provides: "Milestone lifecycle management"
      exports: ["MilestoneManager"]
  key_links:
    - from: "src/storage/milestone-manager.ts"
      to: "src/storage/file-manager.ts"
      via: "RoadmapManager integration"
      pattern: "new MilestoneManager\\(roadmapManager\\)"
    - from: "src/storage/milestone-manager.ts"
      to: ".planning/ROADMAP.md"
      via: "read/write milestone entries"
      pattern: "readRoadmap|writeRoadmap"
---

<objective>
Implement MilestoneManager class for complete milestone lifecycle management.

Purpose: Provide the foundation for all milestone operations (create, track, complete, archive).
Output: MilestoneManager class with Zod-validated types and comprehensive test coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-management/05-CONTEXT.md

# Reference existing patterns
@src/storage/roadmap-manager.ts
@src/types/roadmap.ts
@src/state/state-manager.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Define Milestone types and Zod schemas</name>
  <files>src/types/milestone.ts</files>
  <action>
Create Milestone type definitions following existing patterns from roadmap.ts:

1. Define Milestone interface with required fields:
   - name: string (milestone identifier, e.g., "v1.1 Full Command Implementation")
   - scope: string (description of what the milestone delivers)
   - phases: number[] (array of phase numbers included in milestone)
   - theme: string | null (optional theme/slogan)
   - status: 'planned' | 'in-progress' | 'completed'
   - startedAt: string | null (ISO date when first phase started)
   - completedAt: string | null (ISO date when marked complete)
   - createdAt: string (ISO date when milestone was created)

2. Define MilestoneProgress interface:
   - milestoneName: string
   - phasesCompleted: number
   - phasesTotal: number
   - percentage: number
   - phaseStatus: { number: number, status: string }[]

3. Define MilestoneArchiveEntry interface:
   - name: string
   - scope: string
   - phases: number[]
   - startedAt: string | null
   - completedAt: string
   - plansCompleted: number
   - totalPlans: number
   - executionTime: string (e.g., "2.5 hours")
   - requirementsAddressed: string[]

4. Create matching Zod schemas:
   - MilestoneSchema
   - MilestoneProgressSchema
   - MilestoneArchiveEntrySchema

5. Export all types and schemas from src/types/index.ts

Follow the Type + Schema pattern from existing codebase (every TypeScript interface has matching Zod schema).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(milestone|error)" || echo "TypeScript compilation passed"</automated>
    <manual>Verify Milestone types compile without errors</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Milestone, MilestoneProgress, and MilestoneArchiveEntry types defined with matching Zod schemas, exports added to types/index.ts</done>
</task>

<task type="auto">
  <name>task 2: Implement MilestoneManager class</name>
  <files>src/storage/milestone-manager.ts</files>
  <action>
Create MilestoneManager class following patterns from RoadmapManager and SessionManager:

1. Constructor accepts RoadmapManager instance for ROADMAP.md integration
2. Implement resolveMilestoneArchivePath() - check .planning/MILESTONE-ARCHIVE.md

3. Implement core methods:
   - createMilestone(name: string, scope: string, phases: number[], theme?: string): Milestone
     * Add milestone section to ROADMAP.md (header + bullet list format)
     * Return created milestone
   
   - getMilestone(name: string): Milestone | null
     * Parse milestone from ROADMAP.md section
   
   - getActiveMilestone(): Milestone | null
     * Find first non-completed milestone
   
   - getMilestoneProgress(name: string): MilestoneProgress
     * Calculate by phases completed (primary metric per CONTEXT.md)
     * Query ROADMAP.md for phase completion status
   
   - completeMilestone(name: string): MilestoneArchiveEntry
     * Archive milestone to MILESTONE-ARCHIVE.md (append mode)
     * Include summary + metrics (plans completed, execution time, requirements)
     * Collapse milestone phases to <details> section in ROADMAP.md
     * Return archive entry
   
   - canModifyPhase(phaseNumber: number): { allowed: boolean, warning?: string }
     * Check if phase is part of active milestone
     * Return warning if modification affects milestone scope

4. Use atomic writes via FileManager for MILESTONE-ARCHIVE.md
5. Parse ROADMAP.md milestone sections using regex patterns from RoadmapManager

Error handling: Return null for missing milestones, throw for validation errors.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="milestone-manager" --passWithNoTests 2>&1 | tail -20</automated>
    <manual>Verify MilestoneManager compiles and basic structure is correct</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>MilestoneManager class with createMilestone, getMilestone, getMilestoneProgress, completeMilestone, canModifyPhase methods implemented</done>
</task>

<task type="auto">
  <name>task 3: Create comprehensive test suite for MilestoneManager</name>
  <files>src/tests/storage/milestone-manager.test.ts</files>
  <action>
Create Jest test suite for MilestoneManager following patterns from roadmap-manager.test.ts:

1. Mock dependencies:
   - jest.mock RoadmapManager
   - jest.mock fs/path for file operations

2. Test createMilestone():
   - Creates milestone with required fields
   - Adds milestone section to ROADMAP.md
   - Validates phase numbers exist

3. Test getMilestone():
   - Returns milestone when found
   - Returns null when not found
   - Parses milestone from ROADMAP.md correctly

4. Test getActiveMilestone():
   - Returns first non-completed milestone
   - Returns null when all completed or no milestones

5. Test getMilestoneProgress():
   - Calculates phases completed / total
   - Returns percentage
   - Includes phase status breakdown

6. Test completeMilestone():
   - Archives to MILESTONE-ARCHIVE.md
   - Includes summary + metrics
   - Collapses phases in ROADMAP.md
   - Returns archive entry

7. Test canModifyPhase():
   - Returns allowed=true with warning when phase in active milestone
   - Returns allowed=true without warning when phase not in milestone

Target 80%+ coverage threshold.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="milestone-manager" --coverage --coverageReporters=text 2>&1 | tail -30</automated>
    <manual>Verify all tests pass with 80%+ coverage</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All MilestoneManager tests passing with 80%+ coverage</done>
</task>

</tasks>

<verification>
- [ ] Milestone types defined with Zod schemas
- [ ] MilestoneManager class implements all required methods
- [ ] Tests cover create, get, progress, complete, and validation
- [ ] Coverage meets 80% threshold
</verification>

<success_criteria>
- MilestoneManager can create milestones with name, scope, and phases
- MilestoneManager can mark milestones complete and archive them
- MilestoneManager calculates progress by phases completed
- All tests passing with 80%+ coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-management/05-01-SUMMARY.md`
</output>
