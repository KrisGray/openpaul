---
phase: 05-milestone-management
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - src/tests/integration/milestone-integration.test.ts
autonomous: true
requirements:
  - MILE-01
  - MILE-02
  - MILE-03

must_haves:
  truths:
    - "All milestone commands work together in end-to-end workflow"
    - "Milestone lifecycle (create → track → complete) verified"
    - "Integration with ROADMAP.md and STATE.md validated"
    - "Progress calculation accurate across phases"
  artifacts:
    - path: "src/tests/integration/milestone-integration.test.ts"
      provides: "End-to-end milestone workflow tests"
  key_links:
    - from: "milestone command"
      to: "complete-milestone command"
      via: "Milestone lifecycle"
      pattern: "createMilestone.*completeMilestone"
---

<objective>
Add comprehensive integration tests for milestone management operations.

Purpose: Verify all milestone commands work together in complete workflow.
Output: Integration test suite covering create → track → complete lifecycle.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-management/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Create integration test suite for milestone lifecycle</name>
  <files>src/tests/integration/milestone-integration.test.ts</files>
  <action>
Create integration test suite covering complete milestone workflow:

1. Test complete milestone lifecycle:
   ```
   Create milestone → Track progress → Complete milestone → Verify archive
   ```

2. Integration test scenarios:
   a. Full lifecycle happy path:
      - Create milestone with /openpaul:milestone
      - Verify ROADMAP.md has milestone section
      - Update STATE.md to track milestone
      - Simulate phase completions
      - Verify progress updates correctly
      - Complete milestone with /openpaul:complete-milestone
      - Verify MILESTONE-ARCHIVE.md entry
      - Verify ROADMAP.md has collapsed <details> section

   b. discuss-milestone workflow:
      - Run /openpaul:discuss-milestone for upcoming milestone
      - Verify MILESTONE-CONTEXT.md created
      - Verify content structure

   c. Edge cases:
      - Create milestone with duplicate name (error)
      - Complete already completed milestone (error)
      - Modify phase in active milestone (warning)
      - Progress calculation with partial phase completion

3. Test utilities:
   - Create temp ROADMAP.md for isolation
   - Create temp STATE.md for isolation
   - Clean up after each test

4. Use real file operations for integration tests (not mocks)

5. Verify integration with existing components:
   - RoadmapManager
   - StateManager
   - FileManager (atomic writes)

Target 80%+ coverage for integration paths.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="milestone-integration" --coverage --coverageReporters=text 2>&1 | tail -30</automated>
    <manual>Verify all integration tests pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All milestone integration tests passing</done>
</task>

<task type="auto">
  <name>task 2: Update help output with milestone commands</name>
  <files>src/commands/help.ts</files>
  <action>
Update help.ts to include Phase 5 milestone commands in the command grouping:

1. Add new command group:
   ```
   ### Milestone Management
   - /openpaul:milestone — Create new milestone
   - /openpaul:complete-milestone — Mark milestone complete and archive
   - /openpaul:discuss-milestone — Plan upcoming milestone
   ```

2. Ensure help output is consistent with existing format
3. Add tests for new help output if needed
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="help" --passWithNoTests 2>&1 | tail -20</automated>
    <manual>Verify help output includes milestone commands</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Help output updated with milestone command group</done>
</task>

</tasks>

<verification>
- [ ] Integration tests cover complete lifecycle
- [ ] Tests verify ROADMAP.md integration
- [ ] Tests verify STATE.md integration
- [ ] Tests verify MILESTONE-ARCHIVE.md
- [ ] Help output updated
</verification>

<success_criteria>
- All milestone commands work together in end-to-end workflow
- Integration tests verify complete lifecycle
- Help output includes new commands
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-management/05-05-SUMMARY.md`
</output>
