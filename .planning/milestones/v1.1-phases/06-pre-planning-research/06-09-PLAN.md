---
phase: 06-pre-planning-research
plan: 09
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/tests/storage/pre-planning-manager.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-04
user_setup: []

must_haves:
  truths:
    - "PrePlanningManager tests verify all path resolution methods"
    - "Tests cover artifact generation for all artifact types"
    - "Tests verify dual path support (.paul and .openpaul)"
    - "Tests achieve 80%+ coverage"
  artifacts:
    - path: "src/tests/storage/pre-planning-manager.test.ts"
      provides: "Comprehensive test coverage for PrePlanningManager"
      contains: "describe('PrePlanningManager')"
  key_links:
    - from: "src/tests/storage/pre-planning-manager.test.ts"
      to: "src/storage/pre-planning-manager.ts"
      via: "Unit tests"
      pattern: "PrePlanningManager"
    - from: "src/tests/storage/pre-planning-manager.test.ts"
      to: "src/types/pre-planning.ts"
      via: "Type validation in tests"
      pattern: "import.*pre-planning"
---

<objective>
Create comprehensive tests for PrePlanningManager covering path resolution and artifact generation.

Purpose: Ensure PrePlanningManager works correctly for all pre-planning artifact types.
Output: Test file with 80%+ coverage for PrePlanningManager.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing test patterns
@src/tests/storage/milestone-manager.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create PrePlanningManager test suite</name>
  <files>src/tests/storage/pre-planning-manager.test.ts</files>
  <action>
Create src/tests/storage/pre-planning-manager.test.ts following the pattern from src/tests/storage/milestone-manager.test.ts.

The test suite should cover:

1. **Path resolution tests**:
   - resolvePhaseDir() finds phase directory in .paul/phases/
   - resolvePhaseDir() finds phase directory in .planning/phases/
   - resolvePhaseDir() returns null when not found
   - resolveContextPath() finds existing CONTEXT.md
   - resolveAssumptionsPath() finds existing ASSUMPTIONS.md
   - resolveIssuesPath() finds existing ISSUES.md
   - resolveDiscoveryPath() finds existing DISCOVERY.md
   - All path methods handle missing directories gracefully

2. **Context artifact generation**:
   - createContext() generates valid markdown structure
   - Template includes all required sections (domain, decisions, specifics, deferred)
   - Timestamps are correctly formatted
   - Empty deferred shows default message

3. **Assumptions artifact generation**:
   - createAssumptions() generates valid table format
   - Validation status enum values work
   - Confidence level enum values work
   - Impact descriptions included

4. **Issues artifact generation**:
   - createIssues() generates severity-organized output
   - Critical issues appear before high/medium/low
   - Affected areas parsed correctly
   - Mitigation strategies included

5. **Discovery artifact generation**:
   - createDiscovery() generates valid markdown
   - All depth modes supported (quick/standard/deep)
   - Options considered section included
   - Recommendation and references included

6. **Error handling**:
   - Invalid phase numbers handled
   - Missing required fields caught
   - Write failures handled gracefully

Use jest.mock for fs and atomic-writes.
Follow existing beforeEach/afterEach patterns.
Use describe blocks for method grouping.
  </action>
  <verify>
    <automated>npx jest src/tests/storage/pre-planning-manager.test.ts --passWithNoTests 2>&1 | tail -20</automated>
    <manual>Verify tests pass and coverage meets threshold</manual>
  </verify>
  <done>PrePlanningManager tests pass with comprehensive coverage</done>
</task>

</tasks>

<verification>
- [ ] Test file compiles without errors
- [ ] All path resolution methods tested
- [ ] All artifact generation methods tested
- [ ] Dual path support verified
- [ ] Coverage meets 80% threshold
</verification>

<success_criteria>
- Test suite runs without errors
- All PrePlanningManager methods tested
- Edge cases covered
- 80%+ code coverage achieved
</success_criteria>

<output>
After completion, create `.planning/phases/06-pre-planning-research/06-09-SUMMARY.md`
</output>
