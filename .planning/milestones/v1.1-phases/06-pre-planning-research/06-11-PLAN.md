---
phase: 06-pre-planning-research
plan: 11
type: execute
wave: 3
depends_on:
  - 06-03
  - 06-04
  - 06-05
  - 06-06
files_modified:
  - src/tests/commands/discuss.test.ts
  - src/tests/commands/assumptions.test.ts
  - src/tests/commands/discover.test.ts
  - src/tests/commands/consider-issues.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - BRND-02
user_setup: []

must_haves:
  truths:
    - "All pre-planning commands have comprehensive tests"
    - "Tests verify artifact generation and output formatting"
    - "Tests cover error handling and edge cases"
    - "All tests pass with 80%+ coverage"
  artifacts:
    - path: "src/tests/commands/discuss.test.ts"
      provides: "Tests for /openpaul:discuss command"
      contains: "describe('paulDiscuss')"
    - path: "src/tests/commands/assumptions.test.ts"
      provides: "Tests for /openpaul:assumptions command"
      contains: "describe('paulAssumptions')"
    - path: "src/tests/commands/discover.test.ts"
      provides: "Tests for /openpaul:discover command with depth modes"
      contains: "describe('paulDiscover')"
    - path: "src/tests/commands/consider-issues.test.ts"
      provides: "Tests for /openpaul:consider-issues command"
      contains: "describe('paulConsiderIssues')"
  key_links:
    - from: "src/tests/commands/*.test.ts"
      to: "src/commands/*.ts"
      via: "Unit tests for command implementations"
      pattern: "paulDiscuss|paulAssumptions|paulDiscover|paulConsiderIssues"
---

<objective>
Create comprehensive tests for all pre-planning commands (discuss, assumptions, discover, consider-issues).

Purpose: Ensure all pre-planning commands work correctly with proper output formatting and error handling.
Output: Test files with 80%+ coverage for each command.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing test patterns
@src/tests/commands/discuss-milestone.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create discuss command tests</name>
  <files>src/tests/commands/discuss.test.ts</files>
  <action>
Create src/tests/commands/discuss.test.ts following the pattern from src/tests/commands/discuss-milestone.test.ts.

Test cases:
1. **Successful context creation**:
   - Creates CONTEXT.md with all fields
   - File created in correct phase directory
   - Template includes all required sections
   - Returns formatted success message

2. **Existing file handling**:
   - Prompts to overwrite without flag
   - Overwrites with --overwrite flag

3. **Error handling**:
   - Phase directory not found
   - Invalid phase number
   - Write failure

Use jest.mock for fs, atomic-writes, and @opencode-ai/plugin.
  </action>
  <verify>
    <automated>npx jest src/tests/commands/discuss.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>discuss.test.ts passes with comprehensive coverage</done>
</task>

<task type="auto">
  <name>task 2: Create assumptions command tests</name>
  <files>src/tests/commands/assumptions.test.ts</files>
  <action>
Create src/tests/commands/assumptions.test.ts.

Test cases:
1. **Successful assumptions creation**:
   - Creates ASSUMPTIONS.md with table format
   - Parses comma-separated inputs
   - Assigns validation_status, confidence, impact
   - Returns formatted output with counts

2. **Input parsing**:
   - Parallel arrays for status/confidence/impact
   - Default values for missing fields
   - Handles mismatched array lengths

3. **Error handling**:
   - Invalid validation status value
   - Invalid confidence level
   - Write failure
  </action>
  <verify>
    <automated>npx jest src/tests/commands/assumptions.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>assumptions.test.ts passes with comprehensive coverage</done>
</task>

<task type="auto">
  <name>task 3: Create discover command tests with depth modes</name>
  <files>src/tests/commands/discover.test.ts</files>
  <action>
Create src/tests/commands/discover.test.ts.

Test cases:
1. **Quick mode (--quick flag)**:
   - Returns console output only
   - NO file created
   - Verbal response formatted

2. **Standard mode (default)**:
   - Creates DISCOVERY.md
   - 2-3 options included
   - Returns success message

3. **Deep mode (--deep flag)**:
   - Shows time estimate without --confirm
   - Prompts for confirmation
   - Creates DISCOVERY.md with --confirm
   - 5+ options included

4. **Mode conflict**:
   - Both --quick and --deep → error

5. **Error handling**:
   - Phase directory not found
   - Write failure
  </action>
  <verify>
    <automated>npx jest src/tests/commands/discover.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>discover.test.ts passes with depth mode coverage</done>
</task>

<task type="auto">
  <name>task 4: Create consider-issues command tests</name>
  <files>src/tests/commands/consider-issues.test.ts</files>
  <action>
Create src/tests/commands/consider-issues.test.ts.

Test cases:
1. **Successful issues creation**:
   - Creates ISSUES.md with severity categories
   - Issues sorted by severity (critical → low)
   - Table summary included
   - Returns formatted output with severity counts

2. **Severity handling**:
   - Validates severity values
   - Organizes output by severity sections
   - Pipe-separated areas parsed

3. **Error handling**:
   - Invalid severity value
   - Write failure
  </action>
  <verify>
    <automated>npx jest src/tests/commands/consider-issues.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>consider-issues.test.ts passes with comprehensive coverage</done>
</task>

</tasks>

<verification>
- [ ] All 4 test files compile without errors
- [ ] discuss.test.ts covers context creation
- [ ] assumptions.test.ts covers table format
- [ ] discover.test.ts covers all 3 depth modes
- [ ] consider-issues.test.ts covers severity sorting
- [ ] Coverage meets 80% threshold for each
</verification>

<success_criteria>
- All 4 test suites run without errors
- Depth mode conditional logic tested
- Output formatting verified
- Error handling tested
- 80%+ coverage achieved
</success_criteria>

<output>
After completion, create `.planning/phases/06-pre-planning-research/06-11-SUMMARY.md`
</output>
