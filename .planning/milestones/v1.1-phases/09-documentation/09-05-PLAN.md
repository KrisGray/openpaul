---
phase: 09-documentation
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/state/loop-enforcer.ts
  - src/commands/apply.ts
  - src/commands/unify.ts
autonomous: true
requirements:
  - BRND-01
must_haves:
  truths:
    - "Runtime guidance strings refer only to OpenPAUL and /openpaul:"
    - "Apply/Unify user-facing errors no longer mention PAUL or /paul:"
  artifacts:
    - path: "src/state/loop-enforcer.ts"
      provides: "User-facing next-step guidance for loop enforcement"
      contains: "openpaul:"
    - path: "src/commands/apply.ts"
      provides: "Apply command errors and help strings"
    - path: "src/commands/unify.ts"
      provides: "Unify command errors and help strings"
  key_links:
    - from: "src/state/loop-enforcer.ts"
      to: "OpenPAUL command guidance"
      via: "error/help strings"
      pattern: "openpaul:"
    - from: "src/commands/apply.ts"
      to: "OpenPAUL messaging"
      via: "user-facing error strings"
      pattern: "OpenPAUL"
---

<objective>
Replace remaining PAUL branding in runtime guidance and error strings.

Purpose: Ensure user-facing runtime messages align with OpenPAUL branding.
Output: Updated command and loop enforcement strings with OpenPAUL prefix.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@.planning/phases/09-documentation/09-RESEARCH.md
@.planning/phases/09-documentation/09-VERIFICATION.md
@.planning/phases/09-documentation/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update loop enforcement guidance strings</name>
  <files>src/state/loop-enforcer.ts</files>
  <action>Replace remaining "PAUL" and "/paul:" guidance in loop-enforcer output with "OpenPAUL" and "/openpaul:". Keep wording and formatting intact besides branding. Do not change behavior or logic.</action>
  <verify>rg -n "\\bPAUL\\b|/paul:" src/state/loop-enforcer.ts</verify>
  <done>No matches remain for PAUL or /paul: in loop-enforcer output strings.</done>
</task>

<task type="auto">
  <name>task 2: Update apply/unify user-facing error messages</name>
  <files>src/commands/apply.ts, src/commands/unify.ts</files>
  <action>Replace remaining "PAUL" references in user-facing error/help strings with "OpenPAUL" and update any "/paul:" command guidance to "/openpaul:". Keep message tone and line breaks unchanged.</action>
  <verify>rg -n "\\bPAUL\\b|/paul:" src/commands/apply.ts src/commands/unify.ts</verify>
  <done>Apply/unify command strings contain only OpenPAUL branding and /openpaul: guidance.</done>
</task>

</tasks>

<verification>
rg -n "\\bPAUL\\b|/paul:" src/state/loop-enforcer.ts src/commands/apply.ts src/commands/unify.ts
</verification>

<success_criteria>
Runtime guidance and errors show OpenPAUL branding only, with no PAUL or /paul: references in the updated files.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-05-SUMMARY.md`
</output>
