---
phase: 09-documentation
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/milestone.ts
  - src/commands/complete-milestone.ts
  - src/commands/discuss-milestone.ts
  - src/commands/discuss.ts
  - src/commands/assumptions.ts
  - src/commands/discover.ts
  - src/commands/consider-issues.ts
  - src/commands/research.ts
  - src/commands/research-phase.ts
  - src/commands/verify.ts
  - src/commands/plan-fix.ts
  - src/commands/config.ts
  - src/commands/flows.ts
  - src/commands/map-codebase.ts
autonomous: true
requirements: [BRND-01]
must_haves:
  truths:
    - "All remaining /openpaul: commands work (milestone, discuss, research, etc.)"
    - "No paulX function exports remain in any command file"
  artifacts:
    - path: "src/commands/milestone.ts"
      provides: "Milestone command with openpaulMilestone export"
      contains: "export const openpaulMilestone"
    - path: "src/commands/verify.ts"
      provides: "Verify command with openpaulVerify export"
      contains: "export const openpaulVerify"
  key_links:
    - from: "command files"
      to: "plugin registration"
      via: "exports"
      pattern: "openpaul[A-Z]"
---

<objective>
Rename second batch of command function exports from paulX to openpaulX (14 files).

Purpose: Complete command function renames for consistent OpenPAUL branding (part 2 of 3)
Output: 14 command files with openpaulX function exports
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@.planning/phases/09-documentation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename remaining 14 command function exports</name>
  <files>src/commands/milestone.ts, src/commands/complete-milestone.ts, src/commands/discuss-milestone.ts, src/commands/discuss.ts, src/commands/assumptions.ts, src/commands/discover.ts, src/commands/consider-issues.ts, src/commands/research.ts, src/commands/research-phase.ts, src/commands/verify.ts, src/commands/plan-fix.ts, src/commands/config.ts, src/commands/flows.ts, src/commands/map-codebase.ts</files>
  <action>
For each of these 14 command files:

1. Rename the exported function constant from `paulX` to `openpaulX`:
   - `paulMilestone` → `openpaulMilestone`
   - `paulCompleteMilestone` → `openpaulCompleteMilestone`
   - `paulDiscussMilestone` → `openpaulDiscussMilestone`
   - `paulDiscuss` → `openpaulDiscuss`
   - `paulAssumptions` → `openpaulAssumptions`
   - `paulDiscover` → `openpaulDiscover`
   - `paulConsiderIssues` → `openpaulConsiderIssues`
   - `paulResearch` → `openpaulResearch`
   - `paulResearchPhase` → `openpaulResearchPhase`
   - `paulVerify` → `openpaulVerify`
   - `paulPlanFix` → `openpaulPlanFix`
   - `paulConfig` → `openpaulConfig`
   - `paulFlows` → `openpaulFlows`
   - `paulMapCodebase` → `openpaulMapCodebase`

2. Update the tool `name` property from `paul:x` to `openpaul:x`

3. Update any user-facing strings from `/paul:` to `/openpaul:`

Do NOT change source file names. Do NOT update index.ts yet (that's plan 09-10).
  </action>
  <verify>
    <automated>grep -r "export const paul" src/commands/ --include="*.ts" | grep -v index.ts | wc -l | xargs -I {} sh -c 'test {} -eq 0 && echo PASS || echo FAIL: {} paul exports remain'</automated>
  </verify>
  <done>All 26 command files export openpaulX functions, no paulX exports remain</done>
</task>

</tasks>

<verification>
- Verify no paulX function references remain in src/commands/*.ts
</verification>

<success_criteria>
- All 26 command files (from 09-01 + 09-09) export openpaulX functions
- No paulX exports remain in command files
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-09-SUMMARY.md`
</output>
