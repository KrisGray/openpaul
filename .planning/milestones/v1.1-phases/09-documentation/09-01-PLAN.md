---
phase: 09-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/init.ts
  - src/commands/plan.ts
  - src/commands/apply.ts
  - src/commands/unify.ts
  - src/commands/progress.ts
  - src/commands/status.ts
  - src/commands/help.ts
  - src/commands/pause.ts
  - src/commands/resume.ts
  - src/commands/handoff.ts
  - src/commands/add-phase.ts
  - src/commands/remove-phase.ts
autonomous: true
requirements: [BRND-01]
must_haves:
  truths:
    - "Running /openpaul:init works (command is registered)"
    - "Running /openpaul:plan works (command is registered)"
    - "Help text shows /openpaul: commands only (no /paul: aliases)"
  artifacts:
    - path: "src/commands/init.ts"
      provides: "Init command with openpaulInit export"
      contains: "export const openpaulInit"
    - path: "src/commands/plan.ts"
      provides: "Plan command with openpaulPlan export"
      contains: "export const openpaulPlan"
  key_links:
    - from: "command files"
      to: "plugin registration"
      via: "exports"
      pattern: "openpaul[A-Z]"
---

<objective>
Rename first batch of command function exports from paulX to openpaulX (12 files).

Purpose: Establish consistent OpenPAUL branding in code identifiers (part 1 of 3)
Output: 12 command files with openpaulX function exports
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@.planning/phases/09-documentation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename first 12 command function exports</name>
  <files>src/commands/init.ts, src/commands/plan.ts, src/commands/apply.ts, src/commands/unify.ts, src/commands/progress.ts, src/commands/status.ts, src/commands/help.ts, src/commands/pause.ts, src/commands/resume.ts, src/commands/handoff.ts, src/commands/add-phase.ts, src/commands/remove-phase.ts</files>
  <action>
For each of these 12 command files:

1. Rename the exported function constant from `paulX` to `openpaulX`:
   - `paulInit` → `openpaulInit`
   - `paulPlan` → `openpaulPlan`
   - `paulApply` → `openpaulApply`
   - `paulUnify` → `openpaulUnify`
   - `paulProgress` → `openpaulProgress`
   - `paulStatus` → `openpaulStatus`
   - `paulHelp` → `openpaulHelp`
   - `paulPause` → `openpaulPause`
   - `paulResume` → `openpaulResume`
   - `paulHandoff` → `openpaulHandoff`
   - `paulAddPhase` → `openpaulAddPhase`
   - `paulRemovePhase` → `openpaulRemovePhase`

2. Update the tool `name` property from `paul:x` to `openpaul:x`

3. Update any user-facing strings from `/paul:` to `/openpaul:`

Do NOT change source file names. Do NOT update index.ts yet (that's plan 09-10).
  </action>
  <verify>
    <automated>grep -c "export const openpaul" src/commands/init.ts src/commands/plan.ts src/commands/apply.ts src/commands/unify.ts src/commands/progress.ts src/commands/status.ts src/commands/help.ts src/commands/pause.ts src/commands/resume.ts src/commands/handoff.ts src/commands/add-phase.ts src/commands/remove-phase.ts | grep -v ":0" | wc -l | xargs -I {} sh -c 'test {} -eq 12 && echo PASS || echo FAIL: only {} files have openpaul exports'</automated>
  </verify>
  <done>All 12 command files export openpaulX functions</done>
</task>

</tasks>

<verification>
- Run `npm run build` to verify TypeScript compiles (will have import errors until 09-10 completes)
</verification>

<success_criteria>
- 12 command files export openpaulX functions
- TypeScript compilation succeeds (or fails only on missing imports in index.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-01-SUMMARY.md`
</output>
