---
phase: 08-configuration
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/storage/config-manager.ts
  - src/commands/config.ts
autonomous: true
requirements:
  - CONF-01
must_haves:
  truths:
    - Config values resolve with precedence: env vars > CLI overrides > config file > defaults
    - Missing required values return a clear, actionable error listing required keys
    - Shallow merge applies per section without implicit keys
  artifacts:
    - path: src/storage/config-manager.ts
      provides: Precedence resolution and validation helpers for config
      min_lines: 180
    - path: src/commands/config.ts
      provides: Config command output using resolved config
      min_lines: 120
  key_links:
    - from: src/commands/config.ts
      to: src/storage/config-manager.ts
      via: "resolveEffectiveConfig"
      pattern: "resolve"
---

<objective>
Implement config precedence resolution and validation rules from Phase 8 decisions.

Purpose: Ensure config values resolve deterministically and missing required keys are surfaced clearly.

Output: ConfigManager precedence logic and command output using resolved config.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configuration/08-CONTEXT.md
@.planning/phases/08-configuration/08-RESEARCH.md
@src/storage/config-manager.ts
@src/commands/config.ts
</context>

<tasks>

<task type="auto">
  <name>Add config precedence resolution and validation helpers</name>
  <files>src/storage/config-manager.ts</files>
  <action>
Extend ConfigManager with precedence and validation logic:
- Add a resolveEffectiveConfig({ overrides }) method that merges defaults + file config + overrides.
- Apply shallow merge by section (project, integrations, preferences) without implicit keys.
- Add env var overrides with highest precedence. Map known keys (e.g., OPENPAUL_PROJECT_NAME, OPENPAUL_PROJECT_DESCRIPTION, OPENPAUL_PROJECT_VERSION, OPENPAUL_PREFERENCES_VERBOSE, OPENPAUL_PREFERENCES_PARALLELIZATION, OPENPAUL_PREFERENCES_AUTO_ADVANCE, OPENPAUL_INTEGRATIONS_SONARQUBE_ENABLED, OPENPAUL_INTEGRATIONS_SONARQUBE_URL, OPENPAUL_INTEGRATIONS_SONARQUBE_PROJECT_KEY, OPENPAUL_INTEGRATIONS_SONARQUBE_BRANCH).
- Add validateRequired() that returns a list of missing required keys (project.name required; if integrations.sonarqube.enabled then projectKey and url are required).
- Provide a consistent error message format: "Missing required config keys: project.name, integrations.sonarqube.projectKey".
  </action>
  <verify><automated>npx tsc --noEmit src/storage/config-manager.ts</automated></verify>
  <done>ConfigManager resolves precedence and reports missing required keys</done>
</task>

<task type="auto">
  <name>Use resolved config and validation in /openpaul:config</name>
  <files>src/commands/config.ts</files>
  <action>
Update /openpaul:config to use the resolved config and validation:
- list/get should call resolveEffectiveConfig() and fail if validateRequired() returns errors.
- set should refuse updates that violate required keys or unknown keys.
- When validation fails, show the missing keys and remind the user to edit .openpaul/config.md.
  </action>
  <verify><automated>npx tsc --noEmit src/commands/config.ts</automated></verify>
  <done>Config command reflects precedence rules and surfaces missing required keys clearly</done>
</task>

</tasks>

<verification>
- Precedence order: env > overrides > config file > defaults
- Missing required keys return a clear error message
- Shallow merge applies per section without implicit keys
</verification>

<success_criteria>
- Effective config can be resolved deterministically
- Required config values are enforced with clear errors
- /openpaul:config reflects the resolved values
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration/08-04-SUMMARY.md`
</output>
