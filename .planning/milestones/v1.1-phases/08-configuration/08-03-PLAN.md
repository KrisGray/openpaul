---
phase: 08-configuration
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/codebase-generator.ts
  - src/utils/directory-scanner.ts
  - src/commands/map-codebase.ts
autonomous: true
requirements:
  - CONF-03
must_haves:
  truths:
    - CODEBASE.md includes Structure, Stack, Conventions, Concerns, Integrations, Architecture sections
    - Codebase mapping scans top-level plus key subdirectories and skips vendor/fixtures
    - Regeneration overwrites CODEBASE.md and updates the Last updated timestamp
  artifacts:
    - path: src/utils/codebase-generator.ts
      provides: Codebase documentation generation with required sections
      min_lines: 240
    - path: src/utils/directory-scanner.ts
      provides: Directory scanning with exclude handling and depth limits
      min_lines: 200
    - path: src/commands/map-codebase.ts
      provides: /openpaul:map-codebase command output and file writing
      min_lines: 60
  key_links:
    - from: src/commands/map-codebase.ts
      to: src/utils/codebase-generator.ts
      via: "generateCodebaseDoc"
      pattern: "generateCodebaseDoc"
    - from: src/utils/codebase-generator.ts
      to: src/utils/directory-scanner.ts
      via: "scanDirectory"
      pattern: "scanDirectory"
---

<objective>
Bring /openpaul:map-codebase output in line with the required CODEBASE.md format.

Purpose: Produce consistent, medium-detail codebase documentation that skips vendor/fixtures.

Output: Updated generator and command behavior with the required sections and scope.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configuration/08-CONTEXT.md
@.planning/phases/08-configuration/08-RESEARCH.md
@src/utils/codebase-generator.ts
@src/utils/directory-scanner.ts
@src/commands/map-codebase.ts
</context>

<tasks>

<task type="auto">
  <name>Update codebase generator output format</name>
  <files>src/utils/codebase-generator.ts, src/utils/directory-scanner.ts</files>
  <action>
Update codebase-generator to produce the required CODEBASE.md sections:
- Structure, Stack, Conventions, Concerns, Integrations, Architecture (no extra top-level sections).
- Add a "Last updated" timestamp field near the top.
- Keep detail level medium (2-6 bullets per section) and avoid code dumps.

Adjust scanning scope:
- Build structure from the repository root and highlight key subdirectories (src, tests, docs, etc.).
- Ensure vendor/fixtures directories are excluded (node_modules, .git, dist, coverage, fixtures, vendor).
- Keep maxDepth handling consistent with options.
  </action>
  <verify><automated>npx tsc --noEmit src/utils/codebase-generator.ts</automated></verify>
  <done>Generator emits required sections and scanning skips vendor/fixtures paths</done>
</task>

<task type="auto">
  <name>Align map-codebase command with updated generator</name>
  <files>src/commands/map-codebase.ts</files>
  <action>
Update /openpaul:map-codebase to match the generator changes:
- Use the updated generator and ensure output overwrites CODEBASE.md each run.
- Include the output path and counts in the command response.
- Keep --maxDepth and --force behavior intact.
- Use atomic writes for CODEBASE.md.
  </action>
  <verify><automated>npx tsc --noEmit src/commands/map-codebase.ts</automated></verify>
  <done>Command writes the new CODEBASE.md format and reports the output path</done>
</task>

</tasks>

<verification>
- CODEBASE.md format includes all required sections only
- Scanning ignores vendor/fixtures directories
- Map-codebase overwrites output with a fresh timestamp
</verification>

<success_criteria>
- CODEBASE.md contains the six required sections with medium detail
- Scanning scope covers top-level and key subdirectories
- Output regenerates cleanly with updated timestamps
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration/08-03-SUMMARY.md`
</output>
