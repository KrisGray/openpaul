---
phase: 08-configuration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/config-manager.ts
  - src/commands/config.ts
  - src/templates/config.md
autonomous: true
requirements:
  - CONF-01
must_haves:
  truths:
    - Config loads from .openpaul/config.md YAML frontmatter with markdown notes preserved
    - /openpaul:config init/list/get/set works against explicit project, integrations, and preferences sections
    - Unknown config keys return clear errors instead of being ignored
  artifacts:
    - path: src/storage/config-manager.ts
      provides: YAML frontmatter parsing, strict config schema, and markdown-preserving writes
      min_lines: 140
    - path: src/commands/config.ts
      provides: Config command flow for init/list/get/set using ConfigManager
      min_lines: 120
    - path: src/templates/config.md
      provides: Config file template with YAML frontmatter and notes sections
      min_lines: 80
  key_links:
    - from: src/commands/config.ts
      to: src/storage/config-manager.ts
      via: "ConfigManager usage"
      pattern: "new ConfigManager"
    - from: src/storage/config-manager.ts
      to: src/templates/config.md
      via: "init template content"
      pattern: "config.md"
---

<objective>
Align the config file format and ConfigManager with the Phase 8 configuration decisions.

Purpose: Establish the YAML frontmatter + markdown notes format and explicit key handling for /openpaul:config.

Output: Updated config template, strict ConfigManager parsing, and command output aligned with the new format.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configuration/08-CONTEXT.md
@.planning/phases/08-configuration/08-RESEARCH.md
@src/storage/config-manager.ts
@src/commands/config.ts
@src/templates/config.md
</context>

<tasks>

<task type="auto">
  <name>Update config template and ConfigManager parsing</name>
  <files>src/templates/config.md, src/storage/config-manager.ts</files>
  <action>
Update src/templates/config.md to the new required format:
- Use YAML frontmatter between --- lines.
- Top-level keys only: project, integrations, preferences.
- Add markdown notes below the frontmatter explaining each section.
- Include SonarQube settings under integrations.sonarqube (enabled, url, projectKey, branch).
- Keep content ASCII and provide a minimal but complete example.

Update ConfigManager to load/write the frontmatter format:
- Parse YAML frontmatter from config.md (reject missing or malformed frontmatter with a clear error).
- Use strict Zod schemas (no unknown keys at any level) for project/integrations/preferences.
- Remove the legacy flows config section; flows live in SPECIAL-FLOWS.md.
- Preserve the markdown body when saving; only replace frontmatter.
- Init should write the template content to .openpaul/config.md.
  </action>
  <verify><automated>npx tsc --noEmit src/storage/config-manager.ts</automated></verify>
  <done>ConfigManager reads/writes YAML frontmatter and enforces explicit keys while preserving markdown notes</done>
</task>

<task type="auto">
  <name>Align /openpaul:config output and actions</name>
  <files>src/commands/config.ts</files>
  <action>
Update /openpaul:config to reflect the new config format:
- list: show project, integrations, preferences from ConfigManager.load() with friendly headings.
- get: require --key and surface missing key errors clearly.
- set: reject unknown keys and update frontmatter only (markdown body preserved).
- init: call ConfigManager.init and reference .openpaul/config.md in output.
- Ensure errors mention the allowed top-level keys.
  </action>
  <verify><automated>npx tsc --noEmit src/commands/config.ts</automated></verify>
  <done>/openpaul:config actions operate on the new YAML frontmatter config format</done>
</task>

</tasks>

<verification>
- ConfigManager enforces explicit keys with clear error messages
- Config template matches YAML frontmatter + markdown notes requirement
- Config command actions work against the updated format
</verification>

<success_criteria>
- .openpaul/config.md is generated with YAML frontmatter and notes
- Strict parsing rejects unknown keys
- Config command shows and edits the new sections reliably
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration/08-01-SUMMARY.md`
</output>
