---
phase: 08-configuration
plan: "05"
type: execute
wave: 2
depends_on:
  - "03"
files_modified:
  - src/utils/directory-scanner.ts
  - src/commands/map-codebase.ts
autonomous: true
requirements:
  - CONF-03
must_haves:
  truths:
    - Map-codebase skips scanning when cache is valid unless --force is used
    - Cache data is saved after generation with file stats for change detection
    - Large repositories can be scanned with depth limits and exclusions
  artifacts:
    - path: src/utils/directory-scanner.ts
      provides: Cache entry capture and save/load helpers
      min_lines: 220
    - path: src/commands/map-codebase.ts
      provides: Cache-aware map-codebase flow and --force bypass
      min_lines: 70
  key_links:
    - from: src/commands/map-codebase.ts
      to: src/utils/directory-scanner.ts
      via: "isCacheValid/loadCache/saveCache"
      pattern: "Cache"
---

<objective>
Add incremental codebase mapping support for large repositories.

Purpose: Avoid unnecessary rescans by persisting cache data and honoring --force.

Output: Cache-aware directory scanning and map-codebase command behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configuration/08-CONTEXT.md
@.planning/phases/08-configuration/08-RESEARCH.md
@src/utils/directory-scanner.ts
@src/commands/map-codebase.ts
</context>

<tasks>

<task type="auto">
  <name>Capture cache entries during directory scans</name>
  <files>src/utils/directory-scanner.ts</files>
  <action>
Extend directory-scanner to capture cache entries:
- Add a helper to collect { path, mtime, size } for all scanned files.
- Ensure cache collection honors exclude directories and maxDepth.
- Export a function that returns cache entries for the last scan so map-codebase can persist them.
- Keep cache versioning consistent with CACHE_VERSION.
  </action>
  <verify><automated>npx tsc --noEmit src/utils/directory-scanner.ts</automated></verify>
  <done>Directory scanning can produce cache entries for saveCache</done>
</task>

<task type="auto">
  <name>Persist cache data in map-codebase</name>
  <files>src/commands/map-codebase.ts</files>
  <action>
Update /openpaul:map-codebase to persist cache data after generation:
- After generateCodebaseDoc, call saveCache with collected entries.
- Keep isCacheValid/loadCache behavior for fast exits.
- Ensure --force bypasses cache validity checks.
- When cache is used, report that no rescan occurred.
  </action>
  <verify><automated>npx tsc --noEmit src/commands/map-codebase.ts</automated></verify>
  <done>Map-codebase saves cache data and honors --force</done>
</task>

</tasks>

<verification>
- Cache entries are saved after map-codebase runs
- Cached results are used when no changes are detected
- --force bypasses cache checks and regenerates output
</verification>

<success_criteria>
- Incremental mapping avoids unnecessary scans on large repos
- Cache stays consistent with file system changes
- Command messaging explains cache usage clearly
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration/08-05-SUMMARY.md`
</output>
