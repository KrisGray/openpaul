---
phase: 08-configuration
plan: "06"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
  - "04"
  - "05"
files_modified:
  - src/tests/commands/config.test.ts
  - src/tests/commands/flows.test.ts
  - src/tests/commands/map-codebase.test.ts
  - src/tests/storage/flows-manager.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
must_haves:
  truths:
    - Config, flows, and map-codebase commands have Jest coverage for core behaviors
    - FlowsManager tests match the enabled/disabled array format
    - All new tests pass with Jest
  artifacts:
    - path: src/tests/commands/config.test.ts
      provides: Config command behavior tests
      min_lines: 80
    - path: src/tests/commands/flows.test.ts
      provides: Flows command behavior tests
      min_lines: 80
    - path: src/tests/commands/map-codebase.test.ts
      provides: Map-codebase command tests including cache behavior
      min_lines: 80
    - path: src/tests/storage/flows-manager.test.ts
      provides: Flow parsing and validation tests for enabled/disabled arrays
      min_lines: 120
  key_links:
    - from: src/tests/commands/config.test.ts
      to: src/commands/config.ts
      via: "command execution"
      pattern: "openpaulConfig"
    - from: src/tests/commands/flows.test.ts
      to: src/commands/flows.ts
      via: "command execution"
      pattern: "openpaulFlows"
    - from: src/tests/commands/map-codebase.test.ts
      to: src/commands/map-codebase.ts
      via: "command execution"
      pattern: "openpaulMapCodebase"
---

<objective>
Add Jest coverage for configuration, flows, and map-codebase commands.

Purpose: Validate Phase 8 behavior and guard against regressions.

Output: New command tests and updated FlowsManager tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configuration/08-CONTEXT.md
@.planning/phases/08-configuration/08-RESEARCH.md
@src/commands/config.ts
@src/commands/flows.ts
@src/commands/map-codebase.ts
@src/storage/flows-manager.ts
@src/tests/commands
@src/tests/storage/flows-manager.test.ts
</context>

<tasks>

<task type="auto">
  <name>Add config and flows command tests</name>
  <files>src/tests/commands/config.test.ts, src/tests/commands/flows.test.ts, src/tests/storage/flows-manager.test.ts</files>
  <action>
Create Jest tests for /openpaul:config and /openpaul:flows:

Config command tests:
- init creates .openpaul/config.md with frontmatter.
- list reports project/integrations/preferences sections.
- get returns value for a known key and errors on missing key.
- set updates frontmatter and rejects unknown keys.
- validation error includes missing required keys.

Flows command tests:
- init creates SPECIAL-FLOWS.md with enabled/disabled arrays.
- list shows enabled and disabled flows.
- enable/disable updates the correct array.
- unknown flow ID returns an error listing valid flows.
- conflict in enabled/disabled arrays returns an error.

Update src/tests/storage/flows-manager.test.ts to the new array format and validation behavior.
Use temp directories and existing test patterns from other command tests.
  </action>
  <verify><automated>npm test -- --testPathPattern=commands/(config|flows).test.ts</automated></verify>
  <done>Config and flows command tests cover init/list/get/set and enable/disable behaviors</done>
</task>

<task type="auto">
  <name>Add map-codebase command tests</name>
  <files>src/tests/commands/map-codebase.test.ts</files>
  <action>
Create Jest tests for /openpaul:map-codebase:
- Generates CODEBASE.md with required sections.
- Respects maxDepth and exclude directories by verifying structure output.
- Saves cache data and skips regeneration when cache valid.
- --force bypasses the cache and regenerates output.

Use a temp directory with a small sample structure and clean up after tests.
  </action>
  <verify><automated>npm test -- --testPathPattern=commands/map-codebase.test.ts</automated></verify>
  <done>Map-codebase tests validate output format and cache behavior</done>
</task>

</tasks>

<verification>
- New tests run with Jest and follow existing patterns
- FlowsManager tests cover enabled/disabled arrays and validation
- Command tests cover success and error paths
</verification>

<success_criteria>
- Config, flows, and map-codebase tests added and passing
- Flow validation behavior is covered by tests
- Cache behavior is exercised in map-codebase tests
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration/08-06-SUMMARY.md`
</output>
