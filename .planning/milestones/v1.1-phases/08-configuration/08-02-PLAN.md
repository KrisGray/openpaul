---
phase: 08-configuration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/SPECIAL-FLOWS.md
  - src/storage/flows-manager.ts
  - src/commands/flows.ts
autonomous: true
requirements:
  - CONF-02
must_haves:
  truths:
    - SPECIAL-FLOWS.md uses enabled and disabled arrays with no ad-hoc flow definitions
    - /openpaul:flows rejects unknown flow IDs and reports conflicts clearly
    - Enabling or disabling a flow updates only the enabled/disabled lists
  artifacts:
    - path: src/templates/SPECIAL-FLOWS.md
      provides: Flow catalog plus enabled/disabled list format
      min_lines: 60
    - path: src/storage/flows-manager.ts
      provides: Flow parsing, validation, and enable/disable persistence
      min_lines: 160
    - path: src/commands/flows.ts
      provides: Command interface for list/enable/disable/init
      min_lines: 100
  key_links:
    - from: src/commands/flows.ts
      to: src/storage/flows-manager.ts
      via: "FlowsManager usage"
      pattern: "new FlowsManager"
    - from: src/storage/flows-manager.ts
      to: src/templates/SPECIAL-FLOWS.md
      via: "template initialization"
      pattern: "SPECIAL-FLOWS.md"
---

<objective>
Implement the explicit flow toggle format and validation rules for /openpaul:flows.

Purpose: Support specialized flow enable/disable behavior with strict validation and no ad-hoc flow creation.

Output: Updated SPECIAL-FLOWS.md template, FlowsManager parsing/validation, and command behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configuration/08-CONTEXT.md
@.planning/phases/08-configuration/08-RESEARCH.md
@src/templates/SPECIAL-FLOWS.md
@src/storage/flows-manager.ts
@src/commands/flows.ts
</context>

<tasks>

<task type="auto">
  <name>Revise SPECIAL-FLOWS.md and FlowsManager parsing</name>
  <files>src/templates/SPECIAL-FLOWS.md, src/storage/flows-manager.ts</files>
  <action>
Update src/templates/SPECIAL-FLOWS.md to the new flow toggle format:
- YAML frontmatter with enabled and disabled arrays.
- Include a "Flow Catalog" section listing valid flow IDs with short descriptions.
- Provide a small default catalog (3-5 flows) and ensure none are duplicated.

Update FlowsManager to match the new format:
- Parse frontmatter enabled/disabled arrays.
- Maintain a constant list of valid flow IDs (from the template catalog).
- Reject unknown IDs with an error that lists valid flow IDs.
- Reject conflicts where the same flow is in both arrays.
- Preserve markdown body on save; only update frontmatter arrays.
- Remove legacy table parsing logic.
  </action>
  <verify><automated>npx tsc --noEmit src/storage/flows-manager.ts</automated></verify>
  <done>FlowsManager reads enabled/disabled arrays and validates flow IDs and conflicts</done>
</task>

<task type="auto">
  <name>Align /openpaul:flows command with strict flow rules</name>
  <files>src/commands/flows.ts</files>
  <action>
Update /openpaul:flows to use the updated FlowsManager:
- list: show enabled and disabled flow IDs in separate sections.
- enable/disable: validate flow ID against the catalog and surface unknown/conflict errors.
- init: write the updated template and report the file path.
- Ensure messaging mentions valid flow IDs when errors occur.
  </action>
  <verify><automated>npx tsc --noEmit src/commands/flows.ts</automated></verify>
  <done>/openpaul:flows enforces enabled/disabled arrays with clear validation errors</done>
</task>

</tasks>

<verification>
- SPECIAL-FLOWS.md template includes enabled/disabled arrays and flow catalog
- FlowsManager rejects unknown IDs and conflicts
- Flows command lists and toggles flows using the new format
</verification>

<success_criteria>
- Flow toggles are stored only in enabled/disabled arrays
- Unknown or conflicting flow IDs yield clear errors
- Command behavior matches the Phase 8 decisions
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration/08-02-SUMMARY.md`
</output>
