---
phase: 04-roadmap-management
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/commands/remove-phase.test.ts
  - src/roadmap/roadmap-manager.ts
autonomous: true
gap_closure: true
requirements: [ROAD-01, ROAD-02]

must_haves:
  truths:
    - "Remove-phase command tests execute successfully without syntax errors"
    - "Add-phase command updates STATE.md progress tracking when new phase is added"
  artifacts:
    - path: "src/tests/commands/remove-phase.test.ts"
      provides: "Test suite for remove-phase command"
      min_lines: 280
    - path: "src/roadmap/roadmap-manager.ts"
      provides: "Phase manipulation with STATE.md tracking"
      contains: "updateStateProgress"
  key_links:
    - from: "src/roadmap/roadmap-manager.ts"
      to: ".paul/STATE.md"
      via: "writeFileSync after addPhase"
      pattern: "Total Phases"
---

<objective>
Fix verification gaps blocking Phase 4 completion: test syntax error and missing STATE.md progress tracking

Purpose: Ensure all roadmap management commands work correctly and maintain STATE.md consistency
Output: Working test suite and complete ROAD-01 compliance
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-roadmap-management/04-VERIFICATION.md
@.planning/phases/04-roadmap-management/04-01-SUMMARY.md

Gap sources from VERIFICATION.md:
1. Line 10 in remove-phase.test.ts: `import { formatHeader, from '../../output/formatter'` - missing closing brace
2. addPhase() in roadmap-manager.ts does not update STATE.md Total Phases count

Existing implementation reference:
- RoadmapManager class at src/roadmap/roadmap-manager.ts:144-273 (addPhase method)
- Test file at src/tests/commands/remove-phase.test.ts
- STATE.md has `**Total Phases:** 9` pattern that needs incrementing
</context>

<tasks>

<task type="auto">
  <name>Fix syntax error in remove-phase.test.ts import</name>
  <files>src/tests/commands/remove-phase.test.ts</files>
  <action>
    Fix line 10 malformed import statement.

    Current (broken):
    ```typescript
    import { formatHeader, from '../../output/formatter'
    ```

    Fixed:
    ```typescript
    import { formatHeader, formatBold, formatList } from '../../output/formatter'
    ```

    This enables the test file to compile and the 18 test cases to execute.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/tests/commands/remove-phase.test.ts 2>&1 | head -20</automated>
    <manual>Verify no TypeScript compilation errors</manual>
  </verify>
  <done>Test file compiles without syntax errors, all 18 test cases can execute</done>
</task>

<task type="auto">
  <name>Add STATE.md progress tracking to addPhase()</name>
  <files>src/roadmap/roadmap-manager.ts</files>
  <action>
    Update RoadmapManager.addPhase() to also update STATE.md progress tracking.

    Add a private method `updateStateProgress()` that:
    1. Resolves STATE.md path (check .paul/STATE.md then .openpaul/STATE.md)
    2. Reads current content
    3. Increments `**Total Phases:** N` by 1
    4. Writes updated content back

    Call this method at the end of addPhase() after writing ROADMAP.md and creating the directory.

    Pattern to update in STATE.md:
    - `**Total Phases:** 9` → `**Total Phases:** 10`

    If STATE.md does not exist, skip silently (not all projects have STATE.md).

    Reference existing patterns in roadmap-manager.ts:
    - resolveRoadmapPath() for path resolution pattern
    - readFileSync/writeFileSync for file operations
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="roadmap-manager" --testNamePattern="addPhase" 2>&1 | tail -30</automated>
    <manual>Add test case verifying STATE.md Total Phases is incremented</manual>
  </verify>
  <done>addPhase() updates STATE.md Total Phases count when STATE.md exists</done>
</task>

</tasks>

<verification>
Run full test suite to verify no regressions:
```bash
npm test -- --testPathPattern="roadmap|add-phase|remove-phase"
```

Expected: All tests pass, including the previously broken remove-phase.test.ts
</verification>

<success_criteria>
- [ ] remove-phase.test.ts compiles without syntax errors
- [ ] All 18 remove-phase test cases execute
- [ ] RoadmapManager.addPhase() updates STATE.md Total Phases when present
- [ ] Full roadmap test suite passes (roadmap-manager, add-phase, remove-phase)
- [ ] ROAD-01 fully satisfied (ROADMAP.md + directory + STATE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/04-roadmap-management/04-05-SUMMARY.md`
</output>
