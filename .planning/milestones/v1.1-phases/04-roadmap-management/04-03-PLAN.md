---
phase: 04-roadmap-management
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/commands/remove-phase.ts
  - src/commands/index.ts
autonomous: true
requirements:
  - ROAD-02

must_haves:
  truths:
    - "User can run /openpaul:remove-phase with phase number"
    - "Command blocks removal of completed phases with clear error"
    - "Command blocks removal of current phase from STATE.md"
    - "Command removes phase entry, renumbers subsequent phases, and deletes directory"
    - "Command shows brief success message"
  artifacts:
    - path: "src/commands/remove-phase.ts"
      provides: "CLI command for removing phases"
      exports: ["paulRemovePhase"]
      min_lines: 100
  key_links:
    - from: "src/commands/remove-phase.ts"
      to: "src/roadmap/roadmap-manager.ts"
      via: "RoadmapManager.canRemovePhase and removePhase"
      pattern: "canRemovePhase|removePhase"
---

<objective>
Build /openpaul:remove-phase command that removes phases from the roadmap.

Purpose: Enable users to remove phases with automatic renumbering and cleanup.
Output: Working CLI command with safety validation and cleanup.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-roadmap-management/04-CONTEXT.md

@src/commands/init.ts
@src/commands/status.ts
@src/roadmap/roadmap-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement /openpaul:remove-phase command</name>
  <files>src/commands/remove-phase.ts</files>
  <action>
Create the remove-phase command following existing command patterns:

1. Use tool factory pattern with zod parameters:
   - phase: number (required) - Phase number to remove

2. Execute logic:
   - Create RoadmapManager with context.directory
   - Check ROADMAP.md exists (if not, return error: "ROADMAP.md not found. Run /openpaul:init first")
   - Determine STATE.md path (.paul/STATE.md or .openpaul/STATE.md)
   - Call roadmapManager.canRemovePhase(phaseNumber, statePath) to validate
   - If validation fails, return error listing all validation errors
   - Call roadmapManager.removePhase(phaseNumber)
   - Return success message: "Removed phase {number}"

3. Validation errors to display clearly:
   - "Phase X does not exist"
   - "Cannot remove completed phase X"
   - "Cannot remove current phase X"
   - "Cannot remove phase X with in-progress plans"

4. Error handling:
   - Wrap in try/catch
   - Return formatted error with troubleshooting list on failure
   - Use formatHeader, formatBold, formatList from '../output/formatter'

5. Follow naming convention: export as paulRemovePhase, tool name 'paul:remove-phase'

Reference CONTEXT.md locked decisions:
- Cannot remove completed phases
- Cannot remove current phase from STATE.md
- Cannot remove phase with in-progress plans
- All phase artifacts deleted on removal
- Renumbering only affects ROADMAP.md
- No --force override
  </action>
  <verify>
    <automated>npx tsc --noEmit src/commands/remove-phase.ts 2>&1 | head -20</automated>
  </verify>
  <done>Command compiles and implements all safety checks</done>
</task>

<task type="auto">
  <name>Task 2: Export remove-phase command from index</name>
  <files>src/commands/index.ts</files>
  <action>
Add export for paulRemovePhase from './remove-phase' to the commands index file.
  </action>
  <verify>
    <automated>grep -q "paulRemovePhase" src/commands/index.ts && echo "Export found"</automated>
  </verify>
  <done>paulRemovePhase is exported from commands module</done>
</task>

</tasks>

<verification>
- [ ] src/commands/remove-phase.ts exists with paulRemovePhase export
- [ ] Command accepts phase (required) parameter
- [ ] Command validates removal safety before proceeding
- [ ] Command returns validation errors clearly when blocked
- [ ] Command returns brief success message on completion
- [ ] src/commands/index.ts exports paulRemovePhase
</verification>

<success_criteria>
/openpaul:remove-phase command safely removes phases with proper validation and cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/04-roadmap-management/04-03-SUMMARY.md`
</output>
