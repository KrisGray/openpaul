---
phase: 04-roadmap-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/commands/add-phase.ts
  - src/commands/index.ts
autonomous: true
requirements:
  - ROAD-01

must_haves:
  truths:
    - "User can run /openpaul:add-phase with name and position flags"
    - "Command validates ROADMAP.md exists before proceeding"
    - "Command creates phase directory and updates ROADMAP.md"
    - "Command shows brief success message with new phase name and number"
  artifacts:
    - path: "src/commands/add-phase.ts"
      provides: "CLI command for adding phases"
      exports: ["paulAddPhase"]
      min_lines: 80
  key_links:
    - from: "src/commands/add-phase.ts"
      to: "src/roadmap/roadmap-manager.ts"
      via: "RoadmapManager import"
      pattern: "new RoadmapManager"
---

<objective>
Build /openpaul:add-phase command that adds new phases to the roadmap.

Purpose: Enable users to insert new phases at specified positions with automatic directory creation.
Output: Working CLI command with validation and success messaging.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-roadmap-management/04-CONTEXT.md

@src/commands/init.ts
@src/commands/status.ts
@src/roadmap/roadmap-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement /openpaul:add-phase command</name>
  <files>src/commands/add-phase.ts</files>
  <action>
Create the add-phase command following existing command patterns (see init.ts and status.ts):

1. Use tool factory pattern with zod parameters:
   - name: string (required) - Phase name
   - after: number (optional) - Insert after this phase number
   - before: number (optional) - Insert before this phase number

2. Validate inputs:
   - Exactly one of --after or --before must be provided (error if both or neither)
   - Name must be non-empty string

3. Execute logic:
   - Create RoadmapManager with context.directory
   - Check ROADMAP.md exists (if not, return error: "ROADMAP.md not found. Run /openpaul:init first")
   - Call roadmapManager.addPhase() with options
   - Return success message: "Added phase {number}: {name}"

4. Error handling:
   - Wrap in try/catch
   - Return formatted error with troubleshooting list on failure
   - Use formatHeader, formatBold, formatList from '../output/formatter'

5. Follow naming convention: export as paulAddPhase (internal name), but tool name should be 'paul:add-phase'

Reference CONTEXT.md locked decisions:
- Name is required argument
- Position specified with --after or --before flags
- Directory name auto-generated
- Brief success output only
  </action>
  <verify>
    <automated>npx tsc --noEmit src/commands/add-phase.ts 2>&1 | head -20</automated>
  </verify>
  <done>Command compiles and follows established patterns</done>
</task>

<task type="auto">
  <name>Task 2: Export add-phase command from index</name>
  <files>src/commands/index.ts</files>
  <action>
Add export for paulAddPhase from './add-phase' to the commands index file.
  </action>
  <verify>
    <automated>grep -q "paulAddPhase" src/commands/index.ts && echo "Export found"</automated>
  </verify>
  <done>paulAddPhase is exported from commands module</done>
</task>

</tasks>

<verification>
- [ ] src/commands/add-phase.ts exists with paulAddPhase export
- [ ] Command accepts name (required), after/before (one required) parameters
- [ ] Command validates ROADMAP.md existence
- [ ] Command returns brief success message
- [ ] src/commands/index.ts exports paulAddPhase
</verification>

<success_criteria>
/openpaul:add-phase command works with required arguments and shows brief success message.
</success_criteria>

<output>
After completion, create `.planning/phases/04-roadmap-management/04-02-SUMMARY.md`
</output>
