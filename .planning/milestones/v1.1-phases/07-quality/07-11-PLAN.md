---
phase: 07-quality
plan: "11"
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - src/commands/plan-fix.ts
  - src/tests/commands/plan-fix.test.ts
autonomous: true
requirements:
  - QUAL-02
gap_closure: true
must_haves:
  truths:
    - User is asked to confirm before auto-execution when --execute is requested
    - Auto-execution runs only after explicit confirmation
  artifacts:
    - path: src/commands/plan-fix.ts
      provides: Confirmation + execution gating for /openpaul:plan-fix
    - path: src/tests/commands/plan-fix.test.ts
      provides: Coverage for confirm + execute behavior
  key_links:
    - from: src/commands/plan-fix.ts
      to: /gsd-execute-phase
      via: execute flag with confirm gate
      pattern: "--execute.*--confirm"
---

<objective>
Close the plan-fix auto-execution confirmation gap.

Purpose: Ensure /openpaul:plan-fix asks for confirmation before running execute-phase and only auto-executes when explicitly confirmed.

Output: Updated command behavior + tests covering confirm/execute flow.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quality/07-CONTEXT.md
@.planning/phases/07-quality/07-RESEARCH.md
@.planning/phases/07-quality/07-VERIFICATION.md
@.planning/phases/07-quality/07-03-SUMMARY.md
@src/commands/plan-fix.ts
@src/tests/commands/plan-fix.test.ts
</context>

<tasks>

<task type="auto">
  <name>Add confirmation gate for --execute</name>
  <files>src/commands/plan-fix.ts</files>
  <action>
Update /openpaul:plan-fix to require explicit confirmation before auto-execution.

Behavior changes:
- Add a `confirm` boolean parameter to the Zod schema and PlanFixArgs.
- When `--execute` is set without `--confirm`, do NOT auto-run. Return a confirmation prompt that:
  - States a fix plan was created.
  - Explains auto-execution requires confirmation.
  - Provides the exact re-run command: `/openpaul:plan-fix --phase {phase} --issue {issue} --execute --confirm`.
- When `--execute` and `--confirm` are both set, perform the auto-execution step:
  - Use any available tool context execution helper (if present) to run `/gsd-execute-phase {phase}`.
  - If no execution helper is available, fall back to a clear message that it cannot execute automatically and show the command to run.
- Preserve existing output structure (formatHeader/formatBold/formatList usage) and do not change the non-execute path.

Keep behavior aligned with CONTEXT.md: create the plan first, then ask before execution.
  </action>
  <verify><automated>npx tsc --noEmit src/commands/plan-fix.ts</automated></verify>
  <done>plan-fix requires confirmation before auto-execution and only runs when confirmed</done>
</task>

<task type="auto">
  <name>Update plan-fix command tests for confirm flow</name>
  <files>src/tests/commands/plan-fix.test.ts</files>
  <action>
Extend tests to cover the new confirm gate:

- Add a test for `--execute` without `--confirm` that expects a confirmation prompt and the re-run command with `--confirm`.
- Add a test for `--execute --confirm` that expects the auto-execution path message (or the execution helper being invoked if mocked).
- Update the existing `--execute` test to match the new confirmation message if needed.

Mock any execution helper on the tool context if used, and assert it was called only when `--confirm` is true.
  </action>
  <verify><automated>npm test -- --testPathPattern=commands/plan-fix.test.ts</automated></verify>
  <done>Tests verify confirm gating and auto-execution behavior</done>
</task>

</tasks>

<verification>
- plan-fix shows a confirmation prompt before auto-execution
- auto-execution only proceeds after explicit confirmation
- plan-fix tests pass for confirm and execute paths
</verification>

<success_criteria>
- Running /openpaul:plan-fix with --execute requires confirmation before executing
- Running with --execute --confirm triggers the auto-execute branch
- Jest tests cover the confirmation gate
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality/07-11-SUMMARY.md`
</output>
