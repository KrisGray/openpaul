---
phase: 07-quality
plan: "05"
type: gap-closure
trigger: UAT test failures from Phase 9 branding update
wave: 1
depends_on: []
files_modified:
  - src/tests/commands/handoff.test.ts
  - src/tests/commands/pause.test.ts
  - src/tests/commands/plan-fix.test.ts
  - src/tests/commands/discuss-milestone.test.ts
  - src/tests/commands/research-phase.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - Branding prefix test expectations use /openpaul:
    - Branding rename import typos are corrected
  artifacts:
    - path: src/tests/commands/handoff.test.ts
      provides: Branding prefix assertions for handoff command
      min_lines: 80
    - path: src/tests/commands/pause.test.ts
      provides: Branding prefix assertions for pause command
      min_lines: 200
    - path: src/tests/commands/plan-fix.test.ts
      provides: Corrected branding import for plan-fix tests
      min_lines: 60
    - path: src/tests/commands/discuss-milestone.test.ts
      provides: Corrected branding import for discuss-milestone tests
      min_lines: 60
    - path: src/tests/commands/research-phase.test.ts
      provides: Corrected branding import for research-phase tests
      min_lines: 60
  key_links:
    - from: src/tests/commands/handoff.test.ts
      to: src/commands/handoff.ts
      via: "command invocation assertions"
      pattern: "openpaulHandoff"
    - from: src/tests/commands/pause.test.ts
      to: src/commands/pause.ts
      via: "command invocation assertions"
      pattern: "openpaulPause"
    - from: src/tests/commands/plan-fix.test.ts
      to: src/commands/plan-fix.ts
      via: "import rename"
      pattern: "openpaulPlanFix"
---

<objective>
Fix branding-related test failures identified in UAT.md.

**Root Cause:** Phase 9 branding update (BRND-01: paul → openpaul) was incomplete - tests were not fully updated to match the new command prefix, and some imports have typos from find-replace errors.

**Output:** Updated command test suites pass for branding prefixes and corrected imports.
</objective>

<context>
@.planning/phases/07-quality/07-UAT.md
@.planning/debug/uat-test-failures.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
<name>Fix branding prefix mismatches in tests</name>
<files>
src/tests/commands/handoff.test.ts
src/tests/commands/pause.test.ts
</files>
<action>
Update test expectations from `/paul:` to `/openpaul:` prefix:
1. handoff.test.ts line 73: `/paul:init` → `/openpaul:init`
2. pause.test.ts line 124: `/paul:resume` → `/openpaul:resume`
3. pause.test.ts line 125: `/paul:status` → `/openpaul:status`
4. pause.test.ts line 171: `/paul:resume` → `/openpaul:resume`
5. pause.test.ts line 197: `/paul:init` → `/openpaul:init`
</action>
<verify>npm test -- --testPathPattern="handoff.test.ts|pause.test.ts" passes with no failures</verify>
<done>All branding prefix tests updated and passing</done>
</task>

<task type="auto" tdd="false">
<name>Fix import name typos in test files</name>
<files>
src/tests/commands/plan-fix.test.ts
src/tests/commands/discuss-milestone.test.ts
src/tests/commands/research-phase.test.ts
</files>
<action>
Fix find-replace typos in imports:
1. plan-fix.test.ts line 9: `openpaulpaulPlanFix` → `openpaulPlanFix`
2. discuss-milestone.test.ts line 7: `openpaulpaulDiscussMilestone` → `openpaulDiscussMilestone`
3. research-phase.test.ts line 5: `openpaulpaulResearchPhase` → `openpaulResearchPhase`
</action>
<verify>npm test -- --testPathPattern="plan-fix.test.ts|discuss-milestone.test.ts|research-phase.test.ts" passes with no failures</verify>
<done>All import typos fixed, tests pass</done>
</task>

</tasks>

<verification>
- All targeted test suites pass:
  - npm test -- --testPathPattern="handoff.test.ts|pause.test.ts"
  - npm test -- --testPathPattern="plan-fix.test.ts|discuss-milestone.test.ts|research-phase.test.ts"
</verification>

<success_criteria>
- [ ] All branding prefix mismatches fixed (5 tests)
- [ ] All import typos fixed (3 test suites)
</success_criteria>

<output>
After completion:
1. Create SUMMARY.md documenting all fixes
</output>
