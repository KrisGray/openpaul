---
phase: 07-quality
plan: "09"
type: gap-closure
trigger: Remaining UAT test failures from Phase 9 branding update
wave: 2
depends_on:
  - "07-05"
files_modified:
  - package.json
  - src/utils/diff-formatter.ts
  - src/tests/commands/resume.test.ts
  - src/tests/utils/diff-formatter.test.ts
  - src/tests/utils/directory-scanner.test.ts
  - src/utils/directory-scanner.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - Diff dependency is installed for diff-formatter usage
    - Directory scanner cache validity test passes
    - Full Jest test suite passes with no failures
  artifacts:
    - path: package.json
      provides: diff dependency in dependencies
      min_lines: 40
    - path: src/tests/utils/directory-scanner.test.ts
      provides: isCacheValid test coverage
      min_lines: 120
    - path: src/utils/directory-scanner.ts
      provides: cache validation logic
      min_lines: 80
  key_links:
    - from: package.json
      to: src/utils/diff-formatter.ts
      via: "diff dependency for formatter"
      pattern: "\"diff\""
    - from: src/tests/utils/directory-scanner.test.ts
      to: src/utils/directory-scanner.ts
      via: "isCacheValid expectations"
      pattern: "isCacheValid"
---

<objective>
Resolve remaining UAT test failures and confirm the full suite passes.

Purpose: Close remaining branding-related gaps by installing missing dependencies, fixing cache validation, and validating full test run.

Output: All npm tests passing, UAT gaps closed.
</objective>

<context>
@.planning/phases/07-quality/07-UAT.md
@.planning/debug/uat-test-failures.md
@.planning/STATE.md
@src/utils/diff-formatter.ts
@src/tests/utils/diff-formatter.test.ts
@src/tests/commands/resume.test.ts
@src/utils/directory-scanner.ts
@src/tests/utils/directory-scanner.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
<name>Add missing 'diff' dependency</name>
<files>
package.json
</files>
<action>
Add the 'diff' package to dependencies. Run `npm install diff` or add to package.json. The diff-formatter.ts file imports from 'diff' package.
</action>
<verify>npm test -- --testPathPattern="resume.test.ts|diff-formatter.test.ts" passes with no failures</verify>
<done>'diff' dependency installed, related tests pass</done>
</task>

<task type="auto" tdd="false">
<name>Fix directory-scanner isCacheValid test</name>
<files>
src/tests/utils/directory-scanner.test.ts
src/utils/directory-scanner.ts
</files>
<action>
Investigate and fix the isCacheValid test failure at line 128. The test expects cache validity to work correctly but it's returning false unexpectedly. Review the cache validity logic and fix either the test expectation or the implementation.
</action>
<verify>npm test -- --testPathPattern="directory-scanner.test.ts" passes with no failures</verify>
<done>isCacheValid test passes</done>
</task>

<task type="auto" tdd="false">
<name>Run full test suite verification</name>
<files>N/A</files>
<action>
Run `npm test` to verify all 93 tests pass with no failures.
</action>
<verify>npm test exits with code 0, all tests pass</verify>
<done>All npm tests passing, UAT gaps closed</done>
</task>

</tasks>

<verification>
```bash
npm test
```
Should show all tests passing (93 tests, 0 failures).
</verification>

<success_criteria>
- [ ] 'diff' dependency installed (2 test suites)
- [ ] isCacheValid test fixed (1 test)
- [ ] Full npm test suite passes (93 tests, 0 failures)
- [ ] UAT.md gaps section cleared or marked resolved
</success_criteria>

<output>
After completion:
1. Create SUMMARY.md documenting all fixes
2. Update STATE.md with resolution
3. Update UAT.md to mark gaps as resolved
</output>
