---
phase: 07-quality
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements: [QUAL-01, QUAL-02]
gap_closure: true
must_haves:
  truths:
    - "Jest can resolve the diff dependency required by diff-formatter"
  artifacts:
    - path: "package.json"
      provides: "diff dependency for diff-formatter"
      contains: "\"diff\""
  key_links:
    - from: "package.json"
      to: "src/output/diff-formatter.ts"
      via: "diff dependency resolution"
      pattern: "\"diff\""
---

<objective>
Add the missing diff dependency so Jest can resolve diff-formatter.

Purpose: Close UAT gap for failing Jest suites caused by missing diff dependency.
Output: package.json includes diff dependency for diff-formatter usage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quality/07-UAT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Add diff dependency for diff-formatter</name>
  <files>package.json</files>
  <action>
Add the "diff" dependency to package.json so src/output/diff-formatter.ts can resolve it during Jest runs.
  </action>
  <verify>
    <automated>node -e "require('diff')"</automated>
  </verify>
  <done>package.json declares diff and Node can resolve it locally.</done>
</task>

</tasks>

<verification>
Run node -e "require('diff')" to ensure dependency resolves.</verification>

<success_criteria>
- diff dependency is installed and resolvable for diff-formatter usage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality/07-06-SUMMARY.md`
</output>
