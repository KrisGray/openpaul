---
phase: 02-core-loop-commands
plan: "08"
type: execute
wave: 6
depends_on:
  - "04"
files_modified:
  - src/index.ts
  - src/commands/unify.ts
  - src/output/formatter.ts
  - src/tests/commands/unify.test.ts
autonomous: true
requirements:
  - CORE-04
gap_closure: true
must_haves:
  truths:
    - "User can run /paul:unify from the plugin tool map"
    - "Summary output compares plan vs actual and includes criteria results"
    - "Loop transitions APPLY → UNIFY → PLAN remain intact"
  artifacts:
    - path: "src/index.ts"
      provides: "paul:unify tool registration"
    - path: "src/commands/unify.ts"
      provides: "Summary generation with plan-vs-actual comparison"
    - path: "src/output/formatter.ts"
      provides: "Comparison/criteria output formatting"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/unify.ts"
      via: "tool map registration"
      pattern: "'paul:unify'"
    - from: "src/commands/unify.ts"
      to: "src/output/formatter.ts"
      via: "summary output formatting"
      pattern: "format.*(summary|comparison|criteria)"
---

<objective>
Restore /paul:unify availability and enhance summaries with plan-vs-actual comparisons.

Purpose: Close remaining CORE-04 gaps so users can close loops and see reconciliation results.
Output: Registered unify command and enriched summary output with actuals/criteria.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-commands/02-CONTEXT.md
@.planning/phases/02-core-loop-commands/02-RESEARCH.md
@.planning/phases/02-core-loop-commands/02-VERIFICATION.md
@.planning/phases/02-core-loop-commands/02-04-SUMMARY.md
@.planning/phases/02-core-loop-commands/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: register /paul:unify in plugin tool map</name>
  <files>src/index.ts</files>
  <action>
Add the missing `paul:unify` registration in the plugin tool map so the command is available at runtime. Keep naming consistent with existing command registrations and ensure the export comes from `src/commands/unify.ts`.
  </action>
  <verify>npm test -- src/tests/commands/unify.test.ts</verify>
  <done>Running /paul:unify is possible because the tool is registered in src/index.ts.</done>
</task>

<task type="auto">
  <name>task 2: add plan-vs-actual and criteria results to unify summary</name>
  <files>src/commands/unify.ts, src/output/formatter.ts</files>
  <action>
Extend /paul:unify to accept optional `actuals` and `criteriaResults` arguments and include a reconciliation section in the output:
- Parse and validate `actuals` and `criteriaResults` (string arrays or structured objects as used in tests).
- Compare plan tasks vs actuals to show deltas (e.g., missing tasks, extra tasks, status overrides).
- Render criteria results with pass/fail markers and include in summary output.
- Preserve existing state transitions APPLY → UNIFY → PLAN and existing summary writing behavior.

Use formatter helpers to keep output consistent with Phase 2 rich formatting guidelines.
  </action>
  <verify>npm test -- src/tests/commands/unify.test.ts</verify>
  <done>/paul:unify output includes a plan-vs-actual comparison section and criteria results when provided.</done>
</task>

<task type="auto">
  <name>task 3: update unify tests for reconciliation output</name>
  <files>src/tests/commands/unify.test.ts</files>
  <action>
Update unify command tests to cover:
- Tool registration availability (via plugin tool map or export where appropriate)
- New args for actuals/criteriaResults
- Presence of comparison and criteria sections in output

Keep existing tests for state transitions and summary writing intact.
  </action>
  <verify>npm test -- src/tests/commands/unify.test.ts</verify>
  <done>Unify tests pass and verify reconciliation output includes actuals and criteria results.</done>
</task>

</tasks>

<verification>
Run the unify command suite: `npm test -- src/tests/commands/unify.test.ts`.
</verification>

<success_criteria>
- /paul:unify is registered and callable.
- Summary output compares plan vs actual and shows criteria results.
- State transitions remain unchanged and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-commands/02-08-SUMMARY.md`.
</output>
