---
phase: 16-scaffold-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/cli/output.ts, src/cli.ts]
autonomous: true
requirements: [UX-01, UX-02]
gap_closure: true

must_haves:
  truths:
    - "CLI shows branding and welcome message before prompting for project name"
    - "User sees 'Operation cancelled' message when declining overwrite or confirmation prompt"
  artifacts:
    - path: "src/cli/output.ts"
      provides: "Banner and notice output functions"
      exports: ["showBanner", "notice"]
    - path: "src/cli.ts"
      provides: "CLI entry point with banner display and cancellation feedback"
      contains: "showBanner"
  key_links:
    - from: "src/cli.ts"
      to: "showBanner()"
      via: "direct call after setVerbosity()"
      pattern: "showBanner\\(pkg\\.version\\)"
    - from: "src/cli.ts"
      to: "notice()"
      via: "cancellation messages"
      pattern: "notice\\('Operation cancelled'\\)"
---

<objective>
Add welcome banner and cancellation feedback to CLI UX.

Purpose: Improve first impression with branded welcome message and ensure users always see cancellation confirmation regardless of verbosity level.
Output: Enhanced output.ts with showBanner() and notice() functions, updated cli.ts to use them.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap sources
@.planning/phases/16-scaffold-core/16-UAT.md

# Existing implementations
@src/cli/output.ts
@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Add showBanner() and notice() functions to output.ts</name>
  <files>src/cli/output.ts</files>
  <action>
Add two new exported functions to output.ts:

1. **showBanner(version: string): void** - Display ASCII art banner with OpenPAUL branding
   - Use picocolors for cyan/bold styling on "OpenPAUL" text
   - Show version number below the banner in dim style
   - Add blank line after banner for spacing
   - Example ASCII art pattern (simple, clean):
     ```
       ____  ____   __   ______  ____   ______  __  __  ______
      / __ \/  _/  / /  / ____/ / __ \ / ____/ / / / / / ____/
     / / / // /   / /  / /_    / /_/ // /     / /_/ / / /_    
    / /_/ // /   / /  / __/   / _, _// /___  / __  / / __/    
   /_____/___/  /_/  /_/     /_/ |_| \____/ /_/ /_/ /_/       
     ```
   - Keep it under 60 chars wide for terminal compatibility

2. **notice(message: string): void** - Output message always (no verbosity gate)
   - Use picocolors yellow for "ℹ" icon (same style as info but always visible)
   - Output to console.log
   - This is for user-facing messages that must always appear

Both functions should follow the existing code style in output.ts.
  </action>
  <verify>
<verify>
  <automated>grep -E "export function (showBanner|notice)" src/cli/output.ts && echo "Functions exist"</automated>
  <manual>npx tsx src/cli.ts --help should show banner output before help</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
  </verify>
  <done>showBanner() and notice() functions exist in output.ts and are exported</done>
</task>

<task type="auto">
  <name>Wire banner and notice into CLI entry point</name>
  <files>src/cli.ts</files>
  <action>
Update src/cli.ts with two changes:

1. **Import showBanner and notice from output.js:**
   - Add to existing import: `import { success, step, info, setVerbosity, showBanner, notice } from './cli/output.js'`

2. **Call showBanner immediately after setVerbosity():**
   - Insert `showBanner(pkg.version)` right after `setVerbosity(options.verbose)` (line 40)
   - This ensures banner shows before any info messages or prompts

3. **Replace info() with notice() for cancellation messages:**
   - Line 55: Change `info('Operation cancelled')` to `notice('Operation cancelled')`
   - Line 84: Change `info('Operation cancelled')` to `notice('Operation cancelled')`
   - This ensures users always see cancellation feedback (info is gated by -v flag)

Do NOT change any other info() calls - only the cancellation messages need notice().
  </action>
  <verify>
<verify>
  <automated>grep -E "showBanner\(pkg\.version\)" src/cli.ts && grep -E "notice\('Operation cancelled'\)" src/cli.ts | wc -l | xargs -I {} sh -c 'test {} -eq 2 && echo "Both notice calls found"'</automated>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
  </verify>
  <done>Banner displays on CLI invocation, cancellation messages always visible to users</done>
</task>

</tasks>

<verification>
1. Run `npx tsx src/cli.ts` in fresh directory - should see ASCII banner before project name prompt
2. Run `npx tsx src/cli.ts` in existing .openpaul directory, decline overwrite - should see "Operation cancelled" message
3. Run `npx tsx src/cli.ts` in fresh directory, confirm name, decline final confirmation - should see "Operation cancelled" message
4. Run `npx tsx src/cli.ts -v` - verbose info messages should appear alongside banner
</verification>

<success_criteria>
- ASCII banner displays when running `npx openpaul` with OpenPAUL branding
- Version number shows below banner
- "Operation cancelled" message appears when user declines any prompt
- Message appears regardless of -v flag presence
</success_criteria>

<output>
After completion, create `.planning/phases/16-scaffold-core/16-03-SUMMARY.md`
</output>
