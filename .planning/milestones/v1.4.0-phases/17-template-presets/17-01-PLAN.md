---
phase: 17-template-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/presets.ts
  - src/cli/templates/minimal.ts
  - src/cli/templates/full.ts
autonomous: true
requirements:
  - SCAF-03
must_haves:
  truths:
    - "Preset types are defined and type-safe"
    - "Minimal preset contains opencode.json scaffold and .gitkeep files"
    - "Full preset contains opencode.json, tui.json, example command, and example rule"
    - "resolvePreset() returns correct preset for valid names"
    - "resolvePreset() prompts for selection on unknown preset"
    - "resolvePreset() defaults to minimal when undefined"
  artifacts:
    - path: "src/cli/presets.ts"
      provides: "Preset type definitions and resolution logic"
      min_lines: 40
    - path: "src/cli/templates/minimal.ts"
      provides: "Minimal preset template content"
      contains: "minimalPreset"
    - path: "src/cli/templates/full.ts"
      provides: "Full preset template content"
      contains: "fullPreset"
  key_links:
    - from: "src/cli/presets.ts"
      to: "src/cli/templates/minimal.ts"
      via: "import minimalPreset"
      pattern: "import.*minimalPreset"
    - from: "src/cli/presets.ts"
      to: "src/cli/templates/full.ts"
      via: "import fullPreset"
      pattern: "import.*fullPreset"
---

<objective>
Define preset types, templates, and resolution logic for minimal and full OpenCode configurations.

Purpose: Provide type-safe preset definitions that the CLI can use to scaffold OpenCode configurations.
Output: Preset module with types, two template files, and resolution function.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-template-presets/17-CONTEXT.md
@.planning/phases/17-template-presets/17-RESEARCH.md
@src/cli/output.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create preset types and resolution module</name>
  <files>src/cli/presets.ts</files>
  <action>
Create `src/cli/presets.ts` with:
1. `PresetName` type as `'minimal' | 'full'`
2. `PresetFile` interface with `path: string` and `content: string`
3. `Preset` interface with `name: PresetName`, `description: string`, and `files: PresetFile[]`
4. `PRESETS` const object mapping preset names to preset objects (import from templates)
5. `isValidPreset(value: string): value is PresetName` type guard
6. `resolvePreset(presetArg: string | undefined): Promise<Preset>` async function that:
   - Returns minimal preset with notice "Defaulting to minimal" when presetArg is undefined
   - Returns named preset with info "Preset: {name}" and description when valid
   - Prompts with @inquirer/prompts select() when unknown preset value provided
   - Uses notice() for unknown preset message before prompting

Import select from '@inquirer/prompts', notice and info from './output.js'.
Export all types and functions needed by CLI.

Do NOT include any API keys, model preferences, or user-specific settings in preset logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/cli/presets.ts 2>&1 | head -20</automated>
    <manual>TypeScript compiles without errors</manual>
  </verify>
  <done>Preset types defined, resolution logic handles all three cases (default, valid, unknown)</done>
</task>

<task type="auto">
  <name>task 2: Create minimal preset template</name>
  <files>src/cli/templates/minimal.ts</files>
  <action>
Create `src/cli/templates/minimal.ts` with:

```typescript
import type { Preset } from '../presets.js'

export const minimalPreset: Preset = {
  name: 'minimal',
  description: 'Essential OpenCode configuration with empty directory structure',
  files: [
    { path: 'opencode.json', content: '{"$schema":"https://opencode.ai/config.json"}' },
    { path: 'agents/.gitkeep', content: '' },
    { path: 'commands/.gitkeep', content: '' },
    { path: 'rules/.gitkeep', content: '' },
    { path: 'skills/.gitkeep', content: '' }
  ]
}
```

Format opencode.json with 2-space indentation (JSON.stringify with null, 2).
Include skills/.gitkeep for consistency with other directories.
NEVER include API keys, model configurations, or user-specific settings.
  </action>
  <verify>
    <automated>node -e "import('./src/cli/templates/minimal.js').then(m => console.log(m.minimalPreset.files.length))" 2>&1</automated>
    <manual>Should output 5 (file count)</manual>
  </verify>
  <done>Minimal preset exported with 5 files: opencode.json + 4 .gitkeep files</done>
</task>

<task type="auto">
  <name>task 3: Create full preset template</name>
  <files>src/cli/templates/full.ts</files>
  <action>
Create `src/cli/templates/full.ts` with full preset containing:

1. `opencode.json` - same as minimal (schema only)
2. `tui.json` - with schema reference only
3. `commands/example.md` - single example command with frontmatter:
   - description: "Example custom command"
   - agent: "build"
   - body: "This is an example command. Customize it for your project needs."
4. `rules/example.md` - single example rule:
   - title: "# Example Rule"
   - body: "This is an example rule file. Add your project-specific guidelines here."
5. `agents/.gitkeep` - empty file
6. `skills/.gitkeep` - empty file

The full preset description: "Complete OpenCode configuration with example command and rule"

Format JSON files with 2-space indentation.
NEVER include API keys, model names, permissions, or keybinds.
  </action>
  <verify>
    <automated>node -e "import('./src/cli/templates/full.js').then(m => console.log(m.fullPreset.files.length))" 2>&1</automated>
    <manual>Should output 6 (file count)</manual>
  </verify>
  <done>Full preset exported with 6 files including example command and rule</done>
</task>

</tasks>

<verification>
1. TypeScript compilation succeeds for all new files
2. minimalPreset.files contains 5 files including opencode.json
3. fullPreset.files contains 6 files including example command and rule
4. No API keys or user-specific settings in any template
5. Preset descriptions are 1-2 lines as per CONTEXT.md
</verification>

<success_criteria>
- Preset types defined in src/cli/presets.ts
- Minimal preset has essential .opencode/ structure
- Full preset has examples without sensitive data
- resolvePreset() handles default, valid, and unknown cases
</success_criteria>

<output>
After completion, create `.planning/phases/17-template-presets/17-01-SUMMARY.md`
</output>
