---
enabled: []
disabled:
  - security-review
  - docs-refresh
  - release-audit
  - data-migration
---

# Specialized Flows

**Project:** {{project_name}}
**Created:** {{timestamp}}
**Last Updated:** {{timestamp}}

---

## Overview

This file controls specialized workflow toggles for OpenPAUL. Only the YAML frontmatter
above is parsed; the markdown below is for reference and is preserved on updates.

## Flow Catalog

Valid flow IDs and their purpose. Use these IDs in the enabled/disabled arrays.

| Flow ID | Description |
|---------|-------------|
| security-review | Require an explicit security review for sensitive changes. |
| docs-refresh | Trigger documentation updates for user-facing changes. |
| release-audit | Run a release readiness audit before tagging releases. |
| data-migration | Require migration checks for schema or data changes. |

---

## Usage

- Add a flow ID to `enabled` to turn it on.
- Add a flow ID to `disabled` to keep it off.
- A flow ID cannot appear in both arrays.
- Unknown flow IDs are rejected by `/openpaul:flows`.

## Validation Rules

- Flow IDs must match the catalog exactly.
- Keep enabled and disabled lists free of duplicates.
- Use `/openpaul:flows` to update lists safely.
- Keep flow IDs sorted for readability.

---

## Amendment History

| Date | Change | Reason |
|------|--------|--------|
| {{timestamp}} | Initial creation | Project setup |

---

*Generated by `/openpaul:flows` or `/openpaul:init`*
